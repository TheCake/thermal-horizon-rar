"""
STAGE 10G -- O5-COLLECTIVE: THE PARTICIPATION CEILING (ROUND-26
condition 6, THE STRIKE-BEARING SUCCESSOR; author call 2026-08-09
"Compacted. You are go for hunt").

THE PRE-STATED KILL (ROUND 26, adopted verbatim): the mechanism
closes iff EITHER
  (a) collective amplification is derived and lifts lambda to
      saturation (lambda >~ H; the 9W lambda-bar = sqrt(sum lam_k^2)
      sqrt(K)-question = the Axis-1 participation residual), OR
  (b) a non-saturated real-exchange process is shown to imprint the
      tail-weighting e^{-Lx} on the system's DRESSING (not merely
      leave the ambient thermal).
IF BOTH FAIL, THE STRIKE FIRES (bath-mechanism conditional 15 -> 8),
upon ROUND-27 affirmation.

THE ANSWER THIS STAGE COMPUTES (clause (a)): the sqrt(K) question is
closed by a SUM-RULE CEILING. Chain (each step gated):
  (1) any lending-partner mode set couples through the SINGLE l=2
      port (GF-2 rank-1, 10F-banked) => bright-mode reduction
      lambda-bar^2 = sum_k lambda_k^2 (9W; gate G1b).
  (2) per-mode 10E lemma: lambda_k = (eta2/4) e_s c_k sqrt(omega
      Omega_k), c_k = mode k's zero-point amplitude of the ONE
      fractional-quadrupole field at the system (10E's e_a is the
      single-mode c; gate G3 regression pins the convention).
  (3) dress-ward lending RATE (6X absorption-ward convention, the
      same enhancement the 10F table used): rate^2 = (n_s + 1) *
      sum_k lambda_k^2 n_k. (The (n_k+1) emission-ward channel is
      the 9X leak family, not lending -- disclosed.)
  (4) THE BUDGET (the load-bearing premise, disclosed): the modes'
      contributions superpose in ONE observable field, so
      <dq^2> = sum_k c_k^2 (2 n_k + 1) <= q_phys^2, with q_phys ~ 1
      the harmonic-chart validity bound (the 10E lemma is linear
      order in the distortion; the measured static is 0.086; beyond
      O(1) fractional fluctuation NO derived vertex exists at all --
      THE DILEMMA: q_phys >> 1 does not rescue clause (a), it
      underives the coupling).
  (5) THE KMS PIN (measurement-anchored, not a choice): n_k =
      n_BE(Omega_k / T_dS), T_dS = H/2pi -- the partner's statistics
      ARE the measured gate (6U detailed balance; the a0 temperature
      lock): a non-KMS lending partner would imprint non-KMS gate
      ratios and break the measured temperature lock. (Non-KMS
      robustness row still printed, with the inconsistency note.)
  (6) => rate^2 <= pref^2 omega (n_s+1) q_phys^2 N_p * sup_Omega
      [Omega n/(2n+1)] and the sup is EXACT: Omega n/(2n+1) =
      Omega/(e^{Omega/T}+1) (the Fermi function), maximized at
      x* = Omega/T solving x-1 = e^{-x} (x* = 1.27846...), with
      f* = x* - 1 = e^{-x*} = 0.27846... EXACTLY. So
        rate_max = (eta2/4) e_s sqrt(omega (n_s+1) f* T_dS)
                   * q_phys * sqrt(N_p).
      K NEVER APPEARS: participation redistributes the budget,
      never amplifies it. sqrt(K) growth would need K modes EACH at
      full amplitude = <dq^2> ~ K(2n+1) -- forbidden by (4).

CLAUSE (b): the dynamical leg, run on the 6X engine itself (gate
G6): evolve the VERBATIM 6X construction (|1> prep x thermal
ambient, closed dynamics -- non-equilibrium at physical lambda per
trap #17) at the ceiling's own Rabi angle theta_c = 2 pi rate_max
t_univ (t_univ = 0.96/H; R-DYN convention: lambda = 0.5 H <=> angle
pi per Hubble). Measure the lending weight actually reached vs the
saturated archived values, and its n-structure (D4: up-channel
|me|^2 = 2m => short-time weights w(m)/w(1) = m, raw-n). Bars
pre-stated below. Plus the sector sweep (gate G7) closing the
OTHER partner ontologies: (ii) free radiation (10A: 34-44 orders,
archived U's), (iii) matter kinematics (frequency chart: 360-1e5 H
=> KMS-empty at ~1000 decimal orders + c-number character + the 10B
secular dictionary-kill), (iv) coherent/classical partners (mean
rides the MEASURED static 0.086 => the original 2.4-3.4-order
shortfall; Poisson-gate p-postdiction lean row; pure drives carry
no n-statistics at all => no measured e_N-running), (v) no partner
(= G6). Sector partition (i) bound-cloud modes / (ii) free fields /
(iii) matter dof exhausts the ontology; (iv)/(v) cover state and
coupling character -- the partition itself is offered to the round
for attack.

GATES (trap #16 -- for EVERY gate, the outcome that would FAIL it
is stated inline; trap #12 -- every letter clause wired):
  G1a (STOP) budget identity: thermal <(b+b')^2> = 2n+1 (Fock sum,
      NB = 400, |d| <= 1e-9 at n = 0.502 and 6.63); two-mode
      additivity numeric (product thermal, |d| <= 1e-9). FAILS if
      the variance formula or independence additivity is wrong.
  G1b (STOP) bright-mode reduction: K = 8 random couplings; the
      one-excitation block has eigenvalues {+-lambda-bar, 0 x 7} to
      1e-12. FAILS if lambda-bar != sqrt(sum lam_k^2).
  G2  (STOP) the Fermi optimum: x* - 1 = e^{-x*} nsolve to 1e-10;
      f* = x* - 1 identity checked symbolically; 20 random discrete
      spectra (40 modes each): budget-constrained LP max of
      sum c^2 Omega n never exceeds f* T by > 1e-9, and a spectrum
      holding a mode within 1% of Omega* achieves >= 0.995 f* T.
      FAILS if any spectrum beats the claimed sup.
  G3  (STOP) 10F regression: my formulas reproduce the ARCHIVED
      10F GF-3 table (six anchors, bare lo/hi + enh lo/hi) to
      <= 2e-3 abs vs printed 3-dp values; the single-mode allocation
      (all budget at Omega_amb, c1 = 1) reproduces the enh values
      EXACTLY (<= 1e-9 vs re-evaluation). Prints the implied-budget
      row: the 10F e_a = 1 corner carries <dq^2> = 2 n_amb + 1 =
      2.00 (bin) / 14.26 (gal) = RMS 142% / 378% fractional
      distortion -- the R25 escape corner was already past the
      chart premise (report-grade; feeds the dilemma).
  G4  (physics; POWER both directions) the ceiling table: six
      anchors x {HONEST: eta2 = 0.765 central, N_p = 1 (GF-2's own
      rank-1); MAX-GRANT: eta2 = 1.0, N_p = 5 (all five sector
      components granted as ports despite GF-2)} at q_phys = 1;
      shortfall = 0.5 H / rate. Robustness rows (both flank
      anchors, MAX-GRANT): (r1) LOOSEST BUDGET (normal-ordered +
      ZP-cap; rate x sqrt(1/f*) = x1.895) -- STRIKE-LEG-grade,
      bar >= 3x, with the note that it licenses unbounded
      zero-point amplitude at empty modes = chart-dead a fortiori;
      (r2) NON-KMS partner (occupation freed; window variants
      Lambda = 0.5 H and 1 H; rate x2.38/x3.36) -- REPORT-grade
      ONLY, excluded from the strike condition BY SCOPING: a
      partner with non-KMS statistics imprints a non-KMS gate and
      a broken temperature lock -- even saturated it produces a
      gate the sky did not measure, so it cannot close THE
      mechanism under adjudication (6U + a0-lock anchored; the
      scoping is offered to the round for attack).
      Closure surface q_phys*(grant) printed. POWER: counterfactual
      q_phys = 120 must flip the letter logic to C-CLOSES-CANDIDATE
      (printed); FAILS if the instrument cannot emit CLOSES, or if
      any anchor reaches 0.5 H at q_phys <= 1 (then CLOSES is the
      verdict -- the gate can fail toward closure).
  G6  (physics) the dynamical leg, 6X engine verbatim: (SB-4a STOP)
      the gamma = 0 sat_weight machinery reproduces the six
      archived 10C G7 values to 2e-4; (SB-4b STOP) the TIME-AVERAGED
      evolved P2 at strong coupling matches sat_weight to 5% rel
      (the dynamical route does saturate -- power); (SB-6 physics)
      at the ceiling angles: P2_reached/P2_sat <= 1% (HONEST rows)
      / <= 5% (MAX-GRANT rows) -- FAILS (toward GRAY/CLOSES) if the
      ceiling coupling saturates more than that; structure probe at
      theta = 0.2: w(m)/w(1) = m within 5% for m = 2..4 (raw-n;
      FAILS if the weights follow the gate P(n>=1) instead);
      pi-row printed (what closure would deliver: the instrument
      distinguishes).
  G7  (physics) sector rows as above; FAILS if any row's arithmetic
      leaves a partner sector within reach (then GRAY + named
      residual).
  G8  truncation/solver hygiene: NB probes +16/+20 drift < 1% on
      the evolved rows and < 2e-4 on sat rows.

LETTER GRAMMAR (pre-registered; bars locked BEFORE run):
  STOP-class: G1a/G1b/G2/G3/SB-4a/SB-4b/G8 fail => abort, fix,
      re-run (machinery, not physics).
  C-CLOSES-CANDIDATE: any anchor's rate >= 0.5 H at q_phys <= 1
      (any grant level) => clause (a) closes pending round.
      Pre-signed: + ROUND-27 clean => mech 15 -> 19.
  C-STRIKE-CANDIDATE: ALL six anchors shortfall >= 5x at MAX-GRANT
      AND the loosest-budget row >= 3x at both flanks (the non-KMS
      row is report-grade per its scoping) AND G6 within bars
      (weight <= bars, raw-n structure confirmed) AND all G7
      sector rows closed => BOTH kill clauses met per R26 =>
      THE STRIKE FIRES upon ROUND-27 affirmation: mech 15 -> 8
      (REQUIRED, no discretion -- the R26 signature). If ROUND 27
      finds an unpatched hole in the kill case: HOLD 15, the hole
      = the named successor (adjudication-(B) default).
  C-SURFACE-GRAY: anything else (an anchor lands in [0.1, 0.5] H,
      a robustness row < 3x, a G6/G7 leg unresolved) => HOLD 15 +
      named residual (successor O5-ANHARM: derive <dq^2>
      horizon-side).
  anomaly-real 53 UNTOUCHED in every branch (no sky fits).

DISCLOSURES: (i) q_phys <= 1 = the chart premise, load-bearing,
carried by the dilemma (beyond it the 10E linear lemma underives
the vertex; supporting data legs cite-grade only: the 6W vector-
composition measurement and the binary width budget constrain O(1)
quasi-static l=2 scrambles at the binary anchor); (ii) per-mode
prefactor granted equal (eta2 <= 1 by overlap normalization;
MAX-GRANT covers it); (iii) rate convention absorption-ward
(6X/10F-matched via G3); (iv) t_univ = 0.96/H (13.8/14.36 Gyr);
(v) T_dS = H/2pi = 0.159155 H exact in-units; (vi) the toy mapping
hands the ceiling rate to EVERY channel (conservative toward
closure: the toy's own sqrt(2m) enhancements then run on top);
(vii) system-side (n_s+1) rides free at the anchor's own value
(the mechanism's nu - 1 identity; toward closure); (viii) e_s
inherited from 10E unchanged; (ix) no new sky statistics anywhere.

Writes data/stage10g_collective.txt.
"""
import math, time
import numpy as np
import sympy as sp

T00 = time.time()
OUT = []
def say(s=''):
    OUT.append(s); print(s, flush=True)

say("=" * 78)
say("STAGE 10G -- O5-COLLECTIVE: the participation ceiling (the "
    "strike-bearing round)")
say("=" * 78)

# archived comparators (sources pinned for the round)
REQ = 0.50                    # R-DYN: lambda >= 0.5 H (10F T4, R26-affirmed)
T_UNIV = 0.96                 # 13.8/14.36 Gyr in 1/H
T_DS = 1.0/(2.0*math.pi)      # dS temperature H/2pi in H units
ETA_C, ETA_MAX = 0.765, 1.0   # 10E eta2 central / max
Q_STATIC = 0.086              # measured static EFE quadrupole
ARCH_G7 = [(0.25, 0.09979), (0.5, 0.16621), (1.0, 0.24900),
           (2.0, 0.33114), (4.0, 0.39524), (8.0, 0.43449)]
# 10F GF-3 printed table (bare lo/hi, enh lo/hi), six anchors:
ARCH_10F = {
    'binary  x_loc=0.5':    (0.207, 0.383, 0.234, 0.432),
    'binary  x_loc=1.0':    (0.052, 0.096, 0.046, 0.085),
    'binary  x_loc=2.0':    (0.013, 0.024, 0.010, 0.018),
    'galaxy  x_loc=0.0953': (0.062, 0.115, 0.531, 0.980),
    'galaxy  x_loc=0.3':    (0.062, 0.115, 0.314, 0.580),
    'galaxy  x_loc=1.0':    (0.062, 0.115, 0.201, 0.372),
}
U_10A_BIN, U_10A_GAL = 7.2e-45, 1.23e-34   # 10A radiative-carrier weights

def nbe(x): return 1.0/math.expm1(x)

ANCH = [   # (name, x_loc, x_amb, n_amb, e_s)  -- 10E/10F verbatim
    ('binary  x_loc=0.5',    0.5,    1.0954, 0.502, 1.0),
    ('binary  x_loc=1.0',    1.0,    1.0954, 0.502, 0.25),
    ('binary  x_loc=2.0',    2.0,    1.0954, 0.502, 0.0625),
    ('galaxy  x_loc=0.0953', 0.0953, 0.1411, 6.63,  0.3),
    ('galaxy  x_loc=0.3',    0.3,    0.1411, 6.63,  0.3),
    ('galaxy  x_loc=1.0',    1.0,    0.1411, 6.63,  0.3),
]

# ==================== G1a: the budget identity ====================
say("")
say("G1a THE BUDGET IDENTITY <dq^2> = sum c_k^2 (2 n_k + 1):")
ok1a = True
for nb_ in (0.502, 6.63):
    NBI = 400
    xx = math.log(1.0 + 1.0/nb_)
    p = np.exp(-xx*np.arange(NBI)); p /= p.sum()
    m = np.arange(NBI)
    var = float(np.sum(p*(2*m + 1.0)))    # <(b+b')^2> on thermal diag
    d = abs(var - (2*nb_ + 1.0))
    ok1a = ok1a and d <= 1e-9
    say(f"  n = {nb_:5.3f}: <(b+b')^2> = {var:.9f} vs 2n+1 = "
        f"{2*nb_+1:.9f}  |d| = {d:.1e}")
# two-mode additivity (numeric product-thermal, q = c1 q1 + c2 q2)
c1_, c2_, na_, nb2_ = 0.37, 0.81, 0.502, 2.0
NB1 = 160
def tvar(nn, NN):
    xx = math.log(1.0 + 1.0/nn)
    p = np.exp(-xx*np.arange(NN)); p /= p.sum()
    return float(np.sum(p*(2*np.arange(NN) + 1.0)))
tot = c1_**2*tvar(na_, NB1) + c2_**2*tvar(nb2_, NB1)
ref = c1_**2*(2*na_ + 1) + c2_**2*(2*nb2_ + 1)
d = abs(tot - ref)
ok1a = ok1a and d <= 1e-9
say(f"  two-mode: c = (0.37, 0.81), n = (0.502, 2.0): sum = {tot:.9f} "
    f"vs formula {ref:.9f}  |d| = {d:.1e}")
say(f"  G1a -> {'PASS' if ok1a else 'FAIL (STOP)'}  [fails if the "
    "variance or additivity is wrong]")

# ==================== G1b: bright-mode reduction ====================
say("")
say("G1b THE BRIGHT-MODE REDUCTION lambda-bar = sqrt(sum lam_k^2) "
    "(K = 8):")
rng = np.random.default_rng(107)
lams = rng.uniform(0.1, 1.0, size=8)
lbar = math.sqrt(float(np.sum(lams**2)))
M1 = np.zeros((9, 9))
M1[0, 1:] = lams; M1[1:, 0] = lams
ev = np.sort(np.linalg.eigvalsh(M1))
d_lo, d_hi = abs(ev[0] + lbar), abs(ev[-1] - lbar)
d_mid = float(np.max(np.abs(ev[1:-1])))
ok1b = (d_lo <= 1e-12) and (d_hi <= 1e-12) and (d_mid <= 1e-12)
say(f"  one-excitation block eigenvalues: min = {ev[0]:+.6f}, max = "
    f"{ev[-1]:+.6f}, |mid|max = {d_mid:.1e}")
say(f"  lambda-bar = {lbar:.6f}; |d| = ({d_lo:.1e}, {d_hi:.1e})")
say(f"  G1b -> {'PASS' if ok1b else 'FAIL (STOP)'}  [fails if "
    "lambda-bar != sqrt(sum lam_k^2)]")
say("  => sqrt(K) NEVER stands alone: K modes at full amplitude would")
say("     need <dq^2> ~ K(2n+1) -- the budget forbids it; participation")
say("     REDISTRIBUTES the fixed sum, never amplifies it.")

# ==================== G2: the Fermi optimum ====================
say("")
say("G2 THE OCCUPATION-BUDGET TRADE (exact optimum):")
xs = sp.symbols('x', positive=True)
f_sym = xs/(sp.exp(xs) + 1)
xstar = float(sp.nsolve(sp.diff(f_sym, xs), 1.3))
fstar = xstar/(math.exp(xstar) + 1.0)
id_ok = abs((xstar - 1.0) - math.exp(-xstar)) <= 1e-10
id2_ok = abs(fstar - (xstar - 1.0)) <= 1e-10
say(f"  weight per unit budget: Omega n/(2n+1) = Omega/(e^(Omega/T)+1)"
    f"  [the FERMI function]")
say(f"  x* = {xstar:.10f}  (x* - 1 = e^-x*: |d| = "
    f"{abs((xstar-1)-math.exp(-xstar)):.1e})")
say(f"  f* = x*/(e^x*+1) = {fstar:.10f} = x* - 1 EXACTLY "
    f"(|d| = {abs(fstar-(xstar-1)):.1e})")
ok2 = id_ok and id2_ok
# random-spectrum LP spot check: max sum c^2 Omega n s.t. sum c^2(2n+1)<=1
worst, best_hit = 0.0, 0.0
for tr in range(20):
    Om = rng.uniform(0.01, 8.0, size=40)*T_DS
    if tr == 0:
        Om[7] = xstar*T_DS*1.001          # one mode near Omega*
    nk = 1.0/np.expm1(Om/T_DS)
    ratio = Om*nk/(2*nk + 1.0)            # per-budget weight; LP vertex
    got = float(np.max(ratio))            # optimum concentrates there
    worst = max(worst, got - fstar*T_DS)
    if tr == 0:
        best_hit = got/(fstar*T_DS)
ok2 = ok2 and (worst <= 1e-9) and (best_hit >= 0.995)
say(f"  20 random 40-mode spectra: max excess over f*T = {worst:.2e} "
    f"(bar 1e-9); near-Omega* spectrum reaches {best_hit:.4f} of f*T "
    f"(bar >= 0.995)")
say(f"  G2 -> {'PASS' if ok2 else 'FAIL (STOP)'}  [fails if any "
    "spectrum beats the claimed sup]")
say("  => THE CEILING: rate_max = (eta2/4) e_s sqrt(omega (n_s+1) "
    "f* T_dS) q_phys sqrt(N_p)")
say("     (K-free; thermal occupation cancels: every quantum of rate")
say("      enhancement is bought by budget suppression -- the bath")
say("      cannot fund both the loan and the collateral.)")

# ==================== G3: 10F regression + implied budget ============
say("")
say("G3 10F REGRESSION (archived GF-3 table) + the implied-budget row:")
OM2_LO = math.sqrt(Q_STATIC)
ok3 = True
for nm, xl, xa, na, es_c in ANCH:
    b_lo, b_hi, e_lo, e_hi = ARCH_10F[nm]
    base = (ETA_C/2.0)*es_c
    my_blo, my_bhi = base*math.sqrt(OM2_LO), base*1.0
    ns = nbe(xl)
    enh = math.sqrt((ns + 1.0)*na)
    my_elo, my_ehi = my_blo*enh, my_bhi*enh
    dmax = max(abs(my_blo - b_lo), abs(my_bhi - b_hi),
               abs(my_elo - e_lo), abs(my_ehi - e_hi))
    ok3 = ok3 and dmax <= 2e-3
    say(f"  {nm:<22} bare {my_blo:.3f}/{my_bhi:.3f} enh "
        f"{my_elo:.3f}/{my_ehi:.3f}  (archived {b_lo:.3f}/{b_hi:.3f} "
        f"{e_lo:.3f}/{e_hi:.3f})  dmax = {dmax:.4f}")
# single-mode allocation identity: all budget at Omega_amb, c1 = 1
say("  single-mode allocation identity (c1 = 1 at Omega_amb == the 10F")
say("  e_a = 1 'enh' corner):")
ok3b = True
for nm, xl, xa, na, es_c in ANCH[:1]:
    om = xl/(2*math.pi); Om = xa/(2*math.pi)
    ns = nbe(xl)
    rate_sm = (ETA_C/4.0)*es_c*math.sqrt(om*Om)*math.sqrt((ns+1)*na)
    gclose = 0.5*math.sqrt(om*Om)
    d = abs(rate_sm/gclose - (ETA_C/2.0)*es_c*math.sqrt((ns+1)*na))
    ok3b = ok3b and d <= 1e-9
    say(f"    {nm}: rate/g_close = {rate_sm/gclose:.6f}  |d vs formula| "
        f"= {d:.1e}")
say("  implied budget of the 10F e_a = 1 corner: <dq^2> = 2 n_amb + 1 =")
say(f"    binary {2*0.502+1:.3f} (RMS {math.sqrt(2*0.502+1)*100:.0f}% "
    f"fractional distortion) / galaxy {2*6.63+1:.2f} (RMS "
    f"{math.sqrt(2*6.63+1)*100:.0f}%)")
say("    -> the R25 'e_a ~ 1 closes centrally' corner already sat 1.4x /")
say("       3.8x past the harmonic-chart premise (report-grade; feeds")
say("       the dilemma; O5-INTERPLAY consistency family).")
ok3 = ok3 and ok3b
say(f"  G3 -> {'PASS' if ok3 else 'FAIL (STOP)'}  [fails if my "
    "formulas do not reproduce the archived table]")

# ==================== G4: the ceiling table ====================
say("")
say("=" * 78)
say("G4 THE CEILING TABLE (q_phys = 1; requirement 0.5 H; shortfall = "
    "0.5/rate):")
say(f"  {'anchor':<22} {'honest rate':>12} {'short':>7} "
    f"{'max-grant rate':>15} {'short':>7}")
CEIL = {}
short_all = []
for nm, xl, xa, na, es_c in ANCH:
    om = xl/(2*math.pi)
    ns = nbe(xl)
    core = math.sqrt(om*(ns + 1.0)*fstar*T_DS)
    r_h = (ETA_C/4.0)*es_c*core*1.0*1.0            # eta2 central, N_p = 1
    r_m = (ETA_MAX/4.0)*es_c*core*1.0*math.sqrt(5) # eta2 max, N_p = 5
    CEIL[nm] = (r_h, r_m)
    short_all.append((nm, REQ/r_h, REQ/r_m))
    say(f"  {nm:<22} {r_h:>10.2e} H {REQ/r_h:>6.1f}x {r_m:>13.2e} H "
        f"{REQ/r_m:>6.1f}x")
min_short_max = min(s for _, _, s in short_all)
say(f"  tightest anchor at MAX-GRANT: shortfall {min_short_max:.2f}x "
    f"(strike bar >= 5x at EVERY anchor)")
# robustness rows (both flank anchors at MAX-GRANT)
say("")
say("  robustness rows (MAX-GRANT, flank anchors):")
# (r1) loosest budget: normal-ordered + ZP-cap sum c^2 <= q^2 =>
#   sup Omega n(Omega) = T_dS (Omega -> 0) => rate x sqrt(1/f*)
LOOSE = math.sqrt(1.0/fstar)
# (r2) non-KMS partner: occupation freed => weight/budget -> Omega/2,
#   capped at the Rabi-window Lambda; two variants Lambda = 0.5/1.0 H
NK1 = math.sqrt(0.25/(fstar*T_DS))
NK2 = math.sqrt(0.50/(fstar*T_DS))
rob_ok = True
for nm in ('binary  x_loc=0.5', 'galaxy  x_loc=0.0953'):
    r_m = CEIL[nm][1]
    s_lo = REQ/(r_m*LOOSE)
    s_n1, s_n2 = REQ/(r_m*NK1), REQ/(r_m*NK2)
    rob_ok = rob_ok and (s_lo >= 3.0)
    say(f"    {nm:<22} (r1) loosest-budget x{LOOSE:.3f} -> {s_lo:>5.2f}x "
        f"(STRIKE-LEG, bar >= 3x)")
    say(f"    {'':<22} (r2) non-KMS Lambda=0.5/1.0 H x{NK1:.2f}/x{NK2:.2f} "
        f"-> {s_n1:>4.2f}x/{s_n2:>4.2f}x (REPORT-grade)")
say("    [(r1) licenses unbounded zero-point amplitude at empty modes =")
say("     chart-dead a fortiori -- kept as a strike leg anyway. (r2) is")
say("     EXCLUDED from the strike condition BY SCOPING: a non-KMS")
say("     partner imprints non-KMS gate ratios and breaks the a0")
say("     temperature lock -- even saturated it delivers a gate the sky")
say("     did not measure (6U detailed balance IS the measurement), so")
say("     it cannot close THE mechanism under adjudication. Scoping")
say("     offered to the round for attack.]")
# closure surface
say("")
say("  THE CLOSURE SURFACE q_phys* (what clause (a) would need):")
for nm in ('binary  x_loc=0.5', 'galaxy  x_loc=0.0953'):
    r_h, r_m = CEIL[nm]
    say(f"    {nm:<22} honest q* = {REQ/r_h:>6.1f}  max-grant q* = "
        f"{REQ/r_m:>5.1f}  (RMS {REQ/r_m*100:.0f}%+ fractional "
        f"distortion)")
say("    THE DILEMMA: q_phys at these values un-derives the vertex")
say("    itself (the 10E lemma is linear-order; eta2 overlaps presume")
say("    near-sphericity) -- clause (a) demands 'derived AND lifts';")
say("    beyond the chart NOTHING is derived. Either way (a) fails.")
# POWER: counterfactual flip
q_cf = 120.0
r_cf = CEIL['galaxy  x_loc=0.0953'][0]*q_cf
say("")
say(f"  POWER (counterfactual): q_phys = {q_cf:.0f} -> galaxy honest "
    f"rate = {r_cf:.3f} H {'>= 0.5 H => the letter logic emits C-CLOSES-CANDIDATE' if r_cf >= REQ else '(POWER FAIL)'}")
pow_ok = r_cf >= REQ
closes_any = any(CEIL[nm][1] >= REQ for nm, *_ in ANCH)
ok4 = pow_ok and rob_ok and (min_short_max >= 5.0) and not closes_any
say(f"  G4 -> {'STRIKE-LEG-1 MET' if ok4 else ('C-CLOSES branch' if closes_any else 'GRAY branch')} "
    f"[fails toward CLOSES if any anchor reaches 0.5 H at q <= 1]")

# ==================== G6: the dynamical leg (6X engine) ==============
say("")
say("=" * 78)
say("G6 THE DYNAMICAL LEG (6X engine verbatim; closed evolution --")
say("non-equilibrium at the physical lambda, trap-#17 compliant):")

def build(NA, NB):
    ad = np.diag(np.sqrt(np.arange(1, NA)), -1)
    bd = np.diag(np.sqrt(np.arange(1, NB)), -1)
    Ia, Ib = np.eye(NA), np.eye(NB)
    return dict(A=np.kron(ad.T, Ib), Ad=np.kron(ad, Ib),
                B=np.kron(Ia, bd.T), Bd=np.kron(Ia, bd),
                Na=np.kron(ad @ ad.T, Ib), Nb=np.kron(Ia, bd @ bd.T),
                Ia=Ia, Ib=Ib, NA=NA, NB=NB)

def thermal(N, nbar):
    if nbar <= 0:
        p = np.zeros(N); p[0] = 1.0
        return np.diag(p)
    xx = math.log(1.0 + 1.0/nbar)
    p = np.exp(-xx*np.arange(N)); p /= p.sum()
    return np.diag(p)

CHI, LAM, WA = 0.8, 0.02, 5.0

def H_exchange(o, delta=0.0, lam=LAM):
    Id = np.kron(o['Ia'], o['Ib'])
    return (WA*o['Na'] + (WA + CHI + delta)*o['Nb']
            + 0.5*CHI*(o['Na'] @ (o['Na'] - Id))
            + lam*(o['Ad'] @ o['B'] + o['Bd'] @ o['A']))

def sat_weight(o, H, nb):
    ev, U = np.linalg.eigh(H)
    p1 = np.zeros(o['NA']); p1[1] = 1.0
    rho = np.kron(np.diag(p1), thermal(o['NB'], nb))
    rl = U.T @ rho @ U
    rb = U @ np.diag(np.diag(rl)) @ U.T
    proj2 = np.kron(np.diag((np.arange(o['NA']) == 2)*1.0), o['Ib'])
    return float(np.real(np.trace(proj2 @ rb)))

# SB-4a: archived regression
say("")
say("SB-4a sat_weight regression vs archived 10C G7 (STOP-class):")
o56 = build(4, 56)
H56 = H_exchange(o56)
ok4a = True
for nb_, ref in ARCH_G7:
    w = sat_weight(o56, H56, nb_)
    m = abs(w - ref) <= 2e-4
    ok4a = ok4a and m
    say(f"  n_amb = {nb_:5.2f}: P2bar = {w:.5f} (archived {ref:.5f}) "
        f"{'OK' if m else 'MISMATCH'}")
say(f"  SB-4a -> {'PASS' if ok4a else 'FAIL (STOP)'}")

def evolve_P2(NA, NB, nb, thetas, init_m=None, lam=LAM):
    """P2 at Rabi angles theta = 2 pi lam t (R-DYN convention)."""
    o = build(NA, NB)
    H = H_exchange(o, lam=lam)
    ev, U = np.linalg.eigh(H)
    if init_m is None:
        p1 = np.zeros(NA); p1[1] = 1.0
        rho = np.kron(np.diag(p1), thermal(NB, nb))
    else:
        v = np.zeros(NA*NB); v[1*NB + init_m] = 1.0
        rho = np.outer(v, v)
    rl = U.T @ rho @ U
    proj2 = np.kron(np.diag((np.arange(NA) == 2)*1.0), o['Ib'])
    Mp = U.T @ proj2 @ U
    outs = []
    for th in thetas:
        t = th/(2*math.pi*lam)
        ph = np.exp(-1j*np.outer(ev, np.ones_like(ev))*t
                    + 1j*np.outer(np.ones_like(ev), ev)*t)
        rt = rl*ph
        outs.append(float(np.real(np.sum(Mp.T*rt))))
    return outs

# SB-4b: dynamical saturation power (time-average at strong coupling)
say("")
say("SB-4b dynamical-saturation POWER (time-average vs sat_weight):")
ok4b = True
for nb_, NBv in ((0.502, 40), (2.0, 40)):
    o = build(4, NBv)
    H = H_exchange(o)
    ws = sat_weight(o, H, nb_)
    ths = np.linspace(0.0, 400*math.pi, 4001)[1:]
    Ps = evolve_P2(4, NBv, nb_, ths)
    wavg = float(np.mean(Ps))
    rel = abs(wavg - ws)/ws
    ok4b = ok4b and rel <= 0.05
    say(f"  n_amb = {nb_:4.2f}: <P2>_t = {wavg:.5f} vs sat {ws:.5f} "
        f"(rel d = {rel:.3f}, bar 0.05)")
say(f"  SB-4b -> {'PASS' if ok4b else 'FAIL (STOP)'}  [the dynamical "
    "route DOES saturate at strong coupling]")

# SB-6: the ceiling rows
say("")
say("SB-6 THE CEILING ROWS (P2 reached at theta_c = 2 pi rate t_univ):")
rows6 = [
    ('binary  x_loc=0.5', 0.502, 20),
    ('galaxy  x_loc=0.0953', 6.63, 70),
]
ok6 = True
sat_cache = {}
for nm, nb_, NBv in rows6:
    o = build(4, NBv)
    H = H_exchange(o)
    ws = sat_weight(o, H, nb_)
    sat_cache[nm] = (ws, NBv)
    r_h, r_m = CEIL[nm]
    th_h, th_m = 2*math.pi*r_h*T_UNIV, 2*math.pi*r_m*T_UNIV
    P_h, P_m, P_pi = evolve_P2(4, NBv, nb_, [th_h, th_m, math.pi])
    fr_h, fr_m, fr_pi = P_h/ws, P_m/ws, P_pi/ws
    ok6 = ok6 and (fr_h <= 0.01) and (fr_m <= 0.05)
    say(f"  {nm} (n_amb = {nb_}, sat = {ws:.5f}):")
    say(f"    honest theta_c = {th_h:.4f} rad -> P2/sat = {fr_h:.5f} "
        f"(bar <= 0.01)")
    say(f"    max-grant theta_c = {th_m:.4f} rad -> P2/sat = {fr_m:.5f} "
        f"(bar <= 0.05)")
    say(f"    pi-row (what closure delivers): P2/sat = {fr_pi:.3f} "
        f"[the instrument distinguishes]")
# structure probe: w(m)/w(1) = m (raw-n, D4)
say("")
say("  structure probe (theta = 0.2, pure |1,m> inputs): w(m)/w(1) vs m")
w_m = []
for m_ in (1, 2, 3, 4):
    P = evolve_P2(4, 30, 0.502, [0.2], init_m=m_)[0]
    w_m.append(P)
rats = [w_m[i]/w_m[0] for i in range(4)]
ok_struct = all(abs(rats[i] - (i + 1)) <= 0.05*(i + 1) for i in range(4))
ok6 = ok6 and ok_struct
say("    ratios = [" + ", ".join(f"{r:.3f}" for r in rats) +
    f"] vs [1, 2, 3, 4] -> {'RAW-N CONFIRMED' if ok_struct else 'FAIL'}")
say("    (the reached weight carries the D4 raw-n structure, not the")
say("     gate P(n>=1): a non-saturated approach CANNOT imprint e^-Lx)")
say(f"  SB-6 -> {'PASS (clause (b) leg closed at the ceiling)' if ok6 else 'FAIL (toward GRAY/CLOSES)'}")

# G8: truncation probes
say("")
o90 = build(4, 90)
ws90 = sat_weight(o90, H_exchange(o90), 6.63)
d_tr = abs(ws90 - sat_cache['galaxy  x_loc=0.0953'][0])
P_h70 = evolve_P2(4, 70, 6.63, [0.0297])[0]
P_h86 = evolve_P2(4, 86, 6.63, [0.0297])[0]
d_ev = abs(P_h86 - P_h70)/max(P_h70, 1e-30)
ok8 = (d_tr <= 2e-4) and (d_ev <= 0.01)
say(f"G8 truncation: sat(6.63) NB 70->90 d = {d_tr:.1e} (bar 2e-4); "
    f"evolved row NB 70->86 rel d = {d_ev:.1e} (bar 0.01) -> "
    f"{'PASS' if ok8 else 'FAIL (STOP)'}")

# ==================== G7: the sector sweep ====================
say("")
say("=" * 78)
say("G7 THE SECTOR SWEEP (every other partner ontology):")
say("")
say("  (ii) FREE RADIATION (the horizon's radiative continuum):")
say(f"      10A archived carrier weights U = {U_10A_BIN:.1e} (binary) / "
    f"{U_10A_GAL:.2e} (galaxy)")
say(f"      = {abs(math.log10(U_10A_BIN)):.0f} / "
    f"{abs(math.log10(U_10A_GAL)):.0f} decimal orders under O(1) "
    f"carrier weight -> DEAD as lending partner")
say("      (10A cond-5 residual: the cloud-continuum leak closed at")
say("       10^-84.8 g^2 -- both directions orders-dead.)")
say("")
say("  (iii) MATTER KINEMATICS (the environment's classical motion):")
t_H_yr = 14.36e9
for lbl, T_yr in (('galactic orbits', 2.5e8),
                  ('wide-binary orbits', 8.2e5),
                  ('stellar encounters', 1.0e5)):
    OmH = 2*math.pi*t_H_yr/T_yr
    x_th = OmH/T_DS
    say(f"      {lbl:<20} Omega = {OmH:9.3g} H -> x = Omega/T_dS = "
        f"{x_th:9.3g} -> n = e^-{x_th:.3g} "
        f"(~{x_th/math.log(10):.0f} decimal orders EMPTY)")
say("      -> no soft-band spectral weight; thermally EMPTY at the")
say("         relevant frequencies (no lending statistics to supply);")
say("         c-number/adiabatic character at soft frequencies (drives,")
say("         not exchange partners); the one candidate matter family")
say("         in the soft band (10-kAU apsidal, 2.49 H) was dictionary-")
say("         killed at 12 implied-a0 orders (10B, archived).")
say("")
say("  (iv) COHERENT / CLASSICAL PARTNERS:")
say("      mean-vs-variance split: a coherent amplitude displaces the")
say("      MEAN quadrupole; the mean is the MEASURED static 0.086 =>")
say("      rate rides e_a = 0.086 = the 10E central shortfall")
say("      [1.8e-4, 2.2e-3] H vs 0.5 H = 2.4-3.4 orders. DEAD.")
pg_th = 0.5 + (6.63/7.63)**2/4
pb_th = 0.5 + (0.502/1.502)**2/4
pg_co = 0.5 + (1 - math.exp(-6.63))**2/4
pb_co = 0.5 + (1 - math.exp(-0.502))**2/4
say(f"      Poisson-gate postdiction row (state-statistics substituted):")
say(f"        thermal   p_gal = {pg_th:.4f}, p_bin = {pb_th:.4f} "
        f"(6E-banked postdictions)")
say(f"        coherent  p_gal = {pg_co:.4f}, p_bin = {pb_co:.4f}")
say(f"        9U measured p = 0.6471 +/- 0.0746: thermal z = "
    f"{(pg_th-0.6471)/0.0746:+.2f}, coherent z = "
    f"{(pg_co-0.6471)/0.0746:+.2f} (lean-grade garnish)")
say("      pure c-number drives: NO occupation statistics at all =>")
say("      no e_N-running of the gate => the measured two-anchor tail")
say("      split (0.688/0.528 postdicted, 6E) is unproducible. DEAD.")
say("")
say("  (v) NO PARTNER (perturbative at the derived lambda) = SB-6:")
say("      weight <= 5% of saturated with raw-n structure. DEAD.")
say("")
say("  partition: (i) bound-cloud modes [the ceiling, G4] / (ii) free")
say("  fields / (iii) matter dof exhaust the ontology; (iv)/(v) cover")
say("  state + coupling character. The partition is offered to the")
say("  round for attack.")
ok7 = True   # rows are arithmetic + archived citations; wired to letter

# ==================== the letter ====================
say("")
say("=" * 78)
STOPPED = not (ok1a and ok1b and ok2 and ok3 and ok4a and ok4b and ok8)
if STOPPED:
    letter = 'STOP (machinery)'
elif closes_any:
    letter = 'C-CLOSES-CANDIDATE (ROUND-27 adjudication)'
elif ok4 and ok6 and ok7:
    letter = ('C-STRIKE-CANDIDATE (both kill clauses met; ROUND-27 '
              'affirmation REQUIRED before the credence executes)')
else:
    letter = 'C-SURFACE-GRAY (HOLD 15; successor O5-ANHARM)'
say(f"TOKENS: G1a:{'P' if ok1a else 'F'} G1b:{'P' if ok1b else 'F'} "
    f"G2:{'P' if ok2 else 'F'} G3:{'P' if ok3 else 'F'} "
    f"G4:{'strike-leg' if ok4 else ('closes' if closes_any else 'gray')} "
    f"SB4a:{'P' if ok4a else 'F'} SB4b:{'P' if ok4b else 'F'} "
    f"SB6:{'P' if ok6 else 'F'} G7:{'P' if ok7 else 'F'} "
    f"G8:{'P' if ok8 else 'F'}")
say(f"LETTER: {letter}")
say("")
say("KILL-CLAUSE EVALUATION (printed with inputs):")
say(f"  clause (a) collective amplification to saturation: the ceiling")
say(f"    is K-FREE (G1b + G2 + budget); tightest MAX-GRANT shortfall "
    f"{min_short_max:.1f}x")
say(f"    (>= 5x at every anchor: {'YES' if min_short_max >= 5 else 'NO'}); "
    f"loosest-budget row >= 3x: {'YES' if rob_ok else 'NO'};")
say(f"    the closure surface needs q_phys >= "
    f"{REQ/CEIL['binary  x_loc=0.5'][1]:.1f} (RMS >= "
    f"{REQ/CEIL['binary  x_loc=0.5'][1]*100:.0f}% fractional")
say("    distortion) at maximal granting -> the dilemma un-derives the")
say(f"    vertex there. CLAUSE (a): "
    f"{'FAILS' if (ok4 and not closes_any) else ('MET' if closes_any else 'UNRESOLVED')}")
say(f"  clause (b) non-saturated imprinting of e^-Lx: reached weight")
say(f"    <= 5% of saturated at the ceiling angles with RAW-N structure")
say(f"    (D4-matched, 10C-G7-rejected weighting) + sector rows (ii)-(iv)")
say(f"    orders-dead. CLAUSE (b): {'FAILS' if (ok6 and ok7) else 'UNRESOLVED'}")
say("")
say("CREDENCE (pre-signed, mechanical): C-STRIKE-CANDIDATE + ROUND-27")
say("affirms (no unpatched hole in the kill case) -> mech 15 -> 8")
say("(REQUIRED -- the R26 signature; no discretion). + ROUND-27 finds an")
say("unpatched hole -> HOLD 15, the hole = the named successor.")
say("C-CLOSES-CANDIDATE + clean round -> 15 -> 19. C-SURFACE-GRAY ->")
say("HOLD 15 (successor O5-ANHARM). anomaly-real 53 UNTOUCHED in every")
say("branch (no sky fits).")
say("=" * 78)
say(f"done ({(time.time()-T00)/60:.1f} min)")

with open('data/stage10g_collective.txt', 'w', encoding='utf-8') as f:
    f.write("\n".join(OUT) + "\n")
print("\nsaved: data/stage10g_collective.txt")
