# Testing the Thermal-Horizon Reading of the Radial Acceleration Relation, from Galaxies to Wide Binaries

**Draft v3.7 — assembled from the repository lab notebook (NOTES, Stages 1–7L; THE COOKSON QUANTIFICATION (7L, 2026-07-27): their selection rebuilt on our catalog (N = 1194 proxy of 1,421) — their flatness REPLICATES (step 0.985, CI 0.909–1.060), the landed boost predicts HALF their tested step (1.09–1.10, so their 2.7σ ≈ 1.4σ against the landed model; boost-vs-Newton ≈ 0.9σ at their N), the 2D likelihood on their selection CONSISTENT with the operative α in all four reads — their null and our detection are arithmetically compatible, the fork now a with-numbers question about the removed 92% (§7.4(d)) — added 2026-07-27; THE ACCOUNTING ROUND (7K-a/7K-b/7J-d, 2026-07-27): the forward median GRAY (the landed Newton-best cell produces 1.033/1.043 of the model-light 1.078 — half absorbed, the excess survives; the anchor restated as supporting witness), the census relocated to its self-defending (band = 9, cliff = 2) pair (the landed cell floods band AND cliff, P(≤2) ≤ 7×10⁻⁶ — the cliff bounds tail noise to formal, where the band is unleakable), and the DATA-VOTE UNSUSPENSION with the honest verdict NO FUNCTION DISCRIMINATION (all sixteen laws within ±8 of the occupation law at the landed posterior; the four fenced vetoes read as instrument resolution loss; the fenced binary ĉ₁ = 0.37–0.50 indication ERASED — λ profile flat; Newton function-robustly dead at ΔlnL ≥ +7.9 on all 32 rows; SPARC remains the function discriminator) — added 2026-07-27; THE WIDTH-SHAPE CONTEST (7J-z6, 2026-07-27): the promoted incompleteness offered two pre-registered instrument-noise shapes — both fail their win bars (tail unused; floor edge-rides into ≈4.3× formal) — verdict UNRESOLVED-CARRIED with the operative band standing, and the round's product is the NOISE-CEILING PROFILE of the detection: physical Gaia envelope α = 0.80/0.80 at +35.2/+32.3 (the operative quote is noise-diluted) → operative 0.68–0.74 / +14.5–23.8 → unphysical corner 0.00; the exchangeability arm INFORMATIVE (a validated injected boost survives the floor axis — the sky's corner preference is data-driven, ~7–11 lnL of genuinely unmodeled width); the volumetric caution named (sub-significant nuisance axes drain marginal detections through the α = 0 row); shape exclusion set floor/tail/inflation/smear/perspective with the population-sector successor hypothesis booked — added 2026-07-27; THE ARM SUITE (7J-z5 A–D, 2026-07-27): the operative machinery validated four-for-four at the landed anchor under twin-mismatch injections (clean null — α = 0.00 both laws; own-truths recovered conservatively; nuisances sharp), the noise-edge chase measured UNEXPLAINED by any injected population (real width-shape incompleteness, ~8 lnL, the named next instrument), and the binary-anomaly credence moved ~45% → ~50% with both pre-declared deciders landed — added 2026-07-27; THE CERTIFICATE ROUND (7J-z2c + the anchored re-read, 2026-07-26): the subsystem rate certificated (exact segment-erf integration, profiled coherence length — both v2b protocol objects closed structurally; f̂ six-way stable at 0.159; host 0.29–0.32 flat-convention), the q-shape deliverable measuring the inner subsystems TWIN-HEAVY (+162 over flat; host ≈ 0.23; the wobble-quiet population — the ×3 multiplicity tension dissolving at photometric grade in the conversion audit's predicted direction), and the operative verdict landed at the certificated conversion-widened anchor (α_marg = 0.74/0.70 at +23.8/+23.2, AMBIGUOUS-CARRIED; the flat-q face collapse carried as a rejected-premise conditional; one reader wiring amendment logged) — added 2026-07-26; the round-10 conversion audit (7J-z4, 2026-07-26): the detection–wobble anticorrelation confirmed at 6–8× per companion, the forced-multiplicity rejection measured wobble-binding (ΔlnL ≈ +310 per amplitude doubling), the joint mass-ratio conversion band [0.10, 0.39] bracketing the kinematic preference — the ×3 multiplicity tension not yet kinematic; the anchor-strength curve σ* = 0.02 (the flat curve's missing axis measured); the certificate upgraded by requirement to a (q, P)-resolved rate — added 2026-07-26; the round-9 adequacy decomposition (7J-z3, 2026-07-26): zero anchor strain — α_marg is the width-complete model's own optimum — and the fifth-move exposure named (forcing the measured host rate collapses α to zero in all four reads; the certificate and kernel-degraded arm-suite instruments adjudicate), the width channel's identity flagged open with its candidate bounds, absorber-collapse statements corrected to grid resolution — added 2026-07-26; THE TWO DECIDERS (7J-z/7J-z2b/7J-g, 2026-07-26): the repaired completeness measurement — the vector common mode discovered (within-pair coherence ρ 0.86 → 0.36 with color separation), the coherence-kernel model acquitting the measured rate (kernel-blind over-attribution 0.000), f̂ stable 0.159–0.166 blended per component (host 0.29–0.32 ≈ 3× field, the first measurement at 0.2–50 kAU) across three model classes, two integrators, five basins — unshipped behind two numerical-protocol gates, certificate run queued; the width channel parameterized (sq = 0.2 interior, P = 1.00) flattening the α_marg(anchor) curve to 0.01–0.03 across 0.06–0.34 at α_marg = 0.70–0.74, ΔlnL +23, AMBIGUOUS-CARRIED at 1.2–1.8 below the pre-registered bar; the direction channel shown to veto a phantom ṽ-only α ≈ 0.5 in the width-less configuration (SEPARATION-CONFIRMED at α grade; ABSORBER-LEVEL with the width channel); the Paper-1 rule applied — the 2D claim operates; credence re-affirmed ~45% by the pre-committed map; two same-day instrument aborts by our own gates (GB0w wiring catch at 0.00e+00 retest; the GZQ twin-spike quadrature kill) logged pre-results — added 2026-07-26; the review-round hardening (7J-x/7J-y, 2026-07-25): per-arm injection standard adopted and pre-registered; the α_marg(prior-anchor) curve made the primary fit-level statement (correction #17); the completeness stage's absolute scale retracted by its own reviewer-designed within-pair correlation validation (ρ ≈ +0.47 common-mode; correction #18) — literature subsystem rates now primary, the verdict label AMBIGUOUS at that anchoring pending the six-realization budget, the anchoring-invariant statement (the fenced +99–110 Newtonian rejection dead everywhere) the durable one; correction #16 issued hard and one clause re-graded under #18; Appendix A at eighteen; the decisive completeness marginalization (Stage 7J, 2026-07-25): the companion sector measured rather than fenced — flag completeness 0.41, true host multiplicity 0.42–0.57 — the photocenter correction of the companion amplitude law (the as-published law shown internally inconsistent with the measured multiplicity), and the pre-registered COMPANION-WIN verdict: the fit-level binary amplitude does not survive the measured multiplicity (α_marg = 0.00, robust to the noise grid, injection-validated); fifteenth correction logged (the 7I cut-survival immunity inference retracted); every fit-level binary row now fence-conditional and co-quoted with its null; the anomaly's binary evidence devolved to the model-light channels, their attribution instrument declared (§6.3) — added 2026-07-25; the external-review round — two solicited external reviews (2026-07-24/25) adopted point-by-point: the function FREEZE and primary-analysis declaration (PREDICTIONS.md §0), the bar-locking definition, the design-property reframing of the ambient gate's two-system pass, the two-realization regrade of the binary ĉ₁, the ceiling-pair per-pair release (data/ceiling_pairs.csv), the w_rad external-prior closure (Δα̂ ≤ 0.01), and the strict-multiplicity MATERIAL disclosure worked through three bar-locked instruments to its honest end state (the model-light boost cut-stable at 1.09–1.10 while the fit-level amplitude collapses on the cleaned subsample; both rescue hypotheses refuted on their own bars; COMPANION-DIRECTION, unresolved, handed to the joint completeness marginalization) — added 2026-07-25; the Einstein fluctuation instrument with the bump hunt's inner-disk identification, the vacuum-share contest, the cQED platform translation, the mixing-mean uniqueness theorem, and the Saturn trajectory resolution (the 451-order margin with the binary tie restored) added 2026-07-24; the desktop-analog and pseudomode closures of the laboratory question, the frozen-bath tests, the curated measurement ledger (LEDGER.csv, machine-audited world table), the resolution-bath triangulation (the ambient gate proven *necessary*), the KMS gate derivation (the gate as the detailed-balance cost of two borrowed ambient quanta, unique against every rival weight) with its two new falsifier rungs, and the untied-exponent contest (tied form survives; one pre-registered instrument bar failed and logged) added 2026-07-24; the mixing-family measurement, the O13 completion, the derived running-β functions with their two-system tests, the κ/a₀ ladder, the drive-weighted-rule exclusion, the ambient-gated bath — the only function among those tested to pass both systems, with its pre-registered two-leg tests (§2.4, §4.4, §6.4) — and the grammar formalization with its measured-environment tests and its leg-count reading, the latter deflated to a point-preference by its own same-day bootstrap (correction #13) — added 2026-07-23); pre-circulation verification pass applied 2026-07-22 (INSPIRE reference verification, priority sweep of all novelty claims, flagged full-text checks — Appendix A); the Cookson et al. (2026) engagement with the perspective correction executed at the fit level (§7.4, Stage 4R); the measured zero-point coefficient in two disconnected systems (§4.3 galaxies, §6.4 binaries), the second-moment channel through its identifiability boundary and its executed environmental control (§4.6), the hierarchical bath-matrix contest with its tail decomposition, the geometric-mean construction, the four-seed binary budgets, the a₀ ladder, and the vertical-channel control (§4.4, §6.4, §9.1), the mechanism specification (§2.4), and the amplitude-locked quadrupole (§8.1) added 2026-07-23. All numbers final as logged. Not for circulation before colleague review.**

**Filip Hájek** — independent researcher — hfilip11@gmail.com

*Acknowledgments: the computational analysis, literature verification, and manuscript drafting were performed in collaboration with Claude (Anthropic). Every claimed number is reproduced by a named script in this repository (Appendix B).*

---

## Abstract

The interpolating function preferred by the radial acceleration relation (RAR), ν(y) = (1−e^(−√y))⁻¹, was shown by Cadoni & Tuveri (2019) to equal 1 + n_BE(x), x = √(g_bar/a₀): the Bose–Einstein occupation of thermal excitations of the de Sitter horizon, with a₀ = H/2π derived. That framework has remained untested beyond rotation-curve fitting. We present its first empirical stress-tests, together with an independent measurement of the low-acceleration anomaly in wide binaries and a systematic reconciliation of the conflicting published wide-binary analyses.

(1) We note two exact structural consequences of the identity, apparently unremarked: ν = ½ + ½·coth(x/2) — the Planck oscillator mean-energy law, whose frozen (x→∞) limit identifies the Newtonian regime with the pure zero-point response — and a low-acceleration expansion whose coefficients are the Bernoulli numbers, a ladder of parameter-free predictions. The classical self-consistent limit of the same thermal reading reproduces, exactly, the widely used "simple" interpolating function, so the empirical contest between the two leading ν-functions is a quantum-versus-classical question about the putative bath.

(2) We test the first nontrivial rung on 2,700 SPARC points: the NLO-coefficient-½ branch (shared by the occupation law and simple-ν) is preferred over the c₁ = 0 branch (standard-μ) in 198–200 of 200 galaxy-level bootstraps under a raw-χ² likelihood, deflating to a sign-robust strong lean (Δ(−2lnL) = 56; 166/200) when the relation's intrinsic scatter is marginalized — both treatments are reported. Promoted to a continuous parameter, the coefficient is then *measured*, together with its estimator's nuisance sensitivity: profile ĉ₁ = 0.45 under global M/L and 0.26 under hierarchical per-galaxy M/L (injection-gated, so the spread is structure, not machinery bias); galaxy bootstraps agree across treatments at ≈ 0.4 ± 0.3. **c₁ = 0 is excluded in every treatment** (Δ(−2lnL) = 29–56; ≥ 89% of bootstraps), while the second digit — ¼ versus ½ — remains open on the galaxy side; the raw-χ² objective edge-runs on the same family, making the likelihood-model dependence explicit rather than implicit. A converged hierarchical contest of the full bath-function matrix inverts its flat-M/L ordering — and the inversion *decomposes*: three-quarters of it is a screening-tail preference (p ≈ 0.6–0.65 across treatments, Δ(−2lnL) = 56 over the pure occupation tail), not a deep-coefficient vote. The wide binaries veto the ¼ cell's transition shape outright; the sharpened functions — the fitted ν_p(0.65) and a *parameter-free geometric-mean construction*, ν = 1 + n_BE(y^(3/4)√ν), with derived coefficients c₁ = ⅓ and c₂ = 1/12, which leads every hierarchical galaxy treatment tried — remain binary-viable with interior amplitudes, though the binaries prefer the pure occupation law sign-consistently (+6–10 per realization, four seeds) and its milder a₀ translation. Adding the measured distance/inclination channel to the hierarchy resolves the fits back onto a₀ = cH₀/2π, halves the surviving tail preference (−41/−32), and collapses the ¼ function (−9) — the function-level frontier is measured, open, and three-cornered (½ binary-anchored, ⅓ best cross-system, ¼ excluded). The corners then unify into a one-parameter *mixing family* — ω = ω_source^(1−β)·ω_total^β, with the exact derived ladder c₁ = 1/(2(1+β)), p_tail = (1+β)/2, c₁·p_tail = ¼, c₃ = β(3β+1)/(24(1+β)³) — and the dial is measured in both systems: **the binaries pin β < 0.03 (1σ; every β > 0 loses on all six realizations, 24/24), the galaxies profile to β̂ = 0.45–0.64, and decomposing the galaxy preference by acceleration shows the split runs through regimes, not datasets** — the deep zero point and the transition hold β = 0 everywhere while the screening tail alone votes β ≈ ½–¾. No single member can fit this (the family's own lock forbids c₁ ≈ ½ with p ≈ 0.7 simultaneously), which converts the construction problem into its sharpest form: a source-driven zero point with decoupled, sharpened screening. That construction is then attempted: weighting the response admixture by the *spontaneous-emission fraction* (Einstein-coefficient physics — stimulated processes follow the drive, spontaneous ones probe the mode's own structure) gives zero-parameter running exponents — β = 1/(2ν) at one leg; β = 1/(2(2ν−1)²) when the round trip (emission and reabsorption) must be spontaneous — whose exact consequences (c₁ = ½; at two legs c₂ = 1/12 as well; tail p = ¾; lock product ⅜) were fixed before any fit. The derived functions **lead every hierarchical galaxy treatment monotonically** — the two-leg member reaches −109/−64 past the occupation law, the widest controlled-treatment margin in the function ledger, surviving a 40-rep galaxy bootstrap at 37/40 (−57 ± 38, a strong lean) — but the binaries, probing the same functions through the dominant external field, **reject every sharpened function**: −5 to −10 in likelihood almost independently of transition value, and +4 to +6σ in the a₀ translation (κ = 1.26–1.35), while pure occupation passes at +1.9σ. The simplest configuration-dependent rule (response admixture weighted by the pointwise internal-field share) was then implemented at solver level and **excluded** (−9.6 ± 1.5; though it passes the temperature row at +2.1σ). The split is therefore *system-level*: no local ν(y), and no pointwise field-ratio rule, fits both regimes. The ambient-gated resolution was then derived in the same Einstein-coefficient grammar — a mode can dress itself to its self-consistent frequency only if the ambient reservoir assists the exchange, and reservoir assistance is *stimulated*, ∝ n_amb/(1+n_amb) — giving the zero-parameter, system-level rule **β = [n_amb/(1+n_amb)]²·½/(2ν−1)²** (*admixture = local quantumness × ambient classicality*), which post-dicts both measured screening exponents in advance (tail p = ½ + g_amb/4: 0.69 for galaxy environments against the measured 0.65–0.75; 0.53 for the binaries against their held ½) and keeps c₁ = ½, c₂ = 1/12 exactly (the entire ambient dependence enters at c₃ = −g/16). Tested against pre-registered bars: **the binaries accept it (−0.9 ± 2.7 versus the occupation law, amplitudes interior, and a₀ = 1.28 ± 0.15 — at +1.6σ the least-strained a₀ row in the binary ledger), and the galaxies hold −59 under the controlled treatment (second only to the undiluted two-leg function, whose binary veto it escapes), with the plain-treatment bar missed by 8 and disclosed. One function — alone among the thirteen tested — passes both systems** (joint −57 versus the occupation law) — carried with two flags: the reconciliation is a design property of the function, not a measurement; and the binary leg of every function contest is fence-conditional after (3), so the two-system split this rule reconciles is itself suspended pending the contest re-run under the measured-multiplicity posterior — it was constructed to reduce to each system's preferred law in that system's regime, so its evidence is confined to the non-built-in content and the stated out-of-sample tests: environment-resolved SPARC outskirts should soften with the gate's dial, and weak-ambient (halo) binaries at Gaia DR4 should sharpen toward p = 0.69. The rule's grammar was then formalized — β = ½·[q_loc·s_amb]^L, with the local factor selected by the exact dispersive-pull structure of cavity QED (the frequency pull of a coupled system counts 2n+1: vacuum half-quantum included, an energy share, not a rate share) — and its structural exponent *measured*: the deep series breaks the Bernoulli ladder at rung c_{L+1} (L=1 kills c₂ = 1/12 − s/8; L=2 keeps c₂ and sets c₃ = −s²/16; L=3 keeps both), so the leg count is readable off the deep data, and the hierarchical galaxies point-prefer **L = 2 over both flanking integers** (−52.8 / −59.1 / −54.5 against the occupation law) — a preference whose own galaxy-population bootstrap then deflated it (correction #13): the one-leg rejection holds in 29/40 resamples (a lean, carried by the c₂ Bernoulli break), two-versus-three legs is unresolved (21/40) — the instrument is exact, but this sample's deep arm is too thin to read the rung at population grade. Replacing the fiducial environment with the measured per-galaxy fields of Chae et al. (2021) then improves every treatment at zero parameters and **retro-resolves the one bar the rule had missed** (plain treatment −100.5/−105.9 against the pre-registered ≤ −100; the controlled treatment reaches −61.7, bootstrap-graded at −57 ± 36 with 37/40 — the equal-highest bootstrap fraction in the function ledger, shared with the binary-vetoed two-leg function; joint two-system ledger ≈ −60) — though a subsequent permutation control (correction #14) shows this gain is gate *heterogeneity* rather than ordering: the true assignment loses to all eight distribution-preserving shuffles at weak grade, so the in-sample environmental-ordering claim is withdrawn and the ordering prediction moves out-of-sample. The gate's *necessity* was then proven by triangulation. A zero-parameter *resolution bath* — forced by the de Sitter bath's one-scale property (its KMS occupation varies on the scale of its own temperature, which *is* the a₀ scale), with the dressed-to-source frequency gap R = νy − √y crossing unity at y = 1.9, precisely the measured admixture turn-on — preserves the Bernoulli ladder one rung deeper than any candidate on record (break at c₅ = −1/16), leads the plain-treatment galaxy ladder by −114 (the widest plain-treatment margin in the ledger), **and is shape-rejected by the binaries** (amplitude edge-riding, 0/6 realizations) exactly as its pre-registration warned: local-in-y running alone cannot carry the two-system split, pointwise ambient weighting alone cannot (the drive-weighted exclusion), and only the two-factor rule passes both regimes. The same derivation closes the exact-temperature alternatives analytically — the Deser–Levin source branch has no finite fixed point, the total branch is the excluded ¼ cell plus an invisible constant — and lands the binaries' β < 0.03 as a theorem: a geodesic (free-falling) detector has zero proper acceleration, so its Unruh term vanishes identically. The gate itself was then derived from the same loop as the local factor: path-resolving the dispersive pull separates its absorption (n) and emission (n+1) time-orderings, whose ratio is the KMS detailed-balance weight n/(1+n) = e^(−x) *exactly* — the gate is the Boltzmann cost of borrowing two ambient quanta, the borrow count being the loop's own perturbative order — and of the loop's natural weights (rate-balance, absorption share, inverse ratio, raw amplitude, pointwise, ambient-free) every rival is excluded by already-measured facts, leaving the KMS-ratio-squared form the unique survivor with its postdictions regressing onto the measured tails. Two falsifiable rungs fall out: the ambient-frequency convention splits at weak-ambient binaries (Δp ≈ 0.03 — a Gaia DR4 discriminator), and the Boltzmann cost runs with T = H(z)/2π, so the tail exponent must run with redshift (0.688 → 0.702 by z = 1 at fixed environment). The solar-quadrupole lock is escape-free throughout (4.0–5.8× the Cassini cap across the constant-β family; 4.8–5.3× for the one-leg derived pair; the ambient-gated function itself records the family floor, 4.0×). A first second-moment test finds the RAR's intrinsic scatter acceleration-dependent in the oscillator's direction — surviving disk-M/L marginalization (N ~ 20–60 if thermal) but *not identifiable* against per-galaxy distance/inclination freedom at SPARC depth (the full-hierarchy fit absorbs it, and its own injection gate proves the degeneracy) — while a transition-region scatter excess at x ≈ 1 survives every marginalization tried. The deep trend's environmental rival was then executed with the published per-galaxy field estimates of Chae et al. (2021) and excluded at those amplitudes (global pattern amplitude β̂ = 0.04, the face-value collinear environmental field rejected at Δ(−2lnL) = 168, correlation channel null) — the thermal candidate survives its sharpest control. Extending to the weak-lensing RAR (KiDS-1000, two public reductions, ~2 dex deeper) anchors a₀ and rejects the Newtonian control on 15 points alone, but cannot reach rung 2: the resolving power for 1/12-vs-1/8 is 0.1σ at the surveys' 0.2-dex stellar-mass calibration.

(3) We measure the anomaly in 14,071 Gaia EDR3 wide binaries with a joint two-dimensional (velocity, direction-angle) likelihood, physically identified nuisances, and full seed and bootstrap error budgets: the boost strength is α = 1.18 ± 0.11 (simple-ν) / 1.13 ± 0.13 (occupation law) relative to the galactic calibration at the RAR-inverted Newtonian external field g_N,ext = 1.15 ± 0.05 a₀, with Newtonian dynamics excluded in all 2000 bootstrap contests (minimum Δ lnL +53). The fitted near-parabolic orbit fraction, w_rad = 0.20, independently matches the e > 0.9 mass fraction (20–22%) of the superthermal eccentricity law of Hwang, Ting & Zakamska (2022). The perpendicular-moving wide pairs exhibit a velocity ceiling at the boosted escape edge: eleven pairs populate the long-proposed Newtonian-forbidden band ṽ ∈ [√2, 1.67) (P = 4×10⁻⁹ against Newton plus measured noise) and terminate at √(2×1.36) (P = 0.62), with flyby, triple, chance, and selection identities each excluded. Profiled over the same coefficient family as the galaxies, the binary likelihood under the *fenced* model independently returned ĉ₁ = 0.37–0.50 with the c₁ = 0 branch rejected at ΔlnL ≈ 20 per realization — an indication the landed-posterior re-run (7J-d) then *erased*: with companions and the width channel marginalized, the λ profile is flat within realization scatter (peaks at c₁ = 0.375 and 0.000 across the two realizations), so the binaries carry no constraint on c₁ and the coefficient dial is measured in one system, the galaxies. The same re-run compresses the entire sixteen-law function contest to within ±8 log-likelihood of the occupation law — the binary data vote is *against Newton on every function × realization row (minimum +7.9) and agnostic among the modified laws*, the rotation curves remaining the program's only function discriminator. A dedicated completeness stage then attempted to *measure* the companion sector these fits had fenced (apparent flag completeness 0.41; apparent host multiplicity 0.42–0.57) — and that absolute scale was retracted by its own reviewer-designed validation (correction #18): the within-pair correlation of overluminosity residuals is ρ ≈ +0.47 (coeval, co-chemical pair components share metallicity/age/extinction displacements), so roughly half the single-star spread is common-mode astrophysics the one-dimensional mixture had read as companions — biasing completeness low and host rate high, the direction that manufactures a companion verdict, and resolving the factor ~2–3 tension with published wide-binary subsystem rates in the literature's favor. Re-fitting with the companion fraction, eccentricity mixture, noise inflation, wobble amplitude, and boost freed jointly — under a photocenter-corrected companion amplitude law, the as-published law being kinematically strained at any defensible multiplicity — returned the pre-registered **COMPANION-WIN** at the (retracted) measured prior — α_marg = 0.00 on the injection-validated simple arm, both realizations, robust to the noise grid; the BE arm reads the same but is quoted as *pending*, its own-truth recovery having fallen below band — a label re-graded by the retraction (Appendix A, items 17–18). The primary result is stated as the reviewer proposed, as a *function of the companion-prior anchor* with the verdict labels as annotations — and the finely sampled curve (anchors 0.06–0.30 from the cached likelihood) is nearly **flat**: α_marg ≈ 0.2–0.3 with Newton within +2…+4 under *any* smooth companion prior, including anchors below the old fence, because the likelihood itself pays +12…+28 to sit at f_comp ≈ 0.2 — **the fenced +99–110 detection is recoverable only behind a hard exclusion of f ≥ 0.2 that no rate measurement can justify**. The label remains prior-dependent at the margins (COMPANION-WIN at the retracted measured prior; AMBIGUOUS at the literature anchoring, one realization interior at α_marg ≈ 0.4, one at zero; the six-realization budget pre-registered and running), and the one pending instrument that can materially move the curve is not the subsystem-rate measurement but the forward model's missing width channel — the f_comp ≈ 0.2 cell buys its advantage by supplying variance the model otherwise lacks, the same object as the winning cell's 60–116 misfit and the noise-grid edge. The old fence f ≤ 0.1, priced at ln π = −768 under the retracted measurement, is merely ~1.5σ low under the literature prior. The verdict's grading is stated exactly: at the verdict's firing the injection validation covered the strict-sample simple arm only (recovery α = 1.27, ΔlnL +48.8), and per-arm full-sample and own-truth BE injections plus a six-realization budget were then pre-registered and run to the reviewer's standard; the exact zero is peak-prior-specific — recentring the measured host prior at its 1σ-low end leaves the verdict standing but moves the posterior to f_comp = 0.2 with a residual-boost scrap (α_marg seed means 0.19–0.25, Newton within a few log-likelihood) — and the winning Newtonian cell still misfits the kinematics by 60–116 log-likelihood, *the same order as the +99 gap the fenced fits had won by*: the verdict selects the less-bad of two misspecified models and is provisional against whatever component both are missing. Every fit-level binary number above is fence-conditional and co-quoted with this null; the model-light channels — the median boost, stable at 1.09–1.10 under the strictest multiplicity cleaning, and the perpendicular census — carry the anomaly's remaining evidence, their own companion attribution now the program's declared decisive open instrument. The two deciders then reported (all bars pre-registered). The repaired two-dimensional completeness measurement discovered that the pair-common displacement is a *vector* — within-pair coherence decays smoothly with color separation (ρ 0.86 → 0.36), twins projecting identically — and its coherence-kernel model *acquits* the rate its predecessor measured (kernel-blind over-attribution on a kernel-truth sky: 0.000): the subsystem rate is stable at f̂ ≈ 0.16 blended per component (host 0.29–0.32, ~3× the field rate — the first measurement at 0.2–50 kAU) across every model class, integration scheme, and optimizer basin tried, though it remains formally unshipped behind two numerical-protocol gates (certificate run queued). The forward model's missing width channel was then parameterized (per-system, direction-neutral; demanded by the data at sq = 0.2 with P = 1.00): with it the α_marg(anchor) curve flattens to 0.01–0.03 across 0.06–0.34 — the α answer is companion-prior-*independent* — at **α_marg = 0.70–0.74, ΔlnL(Newton) = +23**, formally AMBIGUOUS-CARRIED at 1.2–1.8 below the pre-registered detection bar, with the old companion-win confined to the width-less slice. The direction-channel decomposition confirms the methodological claim: with the width channel off — the configuration of every published scalar-velocity analysis — ṽ-only fitting reports a phantom α ≈ 0.5 that the direction data veto to ≈ 0.2; with it on, the α answer is channel-robust and γ's work is pinning the absorbers. A follow-up adequacy decomposition then priced the result's conditionality exactly: the quoted α is the width-complete model's own unconstrained optimum (anchor strain zero) — and it is fully exposed to the one named tension, since forcing the measured ~0.3 host rate collapses α to zero at a cell that itself still misfits by 135–153; the certificate and kernel-degraded arm-suite instruments, first in the queue, adjudicate which side gives, and the width channel's physical identity is carried as an open flag (no traced channel produces its 20% scale; the α claim is conditional on it being nuisance rather than signal-shaped, which the direction channel is what tests). The closing conversion audit then reopens the forcing's premise: photometric detection and photocenter wobble are anticorrelated in mass ratio at 6–8× per companion, the rejection is wobble-binding, and the joint conversion band [0.10, 0.39] brackets the kinematic preference — so whether the measured rate and the kinematics actually disagree awaits a mass-ratio-resolved certificate, now a requirement. That certificate then shipped with every gate passing (the rate six-way stable at f̂ = 0.159; the resolution and start-dispersion objects closed structurally) and its mass-ratio deliverable resolves the question in the audit's predicted direction: **the photometry itself measures the inner subsystems twin-heavy (+162 over flat), the wobble-quiet population, making the twin-convention host ≈ 0.23 kinematically equivalent to roughly the preferred f_comp ≈ 0.1 — the ×3 multiplicity tension dissolves at photometric-measurement grade**, and the operative verdict lands at the certificated anchor: α_marg = 0.74/0.70 at ΔlnL ≈ +23, AMBIGUOUS-CARRIED, the flat-q face collapse carried as a rejected-premise conditional. A four-arm mismatch-injection suite then validates the operative machinery at that anchor — the null arm manufactures nothing (α = 0.00 both laws), own-truths recover conservatively, nuisances identify sharply — while localizing the one honest residue: no injected sky reproduces the real sky's noise-edge chase, so a width-*shape* incompleteness worth ~8 log-likelihood stands as the named open systematic, and the program's binary-anomaly credence moves to ~50% with both pre-declared deciders landed.

(4) We localize the published wide-binary disagreement by ablation, and then retract the ablation's verdict-bearing claim (correction #16): its reading — that the Newton-favored analyses' freed companion fraction "requires fractions the photometry forbids" — rested on treating a flagged rate as a bound, which no completeness accounting supports; the companion freedom was directionally legitimate, and our own marginalized re-fit landed on the Newton-favored side of the fit-level question. (One clause of that retraction was itself over-corrected and is re-graded under correction #18: with Part A's scale retracted and literature subsystem rates ~0.2 per pair primary, our old f ≤ 0.1 fence is ~1.5σ low — closer to the truth than Banik et al.'s 0.69, which stays excluded under every reading.) What survives of the critique: the fitted 0.69's exclusion, the kinematic strain of the published wobble amplitude law without photocenter cancellation, and the information advantage of the direction channel. The ablation table is retained as a mechanical map of where each modeling freedom acts.

(5) Feeding the measured boost back through the same external-field solver yields the solar-system anomalous quadrupole Q₂ = 3.9×10⁻²⁶ (α/1.15) s⁻² for both interpolating families — exceeding the Cassini bound of Hees et al. (2014) by ~4×, an independent wide-binary-calibrated confirmation of the Desmond–Hees–Famaey (2024) tension immune to their mass-to-light and bulge mitigations — a contribution suspended with the binary amplitude by (3): the tension reverts to its galaxy-calibrated form, which our solver cross-validation supports but no longer independently strengthens. Executing the corresponding modified-inertia-versus-modified-gravity discrimination on the binary eccentricity structure (apparently for the first time), we find the binaries decisively demand the external-field suppression amplitude but cannot distinguish local from trajectory-global boosts once it is present: the external-field-respecting modified-inertia branch — which need not produce the Cassini-capped quadrupole — emerges as a fully viable, Saturn-safe reading of the anomaly.

The framework's remaining falsifiers are stated: the wide-binary companion attribution (the forward-modeled median and the forbidden-band leakage null evaluated at the measured multiplicity — the declared decisive binary instrument), the rung-2 coefficient (requiring ~0.02-dex lensing mass cross-calibration or Gaia DR4), the modified-inertia/modified-gravity discrimination at DR4 precision, the mixing exponent's regime pattern (any bath construction must produce β = 0 at the transition with sharpened screening beyond it), and a₀ ∝ H(z). A transparency appendix documents the eighteen corrections and retractions logged during this program; every claimed number is reproduced by a script in the public repository. Following an external methodological review, the tested function list, external-field conventions, and primary analyses are frozen in the repository's registered prediction ledger; any function-level claim beyond that list is out-of-sample by construction.

---

## 1. Introduction

Rotation curves of disk galaxies obey a tight radial acceleration relation (RAR): the observed centripetal acceleration g_obs is a function of the Newtonian baryonic acceleration g_bar alone, transitioning from g_obs ≈ g_bar at high accelerations to g_obs ≈ √(g_bar·a₀) below a₀ ≈ 1.2×10⁻¹⁰ m/s² (McGaugh, Lelli & Schombert 2016). Whether this regularity reflects galaxy-formation physics within cold dark matter or a modification of dynamics in the spirit of Milgrom (1983) remains unresolved, in large part because rotation curves probe a single dynamical configuration: near-circular orbits in self-gravitating disks.

Wide stellar binaries offer a categorically different probe. At separations beyond several kAU, the internal acceleration of a solar-mass pair falls below a₀, so any modified-dynamics law calibrated on galaxies makes a definite, parameter-free prediction for binary relative velocities — while the systems remain clean two-body problems embedded in the Galactic external field. Gaia has made samples of thousands of such pairs available, and three groups have measured them to sharply conflicting conclusions: Chae (2023, 2024) finds a velocity boost of γ_g = 1.43 ± 0.06 with Newtonian dynamics rejected at 10σ, reaffirmed at γ_g = 1.37 (+0.10/−0.09) and 5.8–9.2σ across independent sample constructions; Banik et al. (2024) find Newtonian dynamics *preferred* at 16–19σ; Pittordis, Sutherland & Shepherd (2025) find Newton favored with the caveat that "improved understanding of the triple population is necessary to make this fully decisive." (A 2026 quality-framework analysis — Cookson et al., arXiv:2602.24035, with Banik, El-Badry, Sutherland, Penoyre, Pittordis & Clarke — likewise reports no MOND evidence from a median-ṽ flatness test on 1,421 RV-clean pairs within 130 pc; §7.4 engages it directly.) All three fit velocity-magnitude distributions; none uses the velocity *direction* information; and each treats the dominant nuisances — hidden companions, eccentricities, contamination — differently.

This paper does three things.

**First, it re-measures the anomaly** with what we believe is the most complete systematics treatment yet attempted: a joint two-dimensional likelihood over the normalized velocity ṽ and the velocity-separation angle γ; a nuisance model in which every component has a *measured identity* (the eccentricity mixture validated against an independent published measurement; the companion sector bounded jointly by photometry and kinematics; contamination channels with distinct two-dimensional signatures; the catalog's selection function emulated); and full realization + bootstrap error budgets. The direction channel — immune to mass errors by construction — turns out to be the instrument that protects the boost measurement from nuisance absorption (§7.3). The program's final binary stage then measured the fenced companion sector itself, and the fit-level anomaly did not survive the measured multiplicity (§6.3): this paper reports that verdict with the same discipline as its detections, and states exactly which model-light channels still carry the anomaly and what instrument decides them.

**Second, it tests a specific theoretical reading.** Cadoni & Tuveri (2019) derived the empirical RAR function from thermal excitations of the de Sitter horizon: ν = 1 + n_BE, with a₀ = H/2π emerging rather than fitted. We subject that framework to its first coefficient-level tests (§2–§4), measure its predicted boost in binaries (§5–§6), and compute its solar-system consequences (§8) — where we find, feeding our own measurement through our own solver, a quadrupole that Saturn's ephemeris forbids. The paper thus reports a serious internal tension for all modified-*gravity* readings of the anomaly, confirms it cannot be escaped by the mitigations available to previous statements of it, and demonstrates that the modified-*inertia* branch survives both the binaries and Saturn.

**Third, it reconciles the field — and ends up corrected by its own reconciliation instrument.** By ablating our own analysis toward the published Newton-favored choices — removing the direction channel, unfencing the companion fraction, dropping the deep anchor bin — we map, quantitatively and on a single dataset, where each modeling freedom acts (§7.3). Our original reading of that map, that the 16σ-Newton result was *manufactured* by a companion fraction "the photometry forbids," is retracted (correction #16): the flagged-rate-as-bound logic was invalid, the companion freedom was directionally legitimate, and the marginalized re-fit ruled against our own fit-level signal (§6.3). The companion-completeness forward model we built to settle the question — the first in the wide-binary literature, we believe — was then itself corrected by its reviewer-designed validation (correction #18: the within-pair coherence of overluminosity shows its host-rate scale over-attributed), leaving the literature's subsystem rates primary and the fit-level verdict prior-dependent, stated as such. What remains of the critique is specific rather than normative: the fitted 69% is excluded under every reading, and the wobble amplitude law the literature shares omits photocenter cancellation.

A single number threads the paper: **one-half**. It is the zero-point term of the Planck-oscillator form of the RAR function (§2.2); the fixed point shared by both surviving cells of the thermal bath matrix (§2.3); the band the galaxy data measure for the leading coefficient (ĉ₁ ∈ 0.26–0.45, §4.3) and the wide binaries indicated under the fenced model (0.37–0.50 — an indication the landed-posterior re-run erased: the binary λ profile is flat once companions and width are marginalized, §6.4); the branch whose external-field tables the binaries then calibrate at α ≈ 1 (§6); the first rung of a Bernoulli ladder whose next rungs — the ¼-versus-½ digit, and 1/12 versus 1/8, the quantum-versus-classical bath question — are the program's declared open problems (§4.3, §4.5, §9); and the source of a second-moment prediction whose first tests appear in §4.6. We do not claim the half as ours (its derivations precede us; §2), and we do not claim it proven (the galaxy-side profile is nuisance-sensitive within the band, and the conservative bootstrap accounting keeps the 0-branch exclusion at the ~2σ level); we claim the *dial measured* — zero excluded in every galaxy treatment (and, fenced-model-conditionally, in the binaries), the ½ inside every bootstrap band — and we structure the paper so that a reader who takes nothing else away takes that.

Throughout, we practice what we call honest updating: every result passed a validation gate before being trusted; eighteen claims made during this program were subsequently corrected or retracted by our own checks, and all eighteen are documented in Appendix A rather than silently repaired. Where this paper says "pre-registered," it means *bar-locking*: verdict thresholds and kill conditions committed by git hash before the deciding script ran, in the same session and with knowledge of all previous stages. That guards against post-hoc bar-moving and outcome-shopping — several locked bars fired against us — but it is not prospective pre-registration and does not control the error rate of the program's sequential search; we use the word for the former discipline and claim nothing of the latter. Every number quoted in this paper is produced by a named script in the public repository (Appendix B).

Two further disciplines were adopted from an external methodological review of this draft (2026-07; the point-by-point response is in the repository notebook). First, a pre-declared **primary analysis** per headline claim — the vertical-hardened hierarchical treatment for galaxies, the six-seed corrected-velocity budget for binaries — with every other treatment reported as sensitivity. Second, a **function freeze**: the in-sample function search is closed at the thirteen-law world table; newly constructed forms may be registered with their predictions, but their in-sample fits enter the record as consistency rows only, and function-level claims route through the out-of-sample ledger ([PREDICTIONS.md](PREDICTIONS.md) §0). Where this paper compares functions ("widest margin", "equal-highest bootstrap fraction"), the comparison is scoped to that closed ledger and carries no evidential weight beyond it.

## 2. The structure of the RAR function

### 2.1 The occupation identity (Cadoni & Tuveri 2019)

The RAR fitting function of McGaugh et al. (2016),

> ν(y) = 1/(1 − e^(−√y)),  y = g_bar/a₀,

satisfies the exact identity

> **ν = 1 + n_BE(x), x = √y, n_BE(x) = 1/(e^x − 1).**

This is not our observation: Cadoni & Tuveri (2019, arXiv:1904.11835) both stated it (their Eq. 23) and *derived* it, obtaining the anomalous acceleration as the response of Bose–Einstein-distributed soft excitations of the dark-energy/de Sitter horizon in thermal equilibrium at T_dS = H/2π — from which a₀ = H/2π ≈ 1.1×10⁻¹⁰ m/s² follows rather than being fitted. We arrived at the identity independently before locating their priority, and this paper is constructed accordingly: **Cadoni & Tuveri derived it; we test it.**

### 2.2 Two elementary corollaries: the zero-point half and the Bernoulli ladder

Two structural consequences of the identity appear to be unremarked in the literature (verified at the pre-circulation INSPIRE sweep: of the 31 papers citing Cadoni & Tuveri, none states or tests these structures, and the authors published no empirical follow-up; seven low-visibility citing entries resisted retrieval, so "apparently" is retained):

**(a) The Planck-oscillator form.** Algebraically, ν = ½ + ½·coth(x/2): the RAR interpolating function *is* the mean-energy law of a Planck oscillator, E/ℏω = ½·coth(ℏω/2kT), zero-point term included, under x ↔ ℏω/kT. Its limits acquire physical readings: deep MOND (x → 0) is classical equipartition, and the Newtonian regime (x → ∞) is the *frozen* oscillator — ν(∞) = ½ + ½ = 1, Newtonian gravity as the pure zero-point response with the thermal part switched off.

**(b) The Bernoulli ladder.** The low-acceleration expansion of ν has coefficients that are exactly the Bernoulli numbers:

> ν = Σ_{n≥0} B_n⁺ x^{n−1}/n! = 1/x + 1/2 + x/12 − x³/720 + x⁵/30240 − …

Equivalently, g_obs = √(g_bar·a₀)·(1 + c₁x + c₂x² + …) with c₁ = ½, c₂ = 1/12, c₃ = 0, c₄ = −1/720 for the occupation law. Comparison families make different parameter-free predictions at each rung: the "simple" function ν_s = ½ + √(¼ + 1/y) shares c₁ = ½ but has c₂ = 1/8; the "standard" family has c₁ = 0. The ladder converts the choice of interpolating function — historically an aesthetic one — into a sequence of measurable coefficients whose discrimination grows with depth (rung-2 ratio 2:3; rung-3 ratio 5.6; rung-4 ratio ~30).

### 2.3 The bath matrix: simple-ν is the classical bath

Taking the thermal reading seriously generates a 2×2 matrix of bath laws — occupation statistics (quantum Planck vs classical equipartition) × mode-frequency prescription (source-driven, ω ~ √(g_bar·a₀), vs self-consistent, ω ~ g_tot):

| | source-driven | self-consistent |
|---|---|---|
| **quantum** | ν = 1 + n_BE(√y) — c₁ = ½, c₂ = 1/12 (the RAR/C&T law) | ν = 1 + n_BE(νy) — c₁ = ¼, c₂ = 7/96 |
| **classical** | ν = 1 + 1/√y — c₁ = 1 (solar-system-excluded a priori) | **ν = ½ + √(¼ + 1/y) — exactly simple-ν** |

The lower-right identity is exact and two lines long: classical equipartition n = kT/ℏω with the frequency set self-consistently by the total field gives ν = 1 + 1/(νy), whose positive root is the Famaey–Binney (2005) simple function. Two consequences follow. First, the two leading empirical interpolating functions are the *quantum/source-driven* and *classical/self-consistent* cells of one thermal structure, so the long-standing empirical contest between them is secretly a physics question about the bath. Second, the ½ tested in §4 has two readings — zero-point occupation, or self-consistency algebra — and we therefore claim only a branch-level confirmation, not a unique detection of the vacuum term. The remaining cell (the self-consistent quantum "bootstrap," c₁ = ¼) is a new, fully determined function; we test it in §4.4 — disfavored under flat M/L, it becomes the *leading* function of the matrix once per-galaxy M/L is hierarchically marginalized, and the empirical question "which bath?" sharpens into the ¼-vs-½ contest that threads §4.3–§4.4 and §6.4. (Nearest prior art for thermal derivations of interpolating functions is the equipartition-on-holographic-screen family — Pazy & Argaman 2012; Pazy 2013 — which modifies the quantum statistics on a screen rather than closing the mode frequency self-consistently. The current review position is that no first-principles derivation of the simple function exists — "There is no existing clear derivation of such a transition from first principles, and so it is approximately inferred from data," Famaey & Durakovic 2025 — and our verification sweep found no prior statement of the self-consistency identity; the nearest recent thermodynamic construction, Trofimov's "Timeflow Gravity" (CQG 2026), recovers MOND-like dynamics via vacuum phase interference, a distinct mechanism, though its full text was not accessible at our check — Appendix A.)

### 2.4 What any mechanism must now satisfy

The program's measurements convert "why would gravity behave like a Planck oscillator?" from speculation into a specification. Any microphysics for the bath must deliver:

1. **The frequency law.** The occupation argument is x = ℏω/kT_dS with **ω = √(g_N·a₀)/c** — the geometric mean of the local Newtonian acceleration and the horizon scale, over c. In the deep regime this equals g_obs/c, so the argument is the ratio of the *Unruh temperature of the observed acceleration* to the *de Sitter temperature*: x = T_U(g_obs)/T_dS (exactly in the deep limit; the source-driven form of §2.3 uses g_N at intermediate x, while the self-consistent cell — the hierarchical contest's preference, §4.4 — uses the observed acceleration at *every* x, making the occupation-ratio reading exact throughout). The temperature side of this structure is established physics — accelerated detectors in de Sitter space see 2πT = √(a² + Λ/3) (Deser & Levin 1997), and Milgrom (1999) proposed that the excess T(a) − T(0) "depends on a in the same way that MOND inertia does," while stating that an actual inertia-from-vacuum mechanism was "still a far cry off"; an Unruh-flavored MOND reading also exists (arXiv:0908.4239). The *occupation-ratio* observation — the RAR function as a Planck occupation evaluated at that temperature ratio — appears unpublished (pre-circulation scout) and is stated here as an observation, not a derivation.
2. **Bose statistics with a zero-point-scale coefficient.** The measured c₁ — 0.26–0.45 by profile across nuisance treatments, ≈ 0.4 ± 0.3 by bootstrap, 0 excluded throughout on the galaxy side (the binaries' fenced 0.37–0.50 indication erased under the landed posterior; §4.3, §6.4) — must come out of the statistics; whether it is the ½ (zero-point or self-consistency algebra, §2.3) or the self-consistent-quantum ¼ is the live cell-level question, and the two M/L treatments now disagree *coherently*: flat M/L prefers ½ (function contest 3.1σ against ¼), hierarchical M/L prefers ¼ (the profile peaks there and the exact ¼-function leads the converged contest by 76 in −2lnL, 86% of bootstraps; §4.4).
3. **A temperature locked to the horizon.** a₀ = cH₀/2π at the current epoch — the galaxy-side scorecard rows sit within 0.5σ of it (§9.1), and the full-hierarchy fits with measured vertical priors return a₀ = 1.04–1.05 ×10⁻¹⁰ under both the occupation and geometric-mean laws, the program's strongest statement of the lock (§4.4) — with a₀ ∝ H(z) as the standing falsifier.
4. **A finite, modest mode count — if the second-moment channel is thermal.** N ~ 20–60 effective modes per measurement, realized point-level rather than galaxy-level; the channel's identification against per-galaxy vertical nuisances is open at SPARC depth (§4.6).
5. **An ambient-field response.** The external-field suppression amplitude is demanded by the binary data in every bracket tested (§8.2): the bath must know the total ambient field, not only the local source.
6. **A mechanism-agnostic amplitude that spares Saturn.** The boost must be deliverable by either a field (modified gravity) or a trajectory functional (modified inertia) at current precision, while evading the Cassini quadrupole (§8.1) — the strongest single constraint on any concrete construction.

The frequency question — item 1 — can now be stated as a *one-parameter theory family with exact consequences*, which converts "why this function?" into "what sets one exponent?". Let the mode frequency mix the two natural prescriptions of §2.3 geometrically, ω = ω_source^(1−β)·ω_total^β, i.e. an occupation argument u = y^((1+β)/2)·ν^β. Then (derived and numerically gated in `stage5p_betafam.py`):

> **c₁(β) = 1/(2(1+β)), c₂(β) = 1/(12(1+β)) + β/(8(1+β)²), tail exponent p_tail = (1+β)/2 — hence the parameter-free relation c₁·p_tail = ¼ across the entire family.** β = 0 is the occupation law (½, 1/12), β = 1 the quantum bootstrap (¼, 7/96), and β = ½ the geometric mean (⅓, with the Bernoulli 1/12 *recurring*).

Three structural facts follow. First, deep-MOND scale invariance cannot choose β: in the deep limit both frequencies coincide (ω_source, ω_total → g_obs/c), every member is scale-covariant, and the family differs only through the transition and tail. Second, β = ½ is the unique fixed point of the source↔response *exchange symmetry* (swapping the sectors maps β → 1−β): if the bath treats what drives it and what it sustains symmetrically, c₁ = ⅓ follows with no residual freedom. Third, the geometric mean is where maximal-coupling conditions land in several canonical settings — the impedance-matched mode between two sectors (Z = √(Z₁Z₂), the quarter-wave condition), the maximum-power working temperature between two reservoirs (Curzon–Ahlborn: √(T_h·T_c)), and the normal mode of an oscillator whose stiffness one sector sets and whose inertia the other does (ω² = ω₁ω₂ — an unexpectedly modified-inertia-flavored reading). The ladder also extends one rung: the next deep-series coefficients are **c₃(β) = β(3β+1)/(24(1+β)³)** and c₄(β) = (502β³+51β²−54β−8)/(5760(1+β)⁴) (symbolic series, gated against 50-digit implicit roots), and in the tail-rescaled variable the whole ladder goes polynomial — c₁p = ¼, c₂p² = 1/48 + 5β/96, c₃p³ = β(3β+1)/192. Two further algebraic facts single out the distinguished members: c₃'s only zero is β = 0 — the occupation law's Bernoulli zero (the vanishing x² term it inherits from 1/(1−e^(−x)), with c₄(0) = −1/720 = −B₄/4! exactly) is *unique* in the family, so any admixture of self-consistency switches the NNLO rung on — while the third log-cumulant of the deep series, c₃ − c₁c₂ + c₁³/3 = β(2β−1)/(16(1+β)³), vanishes at **β = ½**: the symmetric point is also the unique β > 0 member whose ln(xν) expansion has no cubic term. We state these as readings, not derivations: the sharpened construction problem is to derive, from the thermal-horizon setup of §2.2, *which* combination of the source and response frequencies the occupied modes carry — Cadoni & Tuveri's choice is β = 0, implicit and unexamined. And β is henceforth a measured dial, not a stipulation: profiled over the family with the full hierarchical machinery, the galaxies give **β̂ = 0.64 (disk-M/L hierarchy) and β̂ = 0.45 (adding the measured vertical channel — the most-controlled treatment), both interior, Δ(−2lnL) = 86 and 43 over β = 0** — the hardened galaxy measurement lands on the exchange-symmetric point, implied c₁ = 0.35 — while the binaries pin **β = 0 sharply: every β > 0 loses on every population realization (24/24 sign-consistent), with mean-profile bounds β < 0.03 (1σ) and β < 0.12 (2σ)** (§6.4). And the two-system disagreement *resolved under decomposition* (§4.4): the deep zero point and the transition hold β ≈ 0 everywhere — in the binaries and inside SPARC itself — while the screening tail is the lone β > 0 voter in the program. *The split is not galaxies versus binaries but deep-and-transition versus tail*: the effective mixing exponent runs with regime, which no single member can accommodate, because the family locks c₁·p_tail = ¼ exactly while the data ask for c₁ ≈ 0.4–0.5 and p ≈ 0.65–0.75 at once. Two readings survive, and one is demoted. Surviving: a configuration-dependent bath — the response admixture switching on where the occupation is low (the screening tail is the n ≪ 1 quantum end, the deep limit the n ≫ 1 classical end; a β running from 0 to ½ across that boundary would reproduce every vote in this paper, and would fix the asymptotic tail at the geometric-mean p = ¾) — or a fixed function outside the family that breaks the ¼ lock upward. Demoted: the modified-inertia mimicry reading of the split — the intra-SPARC regime split lives entirely on circular orbits, where modified inertia and modified gravity coincide, so trajectory statistics cannot be what produces it (modified inertia remains the Saturn door of §8.2 on its own merits). [`calcs/stage5p_betafam.py`, `calcs/stage5r_binbeta.py`, `calcs/stage5t_betasplit.py`]

We offer the checklist any construction must clear — with every entry on it measured, not assumed, in this paper — and, as of the function contests of §4.4, one *function-level* candidate that inherits every entry without tuning: the geometric-mean bath ν = 1 + n_BE(y^(3/4)√ν), the β = ½ member. What we still do not offer is the microphysics that would make the *measured β pattern* inevitable — a deep zero point at the occupation value with decoupled, sharpened screening; that is now the construction problem in its sharpest form.

A first execution of that construction is reported in this draft. The regime pattern — β = 0 where occupation is high, β → ½ where it is low — is what Einstein-coefficient channel physics produces if the response admixture enters through the *spontaneous* channel: stimulated processes (rate ∝ n) follow the drive, the source-prepared field; spontaneous emission (rate ∝ 1) probes the mode's own structure. Weighting the response share by the spontaneous fraction gives **β = ½·1/(1+n) = 1/(2ν)** (rate share; the zero-point-energy share gives the sibling β = 1/(2(2ν−1))) — zero parameters, with Cadoni & Tuveri's implicit β = 0 recovered as the *classical limit* of the bath and the exchange-symmetric ½ as its quantum endpoint, now with a reason: it is where only spontaneous processes remain. The exact consequences were derived and gated before any fit: **c₁ = ½ exactly** (the running dies fast enough deep that the occupation zero point survives untouched), **tail p = ¾ exactly**, lock product c₁·p = ⅜ — broken upward precisely into the measured band — with c₂ turning negative (−1/6 and −1/24; a rung-2 signature versus the occupation law's +1/12, unmeasurable at current reach) and ν(1) = 1.470/1.494, between the geometric mean and the occupation law. The two-system test (§4.4, §6.4): the derived functions **lead every hierarchical galaxy treatment outright** — −107/−101 past the occupation law with per-galaxy M/L, −52/−44 with the vertical channel, beating the geometric mean in all four contests — and close a third of the binary transition gap (−5.5 ± 2.3 and −5.8 ± 1.9 versus the geometric mean's −8.5, all amplitudes interior, one realization flipping positive), while the Cassini lock holds at 4.8–5.3× (§8.1). The residual binary lean (~2.4 SE) and the pending a₀ translation of the α̂ ≈ 1.4–1.5 amplitudes are the candidate's open ledger, and they point at the same refinement: the transition wants an even smaller admixture than 1/(2ν) leaves there. The weighting choice is a stated reading, not a proof; but the function it produces is the leading cross-system performer in the function ledger at that stage, and its two sharp predictions were on paper first. (Priority: no running or occupation-dependent interpolating exponent, no Einstein-coefficient derivation of an interpolating function, and no Cadoni–Tuveri follow-up modifying the occupation argument were found at the pre-circulation scout — quoted as scout-level negatives.) [`calcs/stage5u_runbeta.py`, `calcs/stage5v_rungal.py`, `calcs/stage5w_runbin.py`, `calcs/stage5x_runquad.py`]

The refinement was then executed, and the story completed itself in an unexpected direction. Two-leg spontaneity — the self-frequency requiring the round trip, emission *and* reabsorption both spontaneous — squares the share (β = 1/(2ν²) and 1/(2(2ν−1)²)) and protects **both Bernoulli rungs exactly** (c₁ = ½ and c₂ = 1/12; c₃ = −¼ and −1/16), raising ν(1) to 1.503/1.537. The galaxies reward it again — the ladder is monotone toward this shape in both treatments, the two-leg energy-share member reaching −109/−64 with a 37/40 bootstrap (§4.4) — but the binaries reject it as they rejected every sharpened function (−6.8 and −7.0, the latter edge-riding on all six realizations; §6.4), and the a₀ translations land at +5 to +6σ (§6.4). The decisive control followed: the simplest configuration-dependent rule — the admixture weighted by the pointwise internal-field share w = g_int/(g_int+g_ext), which reduces to the two-leg function in isolation and to the pure occupation law under a dominant external field — was implemented inside the external-field solver (isolated-identity and sandwich gates exact) and **excluded** by the binary likelihood (−9.6 ± 1.5, worse than the unweighted function, punished precisely at its predicted mid-separation signature) while *passing* the temperature row (a₀ = 1.31 ± 0.13, +2.1σ). The construction problem's final state, stated plainly: **the two systems are measured to carry different effective baths — occupation-law screening in the external-field-dominated regime, sharpened two-leg screening in isolated disks — and the difference is not a function of the local field configuration.** What can produce that is a system-level, ambient-gated bath character (the environment as a whole, not the local ratio, setting the mode's drive) or a trajectory functional (modified inertia — which the intra-SPARC regime split cannot be attributed to, but the cross-system split can); both are carried as open readings with their §9.2 tests. [`calcs/stage5z_twoleg.py`, `calcs/stage6a_twogal.py`, `calcs/stage6b_twobin.py`, `calcs/stage5y_kladder.py`, `calcs/stage6c_f4boot.py`, `calcs/stage6d_drivebath.py`]

The ambient-gated reading was then derived and tested, and it is the section's current end point. The first guess — ambient traffic *dilutes* the spontaneous share — fails on sign, instructively: weak ambient fields mean *deep, highly occupied, classical* ambient baths (n_amb = n_BE(√(e_N/a₀)) ≈ 6.6 for field-galaxy environments, ≈ 0.52 at the binaries' Galactic field), and the measured pattern wants suppression where the ambient is *sparse*. The reading with the right sign, in the same Einstein-coefficient grammar as everything above: **self-consistent dressing requires the ambient reservoir to assist the frequency exchange, and reservoir assistance is a stimulated process** — a classical ambient is a floppy medium in which the mode re-equilibrates, a quantum ambient is stiff and pins the mode at its drive frequency. The unified rule, with no parameters:

> **β = ½ · [½/(n_loc+½)]² · [n_amb/(1+n_amb)]²** — *admixture = (local quantumness) × (ambient classicality)* — system-level by construction (one ambient occupation per system, which is exactly why the pointwise rule's mid-separation signature does not appear).

Its exact consequences were fixed before the fits: c₁ = ½ and c₂ = 1/12 *independent of the gate*; the entire ambient dependence enters the deep series at a single rung, c₃ = −g_amb/16; and the tail exponent becomes an environmental observable, **p = ½ + g_amb/4 — post-dicting both measured screening exponents (0.69 at galaxy ambients, against §3's 0.65–0.75; 0.53 at the binaries', against their held ½) from zero parameters.** The pre-registered tests: the binaries **accept** it (−0.9 ± 2.7 against the occupation law, 2/6 realizations preferring it, all amplitudes interior at α̂ = 1.060 ± 0.024; κ = 0.924; **a₀ = 1.28 ± 0.15, +1.6σ — the least-strained binary a₀ row in this paper**), and the galaxies **hold** −59.1 under the vertical-hardened treatment (passing its bar; second only to the undiluted two-leg function whose binary veto it escapes; dilution cost 5.2) while missing the plain-treatment bar by 8 (−92.4 against ~−100; the ambient-band ends give −82.6 to −97.4) — a partial disclosed as such. **The joint two-system ledger: −57.3 against the occupation law — the only function among those tested to pass both regimes' pre-registered bars.** The reconciliation is a design property of the function, not a measurement — the gate places AMB within 0.3% of the occupation law at the binary ambient *by construction*, so the binary pass was largely built in — and we flag that plainly; its non-guaranteed content — the vertical-treatment survival, the +1.6σ temperature row, the two tail post-dictions — landed, and its out-of-sample content is stated in §9.2: environment-resolved SPARC outskirts must soften along g_amb(e), and weak-ambient binaries must sharpen toward p = 0.69. (Priority scout: no environment-dependent interpolating-function *shape*, no decoherence-gated modification, no Einstein-coefficient environmental modulation, and no two-function proposal for the binary–galaxy tension found — scout-level.) [`calcs/stage6e_ambgate.py`, `calcs/stage6f_ambgal.py`, `calcs/stage6g_ambbin.py`]

The rule's grammar was then formalized and, where possible, measured. Written as **β = ½·[q_loc·s_amb]^L** — a ceiling admixture times a per-leg weight raised to the leg count — its three structural choices separate cleanly. *The local factor is now derivation-grade:* the exact dispersive-coupling result (a Jaynes–Cummings manifold block; gated symbolically) is that a mode of occupation n pulls a coupled system's frequency by λ²(2n+1)/Δ — the vacuum half-quantum is *in* the pull — so a frequency-dressing weight carries the zero-point share 1/(2n+1) = 1/(2ν−1), an energy share and not a rate share; this selects the (2ν−1)-lineage that the galaxy contests had already preferred, and answers the natural worry about "local quantumness": it is the vacuum fraction of the mode's frequency pull, a deterministic function of the local field through the self-consistent occupation — nothing stochastic enters the solver. (What stays reading-grade is that horizon dressing is dispersive.) *The leg count is now empirical:* the deep series breaks the Bernoulli ladder at exactly the rung c_{L+1} — L=1 kills c₂ (→ 1/12 − s/8), L=2 preserves c₂ and writes c₃ = −s²/16 (the 6E rule), L=3 preserves c₂ and c₃ both, with c₄(s→0) = −1/720 recovered at every L and a new exact rung on record, c₄(L=2) = s²/192 − 1/720, a predicted sign flip of the fourth coefficient at galaxy ambients. The hierarchical galaxies, fit against all three integers at the fiducial gate under the controlled treatment, point-prefer **L = 2**: −59.1 against −52.8 (L=1) and −54.5 (L=3). The 40-rep galaxy-population bootstrap then graded that preference and deflated it (correction #13): the L=1 rejection survives in 29/40 resamples (+9.9 ± 21.4 — a lean, carried by the c₂ rung), L=2-versus-L=3 is statistically unresolved (+1.6 ± 13.1; 21/40), and L=2 is strictly best in only 10/40. The correspondence itself — the Bernoulli-break rung as the leg-count register — stands as exact mathematics, and the point ordering lands where the grammar predicts; but the deep arm of 153 galaxies is too thin to read the rung at population grade, so the round trip remains the *predicted* process order with a first, lean-grade empirical vote — not a measured one. Deeper rotation-curve samples read this dial directly. *The ambient input was then replaced by data:* the per-galaxy environmental fields of Chae et al. (2021) (91 of 149 hierarchical galaxies matched; unmatched and lensing carry the matched median) are *sharper* gates than our e = 0.02 fiducial (median g = 0.87 max-clustering / 0.95 no-clustering versus 0.75), and substituting them improves every treatment at zero parameters — the controlled treatment to −61.7, and the plain treatment to **−100.5/−105.9, across the pre-registered ≤ −100 bar that the fiducial run had missed by 8**: the one disclosed shortfall of the rule's first test is resolved by measurement, not adjustment. (A later permutation control — correction #14 — reattributes this gain to gate *heterogeneity* rather than the physical ordering: the true assignment loses to all eight gate-preserving shuffles at weak grade, so the improvement is real but ordering-blind, and the environmental-ordering claim moves out-of-sample.) The measured-ambient lead was then bootstrap-graded with the same machinery as its rivals: **−57 ± 36 across 40 paired galaxy resamples, 37/40 (92.5%)** — the equal-highest bootstrap fraction in the function ledger, shared with the undiluted two-leg function, now carried by a rule the binaries accept; three of forty replicates flip sign, so this is a strong lean, not a detection, and we quote it as such. Honest residuals stay on the ledger: the galaxy leg alone rewards sharper gates monotonically (the undiluted two-leg function remains its formal ceiling, +2.5 ahead) — it is the *binaries* that pin the gate amplitude, so the rule's content is irreducibly two-system; and the ~5-point spread between Chae's clustering conventions is carried as an input ambiguity. (Priority scout on both new structures — the Bernoulli-break/leg-count correspondence and the (2n+1) selection argument — NOT FOUND; scout-level.) The grammar also rewrites exactly in lab-native thermal functions — **β = ½·tanh²(x_loc/2)·e^(−2x_amb)**, the squared ambient gate being the Boltzmann cost of borrowing two ambient quanta — which makes it testable against standard open-quantum-systems dynamics; a first pre-registered desktop test (exactly solvable two-channel Kerr-mode steady states) *excludes the jump-rate reading* of the grammar — incoherent rate competition yields constant or wrong-sign-running admixture, both already excluded by the sky data, and is rate-ratio-dependent where the grammar is rate-free — leaving the coherent-pull reading (the dispersive-shift channel the (2n+1) anchor actually describes) as the surviving mechanism candidate; we log this as a strike against the mechanism's simplest realization, taken per the pre-committed outcome tree. The physically-derived interpolation — one bath of finite spectral resolution, in which the rate constant cancels exactly — was then computed under its own pre-registration and returned the ambiguous branch (the endpoint gap is K-order while the kernel systematic is b²-order, drowning the estimator mid-scan), with two beyond-band observations on the record: where the estimator is clean, the admixture runs with occupation *opposite* to the grammar, and the resolution dial's gravity translation is transition-peaked and tail-zero — the wrong sky shape in both non-deep regimes. The closing instrument — an exact two-mode Lindblad treatment of the buildable filter configuration, plus the minimal KMS-structured bath — was then executed under its own pre-registration and **closed the laboratory question**: a cancellation theorem (stated in design, confirmed to |λ| ≤ 0.007 at all couplings) shows any flat-occupation bath locks a self-shifting mode to the source occupation exactly, so which occupation such a mode takes is decided entirely by whether the bath's thermal (KMS) structure is resolved across the self-shift; the resolved-structure carrier produces admixture organized by bath geography, not by the mode's occupation share (34× off the grammar's form); and the carrier's gravity translation is transition-peaked and tail-vanishing — the sky's tail-only admixture is *anti-standard*. Three realization classes — jump-rate, golden-rule structured, exact filter dynamics — thus all fail to produce the grammar's gating; per the pre-committed trees we place the bath-microphysics conditional at ~8%, note that a fridge experiment would only re-measure the standard physics computed here, and carry the mechanism's remaining homes explicitly: horizon-specific non-Markovian physics (the de Sitter "bath" is causal structure, not a filter bank), the modified-inertia reading (never bath-dependent), or none — the ambient-gated rule as sky phenomenology, which takes nothing from its function-level record. One horizon-specific sub-reading was then given its two accessible tests: the *frozen-bath* limit (bath correlation time ~1/H exceeds orbital times by 10²–10⁵, making the modes quenched draws rather than a reservoir) predicts a galaxy-coherent thermal-shaped residual channel and an s-shaped per-system scatter in the binaries; the former proved unmeasurable against real per-galaxy radial systematics (a shape-shuffled null gains a third of a nominal "detection" — the identifiability boundary holds even with shape leverage), and the latter's likelihood preference (+11/+13 across two seeds, edge-running) was *rejected by its own control*: an s-flat scatter of matched strength gains 26 points more — the appetite is generic residual broadening (a pipeline error-model fact worth ~+37 lnL, with α̂ exposure ≤ +0.08, inside the quoted ±0.11 systematic), not the frozen fingerprint. Both tests pre-registered; no support, no formal strike; the sub-reading's near-term signatures are spent. [`calcs/stage6h_grammar.py`, `calcs/stage6i_chaegate.py`, `calcs/stage6k_analog.py`, `calcs/stage6m_analog2.py`, `calcs/stage6n_pseudomode.py`, `calcs/stage6o_cohtest.py`, `calcs/stage6p_frozen.py`, `calcs/stage6pb_extend.py`]

The surviving horizon-specific home was then given its first constructive test. The de Sitter bath differs from every laboratory bath in one structural fact: its KMS occupation varies on the scale of its own temperature, and that temperature *is* the a₀ scale — so the cancellation theorem's resolution criterion is not a free dial but a forced function of the field. The dressed-to-source frequency gap in bath-temperature units, **R = νy − √y**, vanishes quadratically in the deep regime (exact source-locking — the measured β = 0 of every deep and transition arm) and grows without bound in the tail, crossing R = 1 at y = 1.88, the arm where the regime decomposition measured the admixture turn-on; no laboratory bath places that crossover naturally, and the one-scale horizon bath cannot place it anywhere else. The zero-parameter function this forces (β = ½·R²/(1+R²) in the frequency mixing; the Lorentzian profile is the one representative choice, flagged) preserves the measured Bernoulli ladder through c₄ and breaks at a new exact rung, **c₅ = −1/16**, reaches the geometric-mean argument exactly in the tail (p = ¾), and closes the exact-temperature alternatives analytically: the Deser–Levin source-frequency branch has no finite fixed point (anti-Newtonian runaway), the total-frequency branch is the excluded bootstrap cell plus an invisible constant floor, and a geodesic detector has T_U = 0 exactly — the free-fall reading of the binaries' β < 0.03. Its two-system test was pre-registered with the prior risk stated (transition sharpness at the two-leg grade with no ambient gate; the eight-function pattern leans reject): the galaxies rewarded it at STRONG grade (**−58.6** vertical-hardened — the ambient rule's grade; **−113.7** plain — the widest plain-treatment margin in the function ledger), and the binaries delivered the pre-registered **shape rejection** (α̂ edge-riding in 5/6 realizations, 0/6 preferring, −7.4 ± 2.1 behind the occupation law — squarely the sharpened-function band). The triangulation is thereby complete and mechanical: local-resolution running alone is galaxy-excellent and binary-dead (this test); pointwise ambient weighting alone is binary-dead (the drive-weighted control); only the two-factor rule — local quantumness × ambient classicality — passes both systems. What survives of the resolution reading is exactly its deep half: the one-scale bath explains *why* deep and transition regimes source-lock everywhere; what it cannot do alone is switch the tail on system-blind. [`calcs/stage6r_resolution.py`, `calcs/stage6s_resngal.py`, `calcs/stage6t_resnbin.py`]

The required gate was then derived to the same grade as the local factor, from the same object. Path-resolving the dispersive loop that produced the (2n+1) pull separates its two time-ordered exchanges: an absorption-side path of weight n (the partner mode must supply a quantum) and an emission-side path of weight n+1 (the vacuum term rides along); their ratio is n/(1+n) = e^(−x) *exactly* — the KMS detailed-balance weight — so a per-leg net-borrowing channel is Boltzmann-taxed at the partner's occupation, and L legs cost s^L = e^(−L·x_amb): the grammar's gate, with L = 2 the loop's own (Lamb-shift) order, and with each vertex contributing both measured factors at once — the local zero-point share on one side, the ambient KMS ratio on the other. The empirical half of the derivation is a uniqueness selection scored entirely against numbers already on the ledger, no new fits: of the loop's algebraically natural weights, the rate-balance steady state s²/(1+s²) postdicts a galaxy tail of 0.607 (below the measured 0.65–0.75 band — excluded), the absorption *share* [n/(2n+1)]² gives 0.554 (excluded — the data choose detailed balance over the share from the same decomposition), the inverse ratio fails both tails with the wrong environmental sign (the formalized version of the construction's original sign lesson), the raw amplitude n² is absurd (p ≈ 11) and violates the admixture ceiling, the pointwise and ambient-free versions are excluded by direct measurement (the drive-weighted control and the resolution-bath test), and only the KMS-ratio-squared form survives; the single- and triple-borrow variants remain at their bootstrap grades (disfavored-lean and open, respectively — the tail instrument is blind to that digit, the Bernoulli rung is not). Two dividends fall out: the ambient frequency convention (source vs dressed) is degenerate today but separates at weak-ambient binaries (Δp ≈ 0.03 — a DR4 discriminator), and the gate is a Boltzmann cost at T_dS = H(z)/2π, so the tail exponent must run with redshift at fixed physical environment (p_gal: 0.688 → 0.702 by z = 1) — folding the gate into the a₀ ∝ H(z) kill test. What remains reading-grade is the borrowing narrative itself — the frozen one-scale bath cannot supply dressing quanta, the occupied ambient can — whose microphysics lives with the non-Markovian horizon formalism; the frozen-bath *variance* nulls do not touch it, since borrowing enters at mean level. The derivation was then handed its own falsifier the same day: the tied-exponent requirement (one loop ⇒ one L) was contested against the untied family β = ½·q^L1·s^L2 on the controlled galaxy treatment under pre-registered bars — **the tied form survives** (no untied cell approaches the strike threshold; the grid is flat within the population noise this instrument is known to carry), while the pre-registered instrument-split bar *failed* and is logged as such: the likelihood reads the local exponent through the transition-suppression profile, not only the deep rung — which incidentally disfavors the one-leg cell through a second, rung-independent channel (−7 at point grade). [`calcs/stage6v_untied.py`]

The borrowing reading then received its first *constructive* mechanical support, in the one regime the laboratory closure left open. The frozen-horizon limit (bath correlation time ~1/H) makes the extreme non-Markovian case exactly solvable: with no bath at all during the evolution, a Kerr-anharmonic mode whose dress-ward transition is resonant with a single thermal ambient mode — dressing must absorb a real ambient quantum — has a dress-ward weight computable in closed form, and exact dynamics returns **½·P(n_amb ≥ 1) = ½·n/(1+n) to 0.2–2.2% over a 32-fold occupation range** (form regression: KMS-ratio slope +0.989 with residuals 6× and 29× smaller than the share and raw-n rivals — the pre-registered support bar cleared decisively). The identity behind it is exact and elementary: for a thermal state, **P(n ≥ L) = [n̄/(1+n̄)]^L = e^(−L·x)** — the geometric-tail identity — so the KMS ratio *is* the probability that the ambient can lend a quantum, and the gate s^L is the probability it can lend all L that the L-leg dressing needs; the Boltzmann-cost and lending-availability readings are the same number, and the availability reading ties the gate exponent to the borrow count directly. Two controls carry the physics: the resonant channel is coupling-strength-independent (Rabi mixing of matched configurations — incidentally a candidate dynamical origin, flagged as a reading, for the admixture ceiling ½ as equal time-sharing), while a detuned control channel scales as λ² and follows raw n̄ — the two channels carry *different* occupation forms, and the sky's measured ratio² selects the real, energy-conserving exchange over virtual dressing, exactly what a frozen bath (which cannot absorb virtual imbalances) forces. Dephasing the exchange coherence closes the channel toward the cancellation-theorem locking. The pre-commitment specified only the failure branch; the mechanism conditional is held at its prior value with the lean now constructive, and the remaining seam is stated: the de Sitter-side identification of the lending reservoir, and the multimode generalization. [`calcs/stage6x_borrow.py`]

Both halves of that seam were then closed to the extent the data permit. The multimode question has an exact answer that the measured gate itself selects: a single collective thermal mode lends with the observed geometric tail P(n ≥ L) = s^L, while M modes lending democratically follow the negative-binomial tail 1 − (1−q)^M(1 + Mq) — which already at M = 2 saturates the galaxy gate (0.95 against the measured 0.75), erases the environmental dependence that the measured per-galaxy fields *improve* the fits with, and pushes the binary tail postdiction to the band edge the binaries reject. **The gate data force M = 1: one collective ambient mode per system** — the same system-level scalar structure the dynamics had already selected through three independent exclusions, now reached through counting statistics. The identification of that mode is stated as a reading: the system's barycentric coordinate in the ambient field — one degree of freedom per system, whose occupation n_amb = ν(e_N) − 1 is *the environment's own dressing cloud*, so that systems borrow from their environment's boost; as e_N → 0 the mode descends into the horizon's soft sector (n → ∞, gate → 1), the isolated limit rejoining the one-bath picture, while a strong ambient gaps the soft sector and closes the gate. Two consequences deserve emphasis. First, a parameter-free falsifier: since the gate is a probability, **the tail exponent is bounded, p ≤ ¾, everywhere and always** — with a practical void-galaxy asymptote near 0.72 — so a single population-grade measurement of a screening tail beyond ¾ kills the construction; the ambient-ordering prediction beneath it (p monotone decreasing in e_N) was tested in-sample by a permutation control at full sample power and *failed at its stated weak grade* (the true gate assignment loses to all eight distribution-preserving shuffles, empirical p ≈ 0.11 — correction #14): the measured-ambient fit gains are gate heterogeneity, generic under permutation, so the ordering moves fully out-of-sample to environment-resolved DR4-era samples, where it remains the rule's signed falsifier. Second, the Saturn corollary: a coupling to the barycentric collective mode is a coupling to the system's *state of motion* in the ambient — a trajectory functional, the modified-inertia class — and that class ties the vector solve on the binaries, thereby meets the composition demand at current resolving power, and carries no capped-type quadrupole. **The borrowing mechanics thus lands the thermal rule in the one door Saturn left open, by derivation rather than preference**; the discriminating instrument remains DR4 eccentricity resolution. [`calcs/stage6y_reservoir.py`]

The one factor the uniqueness selection had taken as derivation-grade without a data contest — the local zero-point share 1/(2ν−1) — was then contested directly. The quantum dispersive pull carries (2n+1): thermal quanta plus the vacuum term; a classical continuous-field bath's pull carries 2n alone, so the classical counterpart of the share is 1/(2(ν−1)), identical grammar, no "+1". The replacement is not a small deformation: without the vacuum floor the share grows as e^x toward the Newtonian side, the admixture diverges, and the screening tail steepens toward a step — and the galaxy ladder, which measures a tail exponent near 0.7, responds accordingly: **the classical-share variant loses by +556 (vertical treatment) and +274 (plain), is worse than carrying no admixture at all (+497 behind pure BE), and loosening the numerical cap on its divergence only worsens it (+580) — the rejection is a lower bound, twenty-fold past the pre-registered kill threshold.** The vacuum term is load-bearing: the "+1" is what caps the share at unity and keeps the admixture finite exactly where the data demand finiteness. The binary half of the same contest could not be run — the capped classical function's screening cliff defeats the field solver's spherical-identity gate at the 10% level, so the leg was aborted rather than fit on an untrustworthy table (disclosed; the binaries support the quantum share indirectly, by accepting the gated rule and bounding every admixture excess). Alongside the banked rungs — the no-vacuum limit β = 0 excluded by the gate credit itself, and the c₁ = 0 branch excluded in both systems — this puts the spontaneous-emission term of the bath's pull in the measured column for the galaxy ledger, conditional as always on the admixture grammar. A classical-statistics *ambient* (equipartition occupation in the gate) was contested in the same stage and returned null everywhere it was expected to (galaxies sit deep-ambient, where the Bose and classical occupations converge by construction) and unresolved where it might have discriminated (binaries, four seeds, scatter dominating a +1.7 mean): the ambient-statistics digit stays open. [`calcs/stage7d_vacuumshare.py`]

Finally, the mechanical toy behind the gate was translated into laboratory language, with its epistemic status stated first: the cancellation theorem forbids a flat-bath laboratory measurement of the sky rule, so what a laboratory can test is the mechanism class. In three-dimensional circuit QED at standard values (a transmon Kerr mode, χ/2π = 250 MHz, beam-splitter-coupled at λ/2π = 2 MHz to a cavity holding calibrated thermal photons), the closed-system lending law reproduces at platform parameters to 0.25%, the full dissipative calculation at real hardware rates (κ/2π = 5 kHz, T₁ = 200 μs) shifts it by under 2%, the saturation that separates a lending probability from linear response is a seven-fold effect, and **the two-quantum channel measures the geometric-tail rung [n̄/(1+n̄)]² directly** (regression slope 0.99 against 34-fold-worse raw-occupation residuals) — the sky's s^L structure as a bench curve, with the incidental observation that the second-order prefactor is again ≈ ½, a second data point for the equal-time-sharing reading of the admixture ceiling. A clean null in such an experiment would strike the borrowing mechanics; a confirmation would validate the grammar's building blocks without touching the horizon question. [`calcs/stage7e_platform.py`]

The mixing algebra itself — *why* the geometric mean between the source argument √y and the total argument νy, the one structural choice the derivation had not contested — was then put through the full power-mean family u_p = [(1−β)(√y)^p + β(νy)^p]^(1/p). Two exact results frame the answer. Deep-side, with the running gate, the ladder is p-blind through c₃ — the symbolic fourth coefficient is a₄ = −gp/64 + g/192 − 1/720, whose p = 0 value reproduces the loop derivation's independently obtained c₄ exactly (two routes, one rung) — so the Bernoulli structure could never have seen the mixing choice, and the tail must do the selecting. Tail-side it does: as the two arguments diverge, any p > 0 snaps the tail exponent to 1 (the boot grade this program's binaries veto) and any p < 0 snaps it to ½ (the ungated value, making the measured two-system tail split impossible) — **only the geometric point interpolates continuously to the measured pair**, and the in-window contest excludes the positive flank immediately (p ≥ +0.1 exits the galaxy band) while bounding the negative flank at −0.5, where the snap hides beyond the observed window. We state the result at its honest grade: an asymptotic uniqueness theorem plus an asymmetric measured bound, p_mix ∈ [−0.5, +0.1), with full closure awaiting deeper-window tails or the c₄ rung. [`calcs/stage7f_mixmean.py`]

## 3. The screening index

Before testing coefficients we ask how sharply the transition is screened, since solar-system bounds constrain soft tails. We fit the one-parameter screening family

> ν_p(y) = (1 − e^(−y^p))^(−1/(2p)),

whose p = ½ member is exactly the RAR function, to 2,700 RAR points from 153 SPARC galaxies (inclination > 30°, quality ≤ 2, δV/V < 0.10; M/L_disk = 0.5, M/L_bulge = 0.7 baseline).

With fixed mass-to-light ratios the galaxy-bootstrap gives p = 0.443 (+0.063/−0.050). Marginalizing the disk M/L (globally, f_d = 1.22 ± 0.10, i.e. M/L_disk ≈ 0.61 — consistent with the independent fits of §4 and with stellar-population values) and propagating a 0.1-dex per-galaxy M/L scatter widens and shifts this to our primary value:

> **p = 0.578 (+0.121/−0.115)** (M/L-marginalized),

with a₀ = (1.05 ± 0.10)×10⁻¹⁰ m/s². The RAR/occupation value p = ½ lies 0.7σ inside the band. The Cassini/ephemeris requirement (anomalous Saturn acceleration < 2×10⁻¹⁴ m/s² under this family's direct screening term) demands p > 0.234: comfortably satisfied at the marginalized 16th percentile (0.462). The honest summary is that p is a *soft* discriminator — M/L-sensitive at the ±0.1 level — while the screening floor is robust. It stopped being soft when the hierarchical machinery arrived: with per-galaxy disk-M/L profiled (the §4.3–§4.4 treatment), the same family's joint-objective preference sharpens to p̂ ≈ 0.65 at Δ(−2lnL) = 56 over p = ½ — the two treatments bracket p ≈ 0.58–0.65, and §4.4 shows this tail dial is what most of the hierarchical function contest is actually measuring. [Scripts: `calcs/sparc_rar_fit.py`, `calcs/stage4h_p_ml.py`, `calcs/stage5g_tailtest.py`]

## 4. Coefficient tests: SPARC and the lensing RAR

### 4.1 The truncated estimator is power-limited (an honest null)

A direct fit of (a₀, c₁, c₂) to the deep-MOND expansion on SPARC is ill-conditioned (x and x² are ~99% collinear over any usable window). A two-step asymptotic-matching estimator — a₀ from ultra-deep points, then weighted least squares for (c₁, c₂) — is honest but power-limited: σ(c₁) ≈ 0.4–0.6 with window-dependent sign flips. SPARC cannot measure c₁ through a truncated series; we quote this null in full because unacknowledged versions of it could masquerade as coefficient measurements. [`calcs/stage4a_nlo_test.py`]

### 4.2 The branch test: full functions, two likelihoods

The truncation-free instrument compares complete ν functions with (a₀, f_ML) free per family. Under the raw-χ² likelihood of the original run, the c₁ = ½ branch (occupation and simple, statistically inseparable here) defeats the c₁ = 0 branch (standard-μ) in 198–200 of 200 galaxy bootstraps across all acceleration windows, with the standard family additionally dragging M/L to unphysical values. Under a scatter-marginalized likelihood (per-family intrinsic scatter s_int ≈ 0.12 dex profiled; the statistically honest treatment, since raw χ²/dof ≈ 57), the verdict deflates to a sign-robust strong lean: Δ(−2lnL) = 56 for the full range, branch preferred in 166/200 bootstraps, with the deep window alone inconclusive. **We report both treatments**; the branch conclusion stands as a robust lean rather than a kill, and the magnitude's likelihood-model dependence is itself a methodological finding — coefficient-level claims in this field inherit the noise model. Within the ½-branch (occupation 1/12 vs simple 1/8), neither treatment discriminates: the raw-χ² "simple lean" reverses under scatter marginalization into a small occupation lean carried by 3 of 153 galaxies — i.e., nothing. [`calcs/stage4b_branchcomp.py`, `calcs/stage4e_lensing_rar.py`, diagnostics in `calcs/stage4e_diag.py`]

### 4.3 The zero-point coefficient measured: ĉ₁ = 0.45

The branch contests of §4.2 ask which discrete family wins; the sharper question is what the data measure the coefficient to *be*. We promote c₁ to a continuous parameter through the one-parameter family ν_λ = (1−λ)·ν_standard + λ·ν_RAR, whose deep-MOND expansion has c₁ = λ/2 exactly (verified numerically to 2×10⁻³ before fitting; the slice's c₂ rides along and is reported at the fit). λ = 0 is the c₁ = 0 branch, λ = 1 the occupation law, and the grid extends to λ = 1.5 so the maximum may fall beyond either. Under the scatter-marginalized joint SPARC+lensing objective (§4.2's likelihood with §4.5's lensing anchor and its calibration nuisance), the profile is parabolic and interior:

> **ĉ₁ = 0.450, profile-likelihood interval 0.385–0.519; galaxy-bootstrap 0.427 (+0.290/−0.246).**
> The zero-point prediction c₁ = ½ lies 0.7σ inside. The c₁ = 0 branch is excluded at Δ(−2lnL) = 56.3 — 7.5σ as a one-parameter profile, 95.5% of 200 galaxy bootstraps (≈1.7σ one-sided). The self-consistent-quantum ¼ cell is disfavored at Δ(−2lnL) = 9.7 (3.1σ profile; 73.5% of bootstraps sit above ¼). At the fit, c₂(λ̂) = 0.100 — between the occupation law's 1/12 and simple's 1/8, which rung 2 cannot yet split (§4.5).

The hierarchical version was then run — per-galaxy disk-M/L offsets (0.1-dex lognormal prior) profiled at every λ node (`calcs/stage4z_hierc1.py`) — and it relocates the profile: **λ̂ = 0.52, ĉ₁ = 0.258 (interval 0.208–0.309)**. This is not machinery bias — the injection gate recovers a λ = 1 truth at 1.03 *with* offsets active, and both endpoint regressions reproduce the flat-M/L fits exactly — but real per-galaxy structure doing work in the global fit: the profile estimator is nuisance-model-dependent at the ±0.2 level, and we quote that spread as part of the measurement. The bootstraps, by contrast, agree across treatments (0.427 +0.290/−0.246 flat; 0.377 +0.234/−0.311 hierarchical) — population variance dominates both. The fair statement of the galaxy-side dial: **ĉ₁ ∈ 0.26–0.45 by profile (treatment-spanning), ≈ 0.4 ± 0.3 by bootstrap; c₁ = 0 excluded in every treatment (Δ(−2lnL) = 29–56; ≥ 89% of bootstraps); and the second digit — ¼ versus ½ — is open on the galaxy side**, the hierarchical profile peaking at the ¼ value and the flat-M/L profile at ½'s doorstep, while the binaries' fenced-model 0.37–0.50 indication was erased by the landed-posterior re-run (§6.4). The ¼-*function* contest of §4.4 is flat-M/L-conditional accordingly.

Two disclosures accompany the number. First, the gap between profile and bootstrap significances is galaxy-population variance — the same lesson as the wide-binary realization systematic (§6.5): curvature-based errors understate coefficient uncertainties in this field, and we quote the bootstrap as primary. Second, the raw-χ² objective *fails structurally* on a continuous family: λ̂ runs to the grid edge (Δχ² in the thousands; the bootstrap pins at the parameter bound), because at χ²/dof ≈ 57 unmodeled intrinsic scatter rewards unbounded boost-shape bending. The §4.2 deflation was a warning; this is the demonstration — raw χ² can rank discrete families but cannot measure a coefficient, and coefficient-level claims built on it would inherit the runaway. Endpoint regression gates against the stored §4.2/§4.4 fits pass exactly. (Nearest art, verified at the pre-circulation scout: continuous interpolating-function fits exist — Desmond et al. 2024 dial their δ-, γ-, and n-families in transition *sharpness*, as does our §3 screening index — but a fit of the deep-expansion coefficient itself appears absent from the literature.) [`calcs/stage4s_c1fit.py`]

### 4.4 The quarter-branch (bootstrap bath): disfavored flat, preferred hierarchically

The self-consistent quantum cell of §2.3 (c₁ = ¼, c₂ = 7/96), solved per point by seeded Newton iteration and validated against its analytic series, was entered into the same contests: dead-grade under raw χ² (beats the occupation law in 4/200, simple in 7/200) and disfavored under the flat-M/L marginalized likelihood (+27/+9 in −2lnL; 62–89/200). Under flat M/L the likelihood along the c₁ axis peaks at ½ (0 dead, ¼ disfavored, ½ preferred, 1 excluded by screening). [`calcs/stage4f_bathmatrix.py`]

The §4.3 hierarchical relocation demanded the same treatment here, and it **reorders the matrix**. With per-galaxy disk-M/L profiled (0.1-dex prior, adaptive coordinate descent run to convergence against the §4.3 warm-chained benchmark; nesting and endpoint gates pass), the converged ladder on the joint SPARC+lensing objective reads: **bootstrap −10510.6 < occupation −10435.0 < standard −10424.5 < simple −10336.3** — the self-consistent quantum cell wins by 75.6 over the occupation law and 174 over simple, and the flat-M/L "simple lean" inverts outright (simple falls last, 88 behind even the dead branch). Three truth-calibrated injections certify the instrument: synthetic data at each truth are recovered by the correct function at margins of +38 (occupation truth, over bootstrap), −98 (bootstrap truth, over occupation), and +8/+92 (simple truth) — **the observed real-data gap (−75.6) sits near the bootstrap-truth calibration and far from the occupation-truth one.** A 50-rep galaxy bootstrap gives −69 ± 65 with the bootstrap function ahead in 43/50 (86%): a strong lean, short of decisive, population variance dominating as everywhere in this program. The coherence is the point: the continuous λ-profile (§4.3) and the exact-function contest are independent instruments, and both relocate to the ¼ cell when per-galaxy M/L is real. Physically the ¼ cell is the reading in which the mode frequency is set by the *total* acceleration — ω = g_obs/c, the occupation ratio x = T_U(g_obs)/T_dS of §2.4 — i.e., the bath the system actually sits in, not the bath its Newtonian source would prepare. The implicit function ν = 1 + n_BE(ν y) appears nowhere in the interpolating-function literature (scout-level negative, as is the fact that the simple function solves its classical limit ν = 1 + 1/(ν y)); we treat both as observations of this program pending a deeper pass. The binary cross-check of this function — its own EFE tables through the v7 likelihood, since the §6.4 λ-mixture carries standard-μ's shape at low λ and cannot stand in for it — is reported in §6.4: **the binaries veto it**, at +17–24 in lnL behind the ½-branch functions with the amplitude riding its grid edge — a shape rejection of the ¼ cell's weak transition (ν(1) = 1.35 against the occupation law's 1.58) by the regime that measures the transition directly. [`calcs/stage5c_hierbath.py`, `calcs/stage5d_hierbath_conv.py`, `calcs/stage5f_bootbin.py`]

The apparent galaxy–binary contradiction then *decomposes*, and the decomposition is the finding. The converged hierarchical ladder — bootstrap, occupation, standard, simple — is monotone in Newtonian-tail sharpness (e^−y, e^−√y, ~y⁻², ~y⁻¹), suggesting the hierarchical galaxies are voting on the *screening tail*, not the deep coefficient. The §3 screening family provides the clean probe, since its p = ½ member *is* the occupation law: run through the identical converged hierarchical machinery (endpoint gate exact), it prefers **p̂ ≈ 0.65 at Δ(−2lnL) = 56 over p = ½ — three-quarters of the bootstrap function's gain, from the tail dial alone, at a ½-branch-grade transition (ν(1) = 1.42)**. Read jointly with the binary veto: the data's hierarchical message is *sharper screening than the pure occupation tail* (consistent with §3's p = 0.578 ± 0.12 and with the Cassini floor's direction), while the ¼-versus-½ deep digit remains open — the ¼ function's residual, non-tail gain (~19) is precisely the part its binary-vetoed transition carries. The synthesis is constructive: the data triangulate a *screening-sharpened occupation law* — ½-branch transition (binary-held), p ≈ 0.6–0.65 tail (hierarchical-galaxy-held), thermal deep structure — and §6.4 reports that exactly this function passes the binary likelihood where the ¼ cell failed it. [`calcs/stage5g_tailtest.py`]

The triangulation admits a *derived* candidate rather than a fitted one. The bath matrix of §2.3 offers two frequency prescriptions; their geometric mean — ω = √(ω_source·ω_total), equivalently an occupation argument x² = [T_U(g_N)/T_dS]·[T_U(g_obs)/T_dS] — defines a third implicit function, **ν = 1 + n_BE(y^(3/4)√ν)**, with no free parameters and the derived series ν√y = 1 + x/3 + x²/12: a zero-point coefficient c₁ = ⅓ (inside both systems' measured bands), the occupation law's Bernoulli c₂ = 1/12 *surviving the mean*, a ½-branch-grade transition (ν(1) = 1.433), and a p ≡ ¾ Newtonian tail — each spec item inherited, none tuned. On the galaxies it performs as a correct construction should: statistically indistinguishable from the occupation law under flat M/L (Δ = 2.0), and *leading the converged hierarchical ladder outright* at −10519.8 — 9 past the ¼-function, 28 past the fitted-p optimum, 85 past the occupation law (the 9 is within population variance; the 85 is the same grade as the flip itself). Its binary test is reported in §6.4. We stress the epistemic status: this is a function-level candidate whose bath reading is stated, not a microphysical derivation of *why* the frequency should be the geometric mean — that question is the §2.4 construction problem in sharpened form. (Priority checked at the pre-print scout plus a direct read of the nearest candidate: no implicit interpolating function, no geometric-mean occupation argument, and no deep coefficient ⅓ appear in the literature; Pazy & Argaman's "general expression" is Fermi–Dirac with deep corrections their own text gives as O(e^(−1/x)) — no polynomial NLO at all.) [`calcs/stage5j_gmboot.py`]

The hierarchical tail vote itself was then given the last control we can build from measured priors: the per-galaxy *vertical* channel of §4.6 (distance and inclination offsets, σ_v from the SPARC table's own errors, median 0.097 dex), profiled alongside disk-M/L in the converged machinery. Three things happen. **First, the a₀ anomaly resolves**: every hierarchical fit had been trading a₀ down (0.84–0.89 ×10⁻¹⁰) against f_ML up; with the vertical channel active all four functions return to a₀ = 1.04–1.13 ×10⁻¹⁰ — on the horizon value — with f_ML at 1.2–1.4 and the channel spending exactly its prior budget (std(δv) = 0.106 vs prior median 0.097). **Second, the tail vote halves but survives**: Δ(geometric-mean − occupation) = −40.6 (from −84.8), Δ(ν_p(0.65)) = −31.7 (from −56.4). **Third, the ¼-function collapses**: −8.6 (from −75.6) — its galaxy case was mostly vertical-degenerate, which closes the cell coherently with its binary veto (§6.4). What remains standing is a factor-two-reduced, still-substantial hierarchical preference for the two sharp-tail functions the binaries accept, now at horizon-consistent a₀ — carried with the §4.6 identifiability caveat (the vertical channel is the demonstrated absorber at SPARC depth, and external distance anchors remain the unlock). A 40-rep paired galaxy bootstrap grades the surviving lead honestly: **Δ(geometric-mean − occupation) = −29 ± 53 with the geometric mean ahead in 29/40 (72%)** — a lean, not a detection, and we quote it as such. The family-level statement of §2.4 supersedes the function-by-function ladder: profiled continuously, the mixing exponent is interior at β̂ = 0.45–0.64 across hierarchical treatments, spanning the symmetric point. [`calcs/stage5m_hierv.py`, `calcs/stage5n_dvboot.py`, `calcs/stage5p_betafam.py`]

The profile's *anatomy* was then measured. At each β's joint optimum, the Δ(−2lnL) against β = 0 decomposes exactly into fixed acceleration bins (pure evaluation at the fitted nuisances; identity gate at 10⁻⁶; the full-sample profile itself reproduces the family fits to ±0.00 at every node), and the galaxy preference turns out to be a **compromise between opposing arms**. At β = ½: the ultra-deep bins vote *against* the mixture (+9.3 — the deep zero point wants the occupation value c₁ = ½, and the penalty grows monotonically in β), the transition bin is mildly for (−5.5) but flips against at β = 1 (the transition defends its 1.58 boost), and the vote is carried by the Newtonian side (−61 for y > 1: the screening tail, this section's own decomposition again). A free fit of the Newtonian arm alone returns β̂ = 0.76, interior; the deep arm alone is ridge-degenerate without a Newtonian anchor (its global M/L runs to 1.94) and is flagged rather than interpreted. **So β is not one number even within SPARC: the effective mixing exponent reads β → 0 in the deep limit and β ≈ ½–¾ in the screening tail — the same pattern as the galaxy–binary split (§6.4), now measured inside a single dataset.** The structural reading is sharper still: the family locks c₁·p_tail = ¼ exactly, while the joint data ask for c₁ ≈ 0.4–0.5 (the deep arm here; §4.3; the binaries, §6.4) *and* p ≈ 0.65–0.75 (§3; the tail vote here) — a product near 0.3 that no member can deliver. The data strain against the family's own lock, and the construction problem of §2.4 acquires its most concrete specification yet: **a deep zero point at the occupation value with decoupled, sharpened screening** — the mixing family is the instrument that made that decoupling measurable, and the measurement is that the two ends do not move together. [`calcs/stage5t_betasplit.py`]

The decoupled function the anatomy asks for was then built — §2.4's spontaneous-fraction derivation, c₁ = ½ and p = ¾ fixed exactly before any fit — and entered in the identical converged machinery (occupation-law endpoint regressions reproduce to ±0.00 in both treatments). It **leads the ladder outright in all four contests**: Δ(−2lnL) versus the occupation law of −101.3 (F1) and −107.2 (F2) under per-galaxy M/L (geometric mean: −84.8), and −44.3/−51.9 with the vertical channel active (geometric mean: −42.7) — the first function to hold its lead through the vertical control undiminished, at horizon-adjacent a₀ (0.98–1.0×10⁻¹⁰). The prediction pattern of the decomposition landed: the running function keeps the tail's gain and recovers the ultra-deep bins the fixed mixtures paid for. Its binary and Saturn legs are §6.4 and §8.1. [`calcs/stage5v_rungal.py`]

The two-leg refinement (§2.4) extends the ladder monotonically: **F3 −111.4 / F4 −108.7 under per-galaxy M/L, and F4 −64.2 with the vertical channel — the widest controlled-treatment margin in the function ledger** — at horizon-adjacent a₀ (0.97×10⁻¹⁰), with both Bernoulli rungs exact by construction. The lead was then graded on the 5N yardstick: a 40-rep paired galaxy bootstrap gives **F4 − BE = −57.4 ± 38.3 with F4 ahead in 37/40 (92.5%)** — against the geometric mean's earlier −29 ± 53 at 29/40 — the highest bootstrap fraction any function lead has reached in this ledger, still formally a lean (~1.5σ) by population variance, and quoted as such. [`calcs/stage6a_twogal.py`, `calcs/stage6c_f4boot.py`]

### 4.5 The lensing RAR: an anchor, not a discriminator — and rung 2's honest power

To reach below SPARC we use the KiDS-1000 weak-lensing RAR in both public reductions: the exact-deprojection table of Mistele et al. (2024; 15 stacked points for isolated lenses, log g_bar ∈ [−14.86, −11.41], extracted verbatim from the published source) as primary, and the original Brouwer et al. (2021) ESD release with full covariance as cross-check. The Newtonian control fails catastrophically on the 15 lensing points alone (Δχ² = +2777 and +1659 in the two reductions) — the anomaly persists two decades below SPARC with no dark-matter component permitted. The lensing data are family-agnostic on their own (all three ν functions fit equally); jointly with SPARC they anchor a₀ = (1.00 ± 0.09)×10⁻¹⁰ (marginalized likelihood; 1.21×10⁻¹⁰ raw — the ~20% likelihood-model sensitivity is disclosed, and the values bracket both our §3 measurement and H₀/2π = 1.08×10⁻¹⁰).

For rung 2 the verdict is a quantified null: even lensing-anchored, the c₂ estimator resolves 1/12-vs-1/8 at **0.09–0.10σ**. The wall is the surveys' 0.2-dex stellar-mass calibration (modeled here as a global log-g_bar offset nuisance with the published prior); the rung needs ~0.02 dex, or the Gaia DR4 distribution-level test. The ladder's next rung is measurable in principle, not yet in practice — and we prefer stating that to pretending otherwise. [`calcs/stage4e_lensing_rar.py`; data provenance in `data/lensing_rar/`]

### 4.6 The bath's second moment: a first fluctuation constraint

**End state first** (the chronological derivation follows; an external review flagged this section as easy to over-read, and it was right). What stands: (i) the scatter *shape* — the RAR's intrinsic scatter is acceleration-dependent, declining from the deep regime to the Newtonian one, and the deep-end trend survives its executed environmental control at published field amplitudes (β̂ = 0.044, the collinear amplitude excluded at +168); (ii) a transition-region excess at x ≈ 1 that survives every per-galaxy marginalization and is identified below as **inner-disk (R < 1.5 R_d) structure** — its fitted amplitude on outer-disk points is exactly zero — astrophysics at the RAR knee, not the relation; (iii) a genuinely point-level (offset-subtracted) scatter floor of 0.04–0.07 dex, convergent with Desmond's hierarchical 0.034. What is retired: the **mode-count constraint (N ~ 20–60) is not identifiable at SPARC depth** — per-galaxy vertical (distance/inclination) freedom absorbs the thermal amplitude, and the calibrated injection gate proves the degeneracy rather than assumes it — and the particle-versus-wave fluctuation exponent γ is **not measurable on SPARC** at any grade tried (within-curve residual coherence ρ ≈ 0.87 caps every per-point likelihood statement; the P6 prediction is fully out-of-sample). Scatter-channel claims in this paper are accordingly restricted to the outer-disk/anchored grade; the mode-count numbers appear below as the history of how the boundary was found.

A thermal occupation fluctuates: Var(n) = n(n+1) for a Bose mode. The oscillator reading therefore makes a *second-moment* prediction — the RAR's intrinsic scatter should carry the shape σ_rel(x) = √(n/(n+1))/√N, rising monotonically into the deep-MOND regime and dying exponentially in the Newtonian one, with amplitude set by the effective number N of modes averaged per measurement. We fit four scatter models jointly with (a₀, f_ML) on SPARC, per-point reported errors always included: constant; pure oscillator; oscillator plus constant floor; and six free acceleration-sextile bins. Findings: (i) the scatter is *not* constant — the free fit beats a constant by Δ(−2lnL) = 43 for five parameters, declining from 0.144 dex in the deepest sextile to 0.107 in the most Newtonian; (ii) a floorless oscillator is excluded outright (+382 — the data demand a scatter floor everywhere); (iii) the one-parameter oscillator-plus-floor term captures most of the trend (−25 for one parameter), with **N̂ ≈ 21 effective modes over a 0.101-dex floor** (a number retired by the identifiability boundary below) — and even attributing the entire deepest-bin scatter to the bath forces N ≥ 7; (iv) a residual bump at x ≈ 1 keeps the free shape ahead (Δ = 18 for four parameters), sitting precisely where per-galaxy mass-to-light scatter projects maximally onto g_obs — flagged as the probable mundane contributor, making the M/L-marginalized second-moment fit the required next instrument before any bath claim. We state the result as a constraint, not a detection: *if* the excess low-acceleration scatter is thermal, the bath averages ~20 modes per point; floorless baths of any N are excluded by the scatter's Newtonian-side persistence. Per-point independence is assumed — a galaxy-level correlated draw (modes shared along a rotation curve) would loosen N and is the natural refinement. Both ν-families give identical results; the injection gate recovers a synthetic N = 30 at 30.4. Prior art, scout-verified: Lelli et al. (2017) report the RAR's residuals uncorrelated with galaxy properties and the global scatter consistent with observational uncertainties; Desmond (2023), with per-galaxy nuisances hierarchically marginalized, finds the intrinsic scatter at 0.034 ± 0.002 dex — a number which, taken as the ceiling on the thermal term, would sharpen the mode bound to N ≳ 10². No acceleration-binned intrinsic-scatter measurement, and no fluctuation-statistics reading of the scatter, appears to have been published. A second mundane contributor is stated for completeness: distance and inclination errors enter both RAR axes covariantly and project onto the residual more strongly where the relation flattens — the marginalized refit must carry both channels. [`calcs/stage4t_bathnoise.py`]

The first rung of that refit was then executed: a hierarchical fit in which every galaxy receives a free disk-M/L offset (0.1-dex lognormal prior) profiled jointly with the globals, the scatter models recompeting on what remains. **The thermal-direction trend survives disk-M/L marginalization**: oscillator-plus-floor beats constant by Δ(−2lnL) = 23 for one parameter on the reduced floor (0.059 dex), with N̂ rising to ≈ 63 — the unmarginalized N ≈ 21 was floor-limited, and the constraint reads **N ~ 20–60, nuisance-depth-dependent**. No galaxy-level draw signature appears (the fitted offsets are uncorrelated with each galaxy's acceleration depth, r = −0.02). [`calcs/stage4u_mlmarg.py`]

The full hierarchy was then run — bulge M/L added for the 31 bulge galaxies, plus a per-galaxy *vertical* offset whose prior is measured, not chosen: the SPARC table's own distance and inclination errors, both of which move g_obs at fixed g_bar under our loader (median prior 0.097 dex). The verdict is an honest identifiability boundary, not a survival and not a refutation. With vertical freedom active, the oscillator term's amplitude collapses (N̂ → 189, i.e. compatible with zero) and oscillator-plus-floor loses to constant by +24 — but the calibrated injection gate **fails at slope 0.23 against a shrinkage-expected 0.93**: the machinery demonstrably cannot separate per-galaxy M/L from per-galaxy vertical offsets on this data (a disk-M/L shift *is* mostly a vertical shift for a disk-dominated galaxy in the low-acceleration regime), and any between-galaxy component of a real x-shaped signal is absorbed along with it. We therefore report the channel as **not identifiable at SPARC depth**: the 4T/4U trend is real; whether it is thermal or per-galaxy distance/inclination structure requires external distance anchors (Cepheid/TRGB-grade, the inputs of Desmond 2023's hierarchy) or an independent dataset. One feature *does* survive every marginalization tried, including bulge M/L and vertical freedom: **a scatter excess at the transition, x ≈ 1** (free-bin 0.054 dex against 0.026–0.035 dex in the adjacent bins). Our first reading attributed it to environmental external-field scatter; we correct that before testing it — for field galaxies the environmental field is e ~ 0.01–0.05 a₀, whose projection onto g_obs peaks in the *deepest* bins, not at the transition. A per-galaxy template contest was then built (screening-index scatter, whose ∂ν/∂p peaks at the transition, against the deep-weighted environmental template from the Chae & Milgrom formula) — and disqualified itself by its own gate: injected per-galaxy sharpness scatter produces *no* transition bump, because within any one galaxy the template is nearly constant in x and the vertical channel absorbs it. That failure is itself the sharpest characterization to date: **the bump survives every per-galaxy channel — vertical, disk and bulge M/L, sharpness, environment — and is therefore point-level structure localized at the transition within rotation curves**, which no galaxy-wide nuisance can produce. On the distance-anchored subsample (43 galaxies, vertical priors halved) the thermal term is not *required* (Δ = 0.02, power-limited; injection slope improves to 0.42 without reaching identifiability), while the environmental template *does* absorb the deep bins (0.063 → 0.045 dex) — keeping environmental-field scatter alive as the thermal term's rival for the monotone trend, a rivalry only per-galaxy environment estimates can settle. [`calcs/stage4w_fullhier.py`, `calcs/stage4y_anchored.py`, `calcs/stage5a_bumpid.py`]

That settlement was then executed with the real fields. Chae et al. (2021) publish per-galaxy environmental Newtonian field strengths (log e_N, from the baryonic large-scale structure, "max clustering" and "no clustering" cases) for 109 SPARC galaxies; 94 fall in our sample (52% of our deep-bin points, median e_N = 0.005). We impose their exact one-dimensional QUMOND ratio (their Eq. 2, gated against its analytic limits) as a suppression template with one global amplitude β times the published per-galaxy pattern, hierarchical disk-M/L active, and every fit run to convergence under nesting-inequality gates (a fixed-depth first pass produced up to 80 lnL of coordinate-descent slop and was discarded — the convergence trap is documented in the notebook because it silently manufactures or destroys exactly the credits at issue here). The instrument is calibrated: injected β = 3 is recovered at 2.97, injected β = 0 at 0.00. On the real data: **β̂ = 0.044 with a Δ(−2lnL) = 1 upper bound of ≈ 0.02–0.05; the face-value collinear max-clustering amplitude (β = 1) is excluded at Δ(−2lnL) = +168**; the small credit the term does take (−7.2) sits inside the distribution of 8 scrambled-pattern controls (up to −10.1) — generic deep-end freedom, not the environmental pattern. The model-light channel agrees: the per-galaxy deep-bin residuals do not correlate with log e_N (Spearman −0.165, p = 0.20; depth-partialed −0.12, p = 0.34). The x ≈ 1 bump is untouched, as an external-field term must leave it (the EFE projects onto the deepest bins, the correction of our own first reading above). Caveats carried: the collinear geometry is maximal (orientation-averaging softens the β = 1 exclusion to roughly β_eff = 0.5, still +55); the published e_N carry ±0.29 dex, which attenuates β̂ but cannot manufacture a +168 rejection; and this is the point-level RAR at SPARC's measured distances — it neither reproduces nor refutes the within-galaxy outer-slope analyses of Chae et al. (2020, 2021), which fit per-galaxy amplitudes with distance and inclination free (a channel Paranjape & Sheth (2021) argue is confounded by clustering–property correlations, and in which Desmond's (2023) hierarchical treatment finds the external-field evidence weak). What it does settle is our own rivalry: **the deep-end scatter trend is not environmental at published field amplitudes — the thermal candidate survives its sharpest control, and the environmental external field is bounded to ≲ 2–5% of the collinear max-clustering amplitude in the deep-end RAR.** One honest heterogeneity note travels with this: on the SDSS-footprint subsample itself the oscillator term's own credit is −4.6 (against −22.8 on the full sample, with proportionate deep-point counts), so the thermal trend's amplitude is sample-dependent beyond point counting — consistent with its status as a constraint, not a detection. [`calcs/stage5b_envtest.py`, `calcs/stage5e_envtest_conv.py`]

The second moment's *statistics* were then given their sharpest in-sample contest (pre-registered as P6 of the program's signed prediction ledger, [PREDICTIONS.md](PREDICTIONS.md), committed before execution). Einstein's 1909 fluctuation decomposition makes the quantum-versus-classical question quantitative with zero new shape freedom: a Bose occupation fluctuates with Var(n) = n̄ + n̄² — particle term plus wave term — which through the mean law forces the intrinsic RAR variance to a pure Boltzmann factor, s² ∝ e^(−x); a classical continuous-field bath (including the classical self-consistent bath behind the simple function) retains only the wave term, s² ∝ e^(−2x); a purely corpuscular bath (Var = n̄) gives e^(−x)(1 − e^(−x)), dying in the deep limit. All rivals carry the same parameter count (amplitude plus floor), a₀ locked to the mean fit, with a free-exponent family s² ∝ e^(−γx) as the measuring instrument. Results at the per-point grade: **the corpuscular bath is excluded** (its best fit collapses onto the constant floor, Δ(−2lnL) = +25 behind either rival — deep-persistent scatter is incompatible with a deep-dying fluctuation law); **the quantum and classical laws are statistically degenerate** (0.43 apart against a pre-registered bar of 9); and the free exponent does not localize — the profile is bimodal with both shallow and steep edges preferred over the middle, the entire axis spanning ≤ 4.4 units. The instrument itself is validated: paired injections of quantum and classical truth are recovered at γ̂ = 1.03 and 2.05 respectively, so the non-verdict is misspecification in the data, not power. The structural reason is identified: the two laws differ maximally over x ≈ 0.4–1.2, and the transition bump — the one scatter feature that survives every marginalization above — sits at x ≈ 0.8–1.4. **The bump occupies the discriminant window.** The prediction stays live and pre-registered in both directions: γ = 1 confirmed would be an in-sample uniquely-quantum signature (the particle term, unavailable to any classical bath — the same argument by which Einstein first inferred light quanta from fluctuation statistics); γ = 2 confirmed would strike the quantum-statistical reading program-wide. The named unlock is the bump's point-level source, a hierarchically hardened γ instrument on distance-anchored data, or next-generation samples. [`calcs/stage7a_einstein.py`]

The unlock was then attempted, and the hunt caught the bump. A pre-registered discrimination matrix (composition / within-curve coherence / geometry, all evaluated at fixed x so the mechanical correlations freeze out) returned two findings. First, a methodological number that travels with every point-level scatter statement in this paper: adjacent-point RAR residuals within a rotation curve correlate at **ρ ≈ 0.87** (permutation null ±0.10) in and out of the transition window alike — smooth per-galaxy radial misfit dominates the raw scatter budget, so all per-point −2lnL gaps are nominally calibrated only, and the genuinely point-level (offset-subtracted) scatter floor is 0.04–0.07 dex, convergent with Desmond's hierarchical 0.034. Second, at fixed x the scatter is organized by disk radius, not by acceleration: points inside 1.5 disk scale lengths carry ~2.4× the variance of outer points in every acceleration slice, and the transition window happens to sample 49% inner-disk points against 16% in the deep control. The confirmatory stage then made the identification: **on outer-disk points the bump's fitted amplitude is exactly zero** — the bump is an inner-disk phenomenon (the radius-mix shift accounts for two-thirds of the window excess; the remainder is x-dependence of the inner excess itself), with beam smearing, bars, and spiral streaming (10–40 km/s at these radii in the literature) the natural driver class — inner-disk astrophysics, not the relation. The cost of the cleaning is stated with equal honesty: with the inner term modeled (one parameter worth 83 units of −2lnL), the γ profile de-bimodalizes to an interior minimum, but the calibrated injection gate fails on the informed design, the clean-subset variant leans the opposite way, and a thinning control erases the residual difference — **the particle-term exponent is not measurable on SPARC at any grade tried**, and the prediction moves fully out-of-sample with both kill directions still pre-committed. Neither confirmed nor struck: untested, with the instrument ready. [`calcs/stage7b_bumphunt.py`, `calcs/stage7c_gammaclean.py`]

## 5. Wide binaries: data, statistic, and model

### 5.1 Sample and observables

From the El-Badry, Rix & Heintz (2021) Gaia EDR3 catalog we select pairs with R_chance < 0.01, both parallaxes > 5 mas at S/N > 20, parallax consistency within 3σ, projected separation 200 AU < s < 50 kAU, both components on the photometric main sequence (2.6 < M_G < 14.2), and per-pair velocity precision σ_v < 0.03 km/s: **14,071 pairs**. Component masses come from an M_G–mass relation anchored to Pecaut & Mamajek (2013); the mass error is *measured* in §5.3. Two observables are formed per pair:

- **ṽ = Δv_sky / v_c(s)**, the sky-projected relative velocity in units of the circular velocity at the projected separation (v_c = √(G·M_tot/s)); bound Newtonian pairs satisfy ṽ ≤ √2 identically;
- **γ ∈ [0°, 90°]**, the folded angle between the separation and relative-velocity vectors on the sky (the v-r angle of Tokovinin 1998 and Hwang et al. 2022) — constructed from *directions only*, hence immune to the mass normalization by construction.

The joint statistic is a multinomial likelihood over a 20 (log ṽ, 0.02–6.0) × 6 (γ) grid in four separation bins (0.2–2, 2–6, 6–20, 20–50 kAU), with per-pair measurement noise drawn from the data and convolved into every model template. We emphasize this last point: models are compared to data *after* forward-modeling the noise, which is the treatment whose absence in one published analysis was criticized by Hernandez, Chae & Aguayo-Ortiz (2024) and which our ablations (§7.3) cannot therefore reproduce.

### 5.2 Forward model

Model populations (10⁶ binaries per evaluation) are generated by a GPU orbit integrator under either Newtonian dynamics or a boosted law g = g_N·B(y), with B interpolated from external-field-effect (EFE) tables produced by an axisymmetric QUMOND (Milgrom 2010) solver (validated: spherical identity to 0.01%; tracks the Chae & Milgrom (2022) fitting formula; and cross-validated against the published solar quadrupole in §8). The boost strength is parameterized as B_α = 1 + α(B − 1): α = 0 is Newton, α = 1 is the parameter-free galactic calibration.

The nuisance model, developed across seven fit generations (v1→v7 in the notebook, each falsifying its predecessor's hypothesis), comprises: an eccentricity mixture — a separation-interpolated power family plus a near-parabolic component of weight w_rad with e ∈ [0.9, 0.995] (§7.1); an undetected-companion sector with orbital wobble drawn from the Raghavan et al. (2010) period–multiplicity statistics applied to both components, PM-suppression for long periods, hidden-mass corrections, and validity limits from resolution — bounded jointly by photometry and kinematics; chance-alignment and flyby contaminant templates with distinct 2D signatures, weighted by an s²-scaling and fenced by the fit; a proper-motion noise-scale factor; and the catalog's velocity-consistency acceptance, measured from the data first (the physical bound ceiling ṽ√s ≈ 2.2 sits well inside El-Badry's 5-M⊙ cut at ≈ 3.0) and applied to model and templates alike. The per-component noise convention (σ/√2) was audited mid-program and its correction logged.

### 5.3 Measured nuisance identities

Three decisions distinguish this pipeline from its published counterparts, and each rests on a measurement rather than a prior:

1. **The mass error is measured, not assumed.** The main-sequence photometric width gives σ_m(mass) = 0.024 — twelve times smaller than the ṽ-broadening it was once hypothesized to cause. The mass-error explanation of the anomaly's width is refuted at that factor.
2. **The companion fraction is fenced by photometry.** 12.3% of component stars are overluminous (unresolved light), and the kinematic fit caps the effective companion fraction at ~0.1; a dedicated physical-multiplicity fit (v5) found the companion model unable to reproduce the signal's shape (−420 in lnL against the alternative) with f_comp pinned at the fence. This is the parameter the Newton-favored analyses leave free (§7.3).
3. **The broadening has an identity.** The excess width of the ṽ distributions — which a phenomenological smear absorbs — was traced through mass errors (refuted), companions (refuted), and a circular sub-population (vetoed by the joint 2D fit: circularity *narrows* ṽ) to a ~20% near-parabolic orbit population demanded independently by the direction channel (§7.1).

## 6. Wide binaries: results

### 6.1 The model-independent anchor

The median-ṽ boost between the deep bins (6–30 kAU) and the Newtonian-regime anchor bins (0.2–2 kAU) is

> **1.086 (68% CI 1.064–1.110)**,

against a Newtonian expectation of 1.000. This statistic is immune to the population-realization systematic (below), robust to RUWE cuts, and stable under a 20× tightening of the chance-alignment threshold (max drift 0.033 with all subsample CIs overlapping; the noisy wide-bin median drifts *upward* under tightening — the opposite direction to a contamination bias). Hidden triples are capped at ≲5% of the effect by the tail shape. The §7.4 perspective audit corrects the anchor for the systemic-RV projection term — measured in our data at its predicted size and removed per pair — to **1.078 (CI 1.052–1.103)**; we quote both values, and note that the perspective-immune perpendicular-component version of the same statistic rises *more* steeply (1.151, CI 1.115–1.197). [`calcs/stage2c_vtilde_data.py`, `calcs/stage2d_ruwe_variant.py`, `calcs/stage4i_rchance.py`]

### 6.2 The external-field convention (a correction we own)

Early fits at an external field of 1.9 a₀ found α̂ ≈ 1.5 for both laws. The cause was a cross-formulation convention error of ours: 1.9 a₀ is the appropriate *total* dynamical field in AQUAL (the aquadratic-Lagrangian theory of Bekenstein & Milgrom 1984; the convention of Chae's self-consistent analysis), while our QUMOND solver's correct input is the *Newtonian* external field. Inverting the RAR at the solar circle (v_c = 233 ± 4 km/s, R₀ = 8.178 kpc) gives **g_N,ext = 1.15 ± 0.05 a₀**, confirmed against direct baryonic estimates. We flag the distinction explicitly because the two published wide-binary modeling traditions sit in different formulations, and an imported number can be silently wrong by 60% — as ours was, for five fit generations. (Chae's own statement of his detection is explicitly of this convention — "This systematic deviation agrees with the boost factor that the AQUAL theory predicts for kinematic accelerations in circular orbits under the Galactic external field," 2305.04613, abstract — verified verbatim at our full-text pass. The cross-formulation consistency of the corrected setup is independently validated by the solar-quadrupole comparison of §8.1.)

### 6.3 Final measurement

At the physical field, with all nuisances active and the acceptance applied, six population realizations and 1000 bootstrap replicates per law give:

> **α = 1.18 ± 0.11 (simple-ν) / 1.13 ± 0.13 (occupation law)** — both consistent with the parameter-free α = 1 (1.6σ / 1.0σ);
> **Newtonian dynamics excluded in all 2000 bootstrap contests** (Δ lnL = +110 ± 18 and +100 ± 15; minimum +53);
> realization scatter 0.045 / 0.039; α̂ interior in 1000/1000 and 998/1000 replicates; w_rad = 0.20 selected in every fit.

The residual ~10–18% upward lean is partially covered by the g_N,ext uncertainty and is disclosed, not interpreted. The two ν-families are not discriminated by the binaries (best-lnL differences −6…−13 across seeds, sign-stable but O(SE)). Model-class robustness: under every alternative boost structure tested in this program — five classes including the modified-inertia brackets of §8.2 — Newton loses by +71 to +122 in all 24 fits. The perspective-corrected re-run of this budget (§7.4; six paired seeds) moves α̂ by ≤ 0.03 with every conclusion intact — the numbers above stand. [`calcs/stage3o_v7fit.py`, `calcs/stage3p_v7budget.py`, `calcs/stage4r_corrected_refit.py`, summaries `data/stage3u_summary.txt`, `data/stage3v_boot.txt`, `data/stage4r_summary.txt`]

Two reviewer-specified robustness rows were then run (bars locked in advance; `calcs/stage7i_ablations.py`). **Freezing w_rad to the external Hwang-implied value 0.21** — replacing the one internally fitted eccentricity nuisance with the independent prior — moves α̂ by ≤ 0.01 and the Newton margin by ≤ 1.4: closed. **A strict multiplicity cut** (RUWE < 1.2 both components and no photometrically detectable overluminosity; 8,047 of 14,071 pairs) fired the pre-declared MATERIAL branch and is disclosed in full rather than absorbed: the *fit-level* amplitude collapses (Δα̂ = −0.74/−0.46; Newton margin at 16–24% of the pair-count-scaled expectation, six seeds) — while the *model-light* anchor-relative median boost **survives the identical cut and slightly strengthens** (raw 1.086 → 1.103; the mass-immune perpendicular statistic 1.151 → 1.185; bootstrap intervals overlapping the full-sample values). The investigation then ran two further bar-locked instruments, and both of our rescue hypotheses died on their own bars: the fixed noise inflation f_pm = 1.5 was the named suspect (calibrated on the full sample, plausibly over-broadening the cleaned one) — refuted, the strict sample *prefers* the full inflation in 4/4 rows with α̂ collapsed at every f_pm; the α ↔ w_rad degeneracy was the surviving flag (w_rad rides its grid edge in all six strict fits, and the thinned γ statistics weaken its usual protection) — refuted, freezing w_rad at the externally validated 0.21 leaves α̂ at 0.55/0.65 (Newton +10/+13, interior 4/4). **The pre-committed verdict is COMPANION-DIRECTION: at full-likelihood level the cleaned subsample genuinely prefers roughly half the boost amplitude, and no instrument correction we locked in advance explains it away.** The internal tension is stated rather than resolved: on the *same* cleaned pairs the model-light median boost is 1.10, the mass-immune perpendicular boost is 1.19, and the forbidden-band census survives at base-rate proportion (5/11 pairs at 57% retention) — the model-free statistics see the boost where the forward model's likelihood does not, and the two readings cannot both be right. This is the reviewers' companion concern given empirical teeth at fit level, and it goes, as pre-stated, to the joint completeness-marginalization stage — (f_comp, w_rad, f_pm, α) freed together under a photometric-completeness prior — as the decisive instrument. Until that stage reports, the fit-level results of this section carry the flag that their strict-subsample behavior is unexplained; the model-light statement stands on its own: **the median boost is stable at 1.09–1.10 under the strictest multiplicity cleaning this catalog supports.** [`calcs/stage7i_material.py`, `calcs/stage7i_fpm.py`, `calcs/stage7i_sw.py`, `data/stage7i_verdict.txt`]

**The decisive instrument then reported (bars and quadrants registered before launch; `calcs/stage7j_completeness.py`, `calcs/stage7j_marginal.py`, `calcs/stage7j_diag.py`).** Part A measured, for the first time in this program, the companion completeness of the photometric flag on the sample's own main-sequence locus: the −0.4 mag overluminosity flag came out catching **C = 0.41** of unresolved companions (false-flag rate 2.3%), and inverting through the blend probability, the sample's apparent host multiplicity came out at f_host = 0.42–0.57. **That absolute scale is retracted (Appendix A, item 18).** The reviewer-designed validation its injection gate lacked — the *over-attribution* test — was then run (`calcs/stage7j_paircorr.py`, bars pre-registered): wide-pair components are coeval and co-chemical, so metallicity, age, and extinction displace *both* components' overluminosity together, while an unresolved companion displaces one; the within-pair correlation of overluminosity residuals therefore reads the common-mode share directly. It is **ρ = +0.47** on the pre-registered core slice (+0.39 with the components' colors forced apart, killing the ridge confound; parallax-noise attenuation negligible; a flagged star's partner sits 0.11 mag bright of baseline) — roughly *half the single-star spread is common-mode astrophysics*, which the one-dimensional mixture had read as companions. So C = 0.41 is biased low and f_host biased high, in exactly the direction that manufactures a companion verdict — and the factor ~2–3 external tension against the published wide-binary subsystem rates (Tokovinin 2014: 7–10% per component; Tokovinin 2010: 12 ± 4%; scout-grade, primary verification pending) resolves in the literature's favor. The coherence passed its own audit: ρ is flat in angular separation from 2″ to 250″ (the BP/RP blending confound's signature is absent; the pre-stated blending-safe bar at θ ≥ 8″ reads +0.448, so the C-FAIL stands independent of blending) and rises with distance as an extinction/metallicity story predicts — it is astrophysical, and stands as a measurement of wide-pair photometric coherence in its own right. What survives of Part A: the completeness formalism, the photocenter physics, that coherence measurement, and the repair path — a joint two-dimensional (δ₁, δ₂) mixture in which common-mode, per-star noise, and companion shoulders (axis-aligned) separate geometrically. One design constraint is fixed in advance, from the review: the common-mode diagonal's slope must be set *per pair* from the two components' colors through a mass-dependent metallicity response (a tenth-dex offset moves a mid-M dwarf several times further than an F star, so an F+M pair's diagonal is far from unit slope while a twin's is near it), with only the amplitude and scatter left free — a single global slope would push the unequal-mass pairs' common-mode onto the companion axes and recreate the retracted over-attribution through the very door being closed. The honest status of the companion prior at these separations is *unmeasured* — no published subsystem rate exists at 0.2–50 kAU — so the repair is the first measurement, not a patch; until it reports, the literature-anchored prior (per-pair ≈ 0.16, from Tokovinin's per-component rates; conclusions insensitive across 0.16–0.22) is primary, per the pre-registered C-FAIL branch. Two model errors surfaced on the way and are recorded. The *as-published* companion amplitude law (photocenter wobble ∝ q/(1+q)) is internally inconsistent with the measured multiplicity — forcing it to the measured host rate costs −486…−1078 log-likelihood against its own low-multiplicity optimum, which is exactly why the earlier physical-multiplicity fit had capped f_comp at 0.1 — and the inconsistency traces to omitted photocenter cancellation: equal-luminosity companions produce *zero* astrometric wobble, so the correct amplitude is |q/(1+q) − ℓ/(1+ℓ)|. (Provenance, stated because it matters: the correction was registered *after* Part A's host inversion raised the amplitude question, and after an interim verdict under a misspecified prior — since quarantined on disk — had already shown the companion direction, but *before* the as-published conditional was explicitly read. The physics is sequence-independent; the adoption environment was not neutral; the as-published conditional verdict therefore travels alongside the corrected one everywhere.) With the corrected law, a wobble-amplitude nuisance κ_w, the noise grid extended off its edge, and the measured-multiplicity prior, the marginalized (f_comp, w_rad, f_pm, κ_w, α) posterior returned the pre-registered **COMPANION-WIN** at the measured prior — α_marg = 0.00 with the Newton margin at +0.0 on the injection-validated simple arm (both realizations; the BE arm reads identically but is quoted as pending, its own-truth recovery at 0.73 against the [0.85, 1.45] band); posterior at f_comp = 0.35; α = 1 disfavored by 38–87 in the marginal; *not* a noise-grid artifact (unchanged with f_pm restricted to the externally calibrated range, though the unrestricted marginal rides the extended grid's own edge at 82–99% posterior on f_pm = 2.4 — the error model lacks a variance channel, the §6.5-adjacent per-system scatter object, and carries that flag). **The primary statement, per the review round and Part A's retraction, is the α_marg(prior-anchor) curve with verdict labels as annotations — and the finely sampled curve is nearly flat: α_marg ≈ 0.2–0.3, Newton within +2…+4, under any smooth prior anchored anywhere in 0.06–0.30 (both widths tried), because the likelihood alone prefers f_comp ≈ 0.2 by +12…+28; the fenced +99–110 detection returns only behind a hard exclusion of f ≥ 0.2, which no measurement of the rate can justify.** The labels sit on this curve as annotations (COMPANION-WIN at the retracted measured prior; AMBIGUOUS at the literature anchoring: one realization interior at α_marg ≈ 0.4–0.5, one at zero; six-realization budget the decider), and the curve relocates the revival mechanism: what could materially move α is not the subsystem rate but the model's missing width channel — the same object as the winning cell's 60–116 misfit and the noise-grid edge — since removing the need for companion-supplied variance would relax the likelihood's f_comp ≈ 0.2 preference itself (Appendix A, item 17; the curve instrument is `stage7j_lowend.py`). The interior reading deserves its positive statement: at the literature anchor the fit does not sit "unresolved between Newton and MOND" — it sits at a *specific value, roughly half the galactic calibration*, which is what a split width budget looks like when four absorbers share one signal; that object, not a label, is what the direction-channel decomposition below is aimed at. The structural diagnosis behind the label's prior-dependence is stated as the section's methodological conclusion: excess ṽ width has four absorbers — the boost, near-parabolic orbits, hidden companions, noise inflation — competing for one width budget, and the verdict has tracked whichever absorber carries the tightest external constraint at the time; better priors on any one absorber relocate the degeneracy rather than break it. What breaks it is the direction channel: companions perturb velocity in directions uncorrelated with the separation vector (flattening the γ distribution), near-parabolic orbits concentrate it, noise broadens nearly isotropically, and the boost scales ṽ — so the decisive methodological test, queued ahead of further power runs, is the four-way contest with γ collapsed, quantifying how much of the (α, w_rad, f_comp, f_pm) separation the direction channel actually carries on this catalog. A power gate demonstrates sensitivity — an injected α = 1.18 signal with measured-rate companions is recovered at α_marg = 1.27 (ΔlnL = +48.8) through the same prior — but its scope at the verdict's firing is stated exactly: the injection ran on the *strict* selection under the *simple* truth only, so the strict verdict carries no formal weight (the gate's both-laws bar failed on a BE grid edge), the full-sample arms carried the verdict without own-truth injections, and the BE arm had never been asked to recover its own truth anywhere. The second review named this, we accepted its standard — any arm carrying a verdict needs its own passing injection, and a grid edge in a power gate disqualifies exactly as in a fit — and pre-registered per-arm full-sample and BE-truth injections plus a six-realization seed budget with standing rules committed in advance (if both full arms fail their own injections, the verdict downgrades to PROVISIONAL-INSTRUMENT and the credence move partially reverts). Their results and limits, stated exactly: the simple-full arm VALIDATED through the measured prior (own-truth 1.18 recovered at 1.48, ΔlnL +94.5); the BE-full arm FAILED (own-truth 1.13 recovered at 0.73, below band) and is quoted as pending; the +0.30/−0.40 own-truth offsets are *prior-free* (the prior-less profiles read 1.47 and 0.73, and re-marginalizing the same injected data under the literature prior moves neither) — they sit in the likelihood/realization layer at single-injection grade, with the multi-truth recovery map queued. Two scope facts follow the retraction of the measured prior: cube re-use is free on the *seed* axis only (the prior enters at marginalization), while power *arms* at a new anchor require newly injected companion populations — so **no arm is currently validated at the operative (literature) anchor**, and per the review's ordering the arm suite (including the BE α = 0.4 injection that separates an additive from a multiplicative recovery map) re-runs at whatever anchor the repaired completeness measurement lands, rather than at cells about to be vacated. One further validation limit is measured and stated for that suite: the injections are *more informative about f_comp than the sky* — the injected likelihood profiles cliff by hundreds of log-likelihood around their truth cell where the real profiles vary by tens across 0.1–0.35 (a 4–30× informativeness excess), because the model's own companions carry a cleaner signature than the sky's width budget permits — so arm validations at any anchor overstate power on data, and the post-repair suite must either degrade its injections with the measured common-mode or state this excess alongside every recovery. A prior-sensitivity companion is co-quoted throughout: recentring the measured host prior at its 1σ-low end (peak 0.51 → 0.42) leaves COMPANION-WIN standing but un-pins the zero — the posterior moves to f_comp = 0.2 carrying a residual-boost scrap (per-realization α_marg 0.37/0.50 and 0.00/0.00; Newton within +5/+7 where interior). Three honest residuals travel with the verdict. First, **no cell fits both the kinematics and the multiplicity**: the winning Newton-plus-companions cell misfits its own data by −60…−116 — *the same order as the +99 Newton-rejection gap the fenced fits had won by* — so the marginalization is selecting the less-bad of two misspecified models, an ordering that is provisional against whatever component both are missing; and zero companions is rejected kinematically by −660…−811 — the forward model is missing something at *any* multiplicity. Second, the completeness measurement retracts one of our own 7I inferences (Appendix A, item 15): with the flag catching only 41% of companions, the median boost's survival of the strict cut no longer refutes a companion carrier — the cut barely moves the companion *fraction* (0.51 → ~0.47) — so the model-light channels (the 1.078 median, the §7.2 census) are measurements awaiting attribution, not companion-immune by construction. Third, the injection gate validates the instrument against its own companion model; a real population differing in mass ratios or inner periods could bias the result in either direction through mismatch the self-consistent gate cannot expose. **Status of this section after 7J: the α and Newton-margin values above are conditional on the multiplicity fence and are co-quoted with the marginalized null; the binary anomaly's evidence now rests on the model-light channels; and the decisive next instruments are the forward-modeled median and the forbidden-band leakage null evaluated at the 7J winning cell.** [`data/stage7j_completeness.txt`, `data/stage7j_verdict.txt`, `data/stage7j_diag.txt`]

**The two deciders then ran (all bars and decision rules pre-registered; the day also produced two instrument aborts by our own gates, recorded in sequence; `calcs/stage7jz_mixture.py`, `calcs/stage7jz2b_exact.py`, `calcs/stage7j_marginal.py` amendment 7, `calcs/stage7jz_read.py`, `calcs/stage7jg_read.py`).** The repaired completeness measurement — the joint two-dimensional (δ₁, δ₂) mixture with per-pair common-mode response — passed every estimator gate at essentially exact recovery, including the over-attribution controls Part A lacked (companion-free skies return f̂ = 0.000 even under deliberate common-mode mis-specification), measured f̂ = 0.166 blended per component, and then failed its own postdiction gate *informatively*: the within-pair coherence decays smoothly with color separation (ρ = 0.86 for near-twins → 0.36 for very unequal pairs), which no scalar common mode can produce — the pair-shared displacement is a *vector* (abundance pattern, age) whose projections onto unequal masses decorrelate, while twins project it identically. The coherence-kernel repair (k = exp(−Δc/λ); the private share folds exactly into per-star variance) is worth +476 log-likelihood at exact integration, postdicts every measured structure including the twin slice, and *acquits* the original rate — a kernel-blind fit on a full-kernel companion-free sky still returns f̂ = 0.000, so the missing kernel never manufactured companions. The rate is stable at f̂ = 0.159–0.166 across three common-mode model classes, two integration schemes, and five optimizer basins — host-axis **0.29–0.32 per component, ~3× the literature field rate**, in the direction of Tokovinin's incidence-grows-with-separation trend and formally the first measurement in the 0.2–50 kAU range — but it did **not ship** as the operative prior: the exact-likelihood run tripped two pre-registered numerical-protocol bars (a resolution gate at d = 2.08 — with twins modeled correctly their likelihood kernels are ~0.04 mag wide and every discreteness matters — and a start-dispersion gate at 2.8 versus 2.0), and the standing no-third-iteration rule held; the certificate run is queued and the deciders read at the pre-registered literature fallback. The decisive readings are then anchor-*independent* anyway. **The width channel** — the per-system normalized-velocity smear the flat curve had located as the revival mechanism, implemented direction-neutrally (normalization errors do not rotate velocities) behind an exact regression gate that caught, at its first firing, an amplitude-law wiring bug in its own build (aborted, fixed, re-run to 0.00e+00 agreement on all four seed-law cubes) — is demanded by the data: sq = 0.2 interior with P(sq > 0) = 1.00 and zero edge mass in every read. With it, **α_marg = 0.74 (simple) / 0.70 (occupation) at ΔlnL(Newton) = +23.8/+23.2, and the α_marg(anchor) curve is flat to 0.01–0.03 across every companion anchor from 0.06 to 0.34** — the α answer no longer depends on the companion prior at all; at the unshipped measured anchor it reads 0.67–0.74 at +12…+16. The old COMPANION-WIN lives in the sq = 0 slice, and the width channel does not close the winning cell's multiplicity misfit — it *inverts* it (the cost of f ≥ 0.35 grows 1.4×): given a width channel, the kinematics prefer f_comp = 0.1 at every anchor, so the measured ~0.3 host rate and the kinematic preference are now in named tension, absorbed as evidence attrition at high anchors rather than α motion. The pre-registered verdict is **AMBIGUOUS-CARRIED** — the seed means missed the α ≥ 0.5 AND ΔlnL ≥ +25 detection bar by 1.2–1.8 — and per this paper's boundary-free standard the primary statement is the curve: an interior boost near 0.7 at +23, prior-independent, four times below the fenced claim and six times above the width-less honest read; the occupation arm still rides the noise grid's 2.4 edge at marginal level (extension queued). **The direction-channel decomposition** (γ-collapsed twin cubes, read in both configurations per the review): with the width channel active the tier is ABSORBER-LEVEL — the α answer is *channel-robust* (ṽ-only reads 0.62–0.66 against the two-dimensional 0.70–0.74) while γ's distinct work is pinning the absorbers (the near-parabolic fraction's posterior mass collapses onto the single w_rad = 0.20 grid node — SD from 0.015–0.035 to below the 0.10 grid step); with the width channel pinned off — the four-absorber configuration this section's diagnosis was posed in, and the one every published scalar-velocity pipeline occupies — the tier is SEPARATION-CONFIRMED at α grade by the sharpest available mechanism: **ṽ-only fitting reports a phantom α ≈ 0.50 at ΔlnL +12…+13 which the direction data veto down to 0.20–0.25 at +2…+4**. The tier difference is itself a measurement: the direction channel and the per-system width channel are partially interchangeable absorber-police, and γ's α-grade power is exactly the width channel's absence. The methodological claim of this section therefore operates — at nuisance grade in the full model, at α grade in the configuration the field actually works in — and the phantom-veto demonstration is the two-dimensional likelihood's cleanest credential: on this catalog, a scalar-velocity analysis with standard error modeling can manufacture half a boost from the width budget, and the direction data are what catch it. [`data/stage7jz_mixture.txt`, `data/stage7jz2b_exact.txt`, `data/stage7jz_read.txt`, `data/stage7jg_read.txt`]

**The adequacy decomposition (review round 9; read-only, frame and reading bands pre-committed).** The review asked whether the winning cell's 60–116 kinematic misfit closes once the width channel is active. That statistic had been pre-registered and shipped as D2 — it does not close, it grows 1.4× — but its decision-relevant decomposition had not been computed. It now has, behind exact regressions against the shipped read (4/4): (i) the **anchor strain is 0.0** in all four seed-law reads — α_marg = 0.74/0.70 is the width-complete model's *own unconstrained optimum*, the companion prior does no work, and the kinematic face of the adequacy question is closed; (ii) forcing the measured host rate (f_comp ≥ 0.35) collapses α̂ to 0.00 at ΔlnL = +0.0 in all four reads — **α is fully exposed to the multiplicity tension** (pre-stated band: FIFTH-MOVE-LIVE). If the ~0.3 host-rate measurement certificates and the per-companion kinematic signature is as modeled, the fitted boost goes to zero — at a cell that still misfits the kinematics by 135–153, so nothing fits there and the model-light channels remain; if the certificate fails, or if the coherence-kernel discovery's own direction holds (common-mode vector displacement masquerading as companion light, inflating the modeled per-companion wobble), α stands. The two instruments that decide — the certificate run and the arm suite with kernel-degraded injections — are first in the queue. Two standards gaps the review named are conceded and flagged. First, the width channel's *identity* is open: its existence is measured four independent ways (the §5.3 smear, the s-flat per-system scatter, the 60–116 misfit, the noise-grid edge), but no traced physical channel produces a 20% per-system velocity scale — the photometric-mass channel is measured 12× too small (§5.3), the coherent pair-common mode is ≲3% in ṽ by the same main-sequence translation even taken fully coherent, distance errors contribute 1–2%, and companion-shaped broadening is rejected on shape — and sq = 0.2 exceeds the ~12% velocity signal it sits beside. α is therefore quoted as conditional on the width channel being nuisance rather than signal-shaped — which is what the direction channel tests, since sq is γ-invisible by construction: with a γ-invisible width absorber free, unmodeled γ-invisible systematics load onto it rather than onto α, and α is identified by direction-resolved shape. That identification makes w_rad load-bearing, and w_rad carries an external anchor (freezing it to the Hwang, Ting & Zakamska value moved α̂ by ≤ 0.01, §7.3). Second, the absorber-collapse statements are corrected to grid resolution — all posterior mass on a single grid node, SD below one grid step, not "zero width". The median boost, immune to a symmetric per-system smear by construction, is unaffected by this exposure; its own attribution instrument is unchanged. [`calcs/stage7j_sqclose.py`, `data/stage7j_sqclose.txt`]

**The conversion audit (review round 10).** The review's parting objection: photometric detection and photocenter wobble are steeply anticorrelated in mass ratio — the blend flag needs ℓ ≥ 0.445 (q ≳ 0.7–0.9 before noise smearing) while the wobble |q/(1+q) − ℓ/(1+ℓ)| vanishes for twins — so passing the measured host rate to the kinematic model's flat-q f_comp axis as a scalar compares two differently-weighted moments of the subsystem mass-ratio distribution. Executed with the forward model's own laws behind exact regressions (all gates pass; two instrument-design amendments — a gate clause that tested a dark-companion limit the shared implementation intentionally clips away, and a completeness rescaling missing from the first band design — logged before any number was quoted): the anticorrelation is confirmed at 6–8× per companion (detected companions carry 0.13–0.17 of the undetected wobble variance); the channel attribution is decisive — the forced-multiplicity rejection is *wobble-binding* (doubling the wobble amplitude triples the cost, ΔlnL ≈ +310 along the amplitude nuisance) — and the joint conversion factor spans **[0.10, 0.39]** across mass-ratio-distribution brackets: a detection-shaped subsystem population makes the measured host 0.30 kinematically equivalent to ≈ 0.10–0.12, exactly the kinematic preference, while a low-q-weighted population makes the tension worse. The reviewer's missing half is also measured: the hidden-mass channel √(1+q/2) is twin-maximal and bracket-stable (joint 0.25–0.30 — completeness and mass-weight co-vary and cancel), but non-binding at the margin. The anchor-strength curve completes the exposure's coordinates: α_marg holds 0.67–0.75 for any anchor of width σ ≥ 0.03 and collapses (through the f_comp = 0.2 cell) at σ ≤ 0.02, while the unshipped rate's apparent precision is ~0.015. Together: the fifth-move exposure lives on (rate, precision, mass-ratio distribution) *jointly* — the certificate run must ship a (q, P)-resolved rate, not a scalar (the reviewer's principled version, adopted as a requirement) — and the ×3 multiplicity tension of the preceding passages is *not yet a kinematic tension*: whether the photometric rate and the kinematic preference actually disagree is exactly the open conversion. [`calcs/stage7j_qmoments.py`, `data/stage7j_qmoments.txt`]

**The certificate run then shipped — and its q-resolved deliverable measures the resolution.** The two v2b protocol objects are closed structurally rather than by finer grids: the twin-spiked mass-ratio direction is integrated in closed form (a conditional decomposition makes each single-companion state a segment-wise error-function sum with *no resolution parameter*; certified at 1.7×10⁻⁸ relative against a 30001-node reference, with the f = 0 identity to the discrete likelihood exact), the one remaining numeric axis converges at 0.010 against a 0.5 bar, and profiling the coherence length removes the start-dispersion direction outright (top-two gap 0.03 versus the failing 2.8, the profile peaked and interior). **The subsystem rate is certificated at f̂ = 0.159 blended per component — stable across three model classes, three integration schemes, and every optimizer basin tried — host-axis 0.29–0.32 in the flat-q convention**, with the kernel-blind acquittal repeating (companion-free truth skies return exactly zero) and injection recoveries sharp. The shipped anchor carries its own epistemic guard: the conversion band and a declaration that face-precision imposition on the flat-q axis is invalid. **The q-shape deliverable is the round's discovery: the blended-photometry likelihood itself measures the inner-subsystem mass-ratio distribution *twin-heavy*** — a twin-concentrated law beats flat by 162 log-likelihood (and the low-q-weighted law by 267), with f̂ = 0.124 under the winning law (host ≈ 0.23, nearer the published field-like rates than the flat-convention headline). The population the photometry demands is exactly the wobble-quiet one the conversion audit identified: twins are photometrically loud and kinematically near-silent, so the twin-convention host of ~0.23 carries a flat-q kinematic equivalent of roughly 0.10–0.15 — *consistent with the kinematic preference*. The ×3 multiplicity tension is dissolving in the direction the review predicted, at measurement grade on the photometric side (the joint kinematic–photometric population fit remains future work and is stated as such). The anchored re-read then lands the operative verdict: at the conversion-widened certificate anchor, **α_marg = 0.74/0.70 at ΔlnL = +23.8/+23.2 — AMBIGUOUS-CARRIED, now at a landed and certificated anchor rather than a literature fallback** (the widened and literature anchors agree to the last digit because the curve is flat), while the face-precision flat-q read collapses α to zero exactly as the anchor-strength curve predicted — quoted as the conditional whose premise the certificate's own q-table rejects. One wiring amendment on the reader (the pre-committed operative-anchor flip omitted in the first edit, caught in the output, fixed to the committed spec and re-run) is logged. A noise-grid extension test then adds the honest exposure band: offered an fpm = 3.0 node — a formal-error inflation beyond the published Gaia calibration ceiling — the posterior chases the new edge in both laws and cedes ~8 log-likelihood and ≤ 0.06 of α to it (the missing-width-*shape* signature; the pre-registered decision clause halts further extensions as chasing an unphysical direction), so the operative numbers are co-quoted with the band **α = 0.68–0.74, ΔlnL = +14.5–23.8**, the mismatch-arm suite named as adjudicator. [`calcs/stage7jz2c_cert.py`, `data/stage7jz2c_cert.txt`, `data/stage7jz_read.txt`, `data/stage7jz5_eread.txt`]

**The mismatch-arm suite then validated the operative machinery — four for four, with a clean null.** Injected skies carrying the sky's full measured confusion set — the twin-concentrated companion law the certificate measured (fed to a fitter that stays flat-q), the width channel at its demanded 0.2, the noise factor at its posterior mode — were read blind at the operative anchor against pre-registered bars. The null arm (α = 0, companions at twice the kinematically preferred rate) returns **α_marg = 0.00 in both laws**: under honest mismatch the pipeline manufactures nothing, answering the injection-optimism concern at the anchor that matters. The own-truth arms recover with mildly *conservative* bias (0.74 → 0.65, 0.70 → 0.64), the low-α discriminator arm resolves 0.40 as 0.48 with profile and marginal coincident, and the nuisance sector identifies sharply in all eight reads (the injected width, companion fraction, and noise factor each recovered at their true values). The one adverse residue is now precisely localized: **no injected sky reproduces the real sky's noise-edge chase** (truth-law P(fpm = 3.0) ≤ 0.02 across all arms, against 0.54–0.97 on the sky), so the chase is genuine width-*shape* incompleteness — not companions, not twins, not the flat per-system smear — and its refinement is the named next instrument; consistently, matched-truth injections score ΔlnL +21 to +39 where the sky scores +23, the sky sitting at the low end as residual unmodeled width eats significance. With both pre-declared deciders landed — the certificate and the arms — the program's credence in a real binary anomaly moves ~45% → ~50% (a reasoned update, disclosed as such: no mechanical map was pre-committed for this pair; upward on the collapse of the companion alternative's quantitative case and the machinery's adversarial validation, capped by the still-AMBIGUOUS verdict and the open width-shape systematic). [`calcs/stage7jz5_armread.py`, `data/stage7jz5_armread.txt`]

**The width-shape contest then closed the noise-costume question and shipped the risk axis.** A pre-registered contest offered the promoted incompleteness two instrument-noise shapes — an error-independent velocity floor and a heavy-tail fraction (a small subset of pairs at 4× their formal errors) — with win bars, a chase-dissolution bar, and a reproduction arm locked before any cube was built, over a diagnostic whose own regression reproduces the stored likelihood bit-for-bit. Neither shape wins: the tail axis goes entirely unused (gain +0.0 in both laws — consistent with the diagnostic, which located the demand in the *mid-shoulder* of the velocity distribution and the *radial* orientation column of the inner separation bins, not in any tail), and the floor clears its bar in one law only, by riding its grid edge into territory ≈ 4.3× the formal errors while the fpm chase continues underneath — so the verdict is UNRESOLVED-CARRIED and the operative band stands. The round's product is the **noise-ceiling profile of the detection**: under the *physical* Gaia error envelope (calibration-ceiling inflation, systematic floor at the published angular-covariance scale) the detection reads **α = 0.80/0.80 at ΔlnL = +35.2/+32.3** — stronger than the operative quote, which is therefore noise-*diluted*, not noise-protected; at the operative flat-to-3.0 grid it reads the co-quoted 0.68–0.74 / +14.5–23.8; and only when isotropic noise several-fold beyond the published error budget is admitted does it reach 0.00. Two guards give this curve its standing. First, an exchangeability arm: a validated injected-boost sky (α = 0.74 truth, no floor) re-fit with the floor axis open *keeps its boost and rejects the floor* (floor posterior quiet, edge-chase probability 0.00) — the axis is not a boost-eater by construction, so the real sky's corner preference is data-driven: the sky genuinely contains ~7–11 log-likelihood of width the model lacks. Second, the mechanism is volumetric: an axis unused at its own peak still drains the Newton margin through the α = 0 row (the tail leg loses half its ΔlnL to cells it never prefers) — a caution transferable to any marginalized pipeline: sub-significant nuisance axes can zero a marginal detection by posterior volume alone. What remains is an exclusion set — the residual width object is not floor-shaped, not tail-shaped, not error-inflation-shaped, not the flat per-system smear, and not the systemic-velocity perspective residual (the sample carries full RV coverage) — with a population-like fingerprint, so the named successor hypothesis is the inner-bin eccentricity/radial-orbit sector of the *population* model, booked and not opened here. The physical-envelope reading is reported as a conditional alongside the operative numbers, not in place of them. [`calcs/stage7jz6_widthshape.py`, `calcs/stage7jz6_read.py`, `data/stage7jz6_read.txt`]

**The forward median then priced the model-light anchor's independence (7K-a).** The §6.1 boost statistic is a ratio of median ṽ between separation decades, and its "Newton predicts 1.000" framing carries two conditions this instrument measured on the way in (both gate alarms, both real model facts, both logged before any verdict number was read): the *fitted* population's eccentricity distribution runs with separation, putting model-Newton's ratio at ≈ 0.98 rather than 1.000, and the semimajor-axis prior's truncation distorts the edge-adjacent bins — the clean scale-free identity holds on interior bins (control ratio 1.005/1.002). Against the pre-registered bars, the landed Newton-best cell — companions, noise at its demanded level, smear, all on — forward-produces **R = 1.033/1.043 of the observed 1.078 (CI 1.052–1.103): GRAY, both realizations** — the absorber budget covers roughly half the model-referenced gap (lift +0.047/+0.051 of the needed +0.092/+0.086) and cannot reach the data's confidence interval, while the boost cells *over*-produce the ratio (1.114–1.127, the noise-edge lift, flagged). The anchor's honest role after this stage: the median is no longer a standalone Newton-killer — at the landed absorber budget the data sit nearly equidistant between the Newton-best and boost cells on this one statistic — but it survives as a model-light *excess* over Newton-plus-absorbers of +0.035–0.045, and the discrimination lives in the full two-dimensional likelihood where Newton continues to lose. [`calcs/stage7ka_median.py`, `data/stage7ka_median.txt`]

### 6.4 The binaries read the same dial

The §4.3 coefficient family was pushed through the external-field solver — six λ-tables at the physical field, with generation gates (the λ = 1 table reproduces the stored occupation table to <1%; the λ = ½ table satisfies the isolated-sphere identity to <2%) — and the full v7 likelihood, on perspective-corrected data with every nuisance active and α refit per node, was profiled over λ on two population realizations. The binary data's answer, from a regime (x ≈ 1, external-field-dominated) disjoint from the rotation curves':

> **binary ĉ₁ = 0.37–0.50 (a two-realization indication, not yet a measurement)** — the per-seed profiles peak at λ = 0.75 and 1.00 (curvature σ_λ ≈ 0.08, realization scatter comparable to the 0.25 grid step); **the c₁ = 0 branch is rejected at ΔlnL ≈ 20 in each realization**, and below λ = 0.5 the fits ride the amplitude grid edge (α̂ → 2) and still lose — a shape rejection, not an amplitude trade.

Two disconnected dynamical systems — disk rotation at low accelerations (§4.3: profile 0.26–0.45 across treatments, bootstrap ≈ 0.4 ± 0.3) and stellar pairs at the external-field transition — independently return a leading coefficient in the same band, each excluding the c₁ = 0 branch on its own. The grade is stated with the claim: the binary side rests on two realizations at a 0.25 grid step — the thinnest number carrying a headline anywhere in this paper — and is quoted as an *indication* until the six-seed budget completes; the c₁ = 0 rejection (a shape rejection at the grid edge) is the robust part. Further caveats: the g_N,ext systematic shared with §6.3, and the family-conditional character shared with §4.3. [`calcs/stage4x_binlam.py`]

**The landed-model re-run then erased the binary leg (7J-d).** With the §6.3 companion and width sectors marginalized rather than fenced — the λ-family run through the full width-complete machinery at the certificated anchor, behind two exact identity gates (every function's Newton row bit-identical to the reference; the λ = 1 cube reproducing the occupation-law cube bit-for-bit) — the evidence profile over λ is *flat within realization scatter*: the peak sits at c₁ = 0.375 in one realization and at c₁ = 0.000 in the other, with rejection margins (+3.4, +0.0) both inside the ±5 tie band. **The fenced c₁ = 0 rejection was a fenced-model result; under the landed posterior the binaries carry no constraint on c₁, and the "same dial" claim above rests on the galaxy leg alone.** This supersedes the queued six-seed fenced budget by construction (more realizations of a retired model answer nothing). The same run generalizes the lesson to the whole function set: all sixteen world-table laws — including four whose fenced vetoes had been decisive — sit within ±8 log-likelihood of the occupation law with seed scatter of the same size, so the fenced vetoes stand as fenced-model results and the binary data vote reads: *against Newton at the co-quoted band grade on every one of the 32 function × realization rows (minimum ΔlnL +7.9), and agnostic among the sixteen modified laws.* The rotation curves remain this program's only function discriminator; the noise-edge chase is function-blind (posterior at the edge for every law), placing the §6.3 width object upstream of the ν-choice. [`calcs/stage7jd_read.py`, `data/stage7jd_read.txt`]

The §4.4 hierarchical winner was then given the same treatment on its own terms — a dedicated bootstrap-function EFE table (spherical-identity gate 0.01%) through the identical likelihood. **The binaries veto it**: −56360.6 / −56369.1 across the two realizations, +17–24 behind the ½-branch functions, with α̂ driven to the amplitude grid edge in both — the same edge-riding shape-rejection signature the low-λ mixtures produced, now for a function whose *deep* coefficient the hierarchical galaxies prefer. The transition regime is the binaries' home ground, and it holds the ½ branch. [`calcs/stage5f_bootbin.py`]

The §4.4 decomposition then supplies candidates that should satisfy both regimes, and the four-seed budget delivers the full picture. The sharpened functions — ν_p(0.65) and the geometric-mean construction — are **binary-viable**: α̂ interior in every realization (1.40 ± 0.04 and 1.44 ± 0.08 respectively; no edge-riding; Newton excluded at +77 to +98 under every function). But viability is not preference: across six seeds, **the binaries prefer the pure occupation law sign-consistently in 18 of 18 contests** (Δ lnL = +5.2 ± 0.9 for ν_p(0.65), +8.5 ± 2.1 for the geometric mean, +7.7 ± 0.7 for ν_p(0.578) at four seeds) — a modest, real counter-lean, an order of magnitude smaller than the hierarchical galaxies' preference in the opposite direction. The six-seed amplitudes are α̂ = 1.078 ± 0.023 (occupation), 1.41 ± 0.04 (ν_p(0.65)), 1.36 ± 0.07 (geometric mean), every fit interior. The a₀ bookkeeping sharpens the objection: translated through their own external-field tables (κ = 1.22/1.34, gates passing), the sharp-function amplitudes give a₀(binaries) = 1.59/1.51 ± 0.11 ×10⁻¹⁰ — **+5.1σ/+4.3σ from cH₀/2π, against ≈ +1.8σ under the occupation law** — so the tail dial buys hierarchical-galaxy likelihood at the price of the binary amplitude's temperature lock. The transition regime holds the ½ branch on both counts. [`calcs/stage5h_punified.py`, `calcs/stage5k_binprofile.py`, `calcs/stage5l_ladder.py`]

The mixing family of §2.4 then received its dedicated binary profile — new external-field tables at β = ¼ and ¾ (generation gates: the β = 0 table reproduces the stored occupation table end-to-end to 10⁻¹⁵; spherical identities at 0.01%; the external-field boost is strictly monotone decreasing in β at every acceleration), the bootstrap member completed to six seeds, a full 6-realization × 5-member likelihood matrix on one machinery footing. **The binaries pin the dial at zero: β = 0 beats every β > 0 on every realization — 24 of 24 contests — by ΔlnL = −4.1 ± 1.1 (β = ¼), −8.5 ± 2.1 (½), −7.9 ± 1.9 (¾), −15.4 ± 2.0 (1), and the mean profile crosses ΔlnL = 0.5 at β = 0.03 and ΔlnL = 2.0 at β = 0.12** — the transition regime's ½-branch verdict, now as a continuous one-parameter bound. The texture repeats the function contests': the β ≥ ¾ members ride the amplitude grid edge (5/6 and 3/6 realizations — shape rejection, not amplitude trade), while β = ¼ stays interior at α̂ = 1.57 ± 0.11. Set against the galaxy profile (β̂ = 0.45–0.64) this would read as a flat two-system contradiction; the §4.4 decomposition shows it is instead a *regime* split running identically through both datasets — the transition holds β = 0 wherever it is measured. [`calcs/stage5r_binbeta.py`]

The derived running-β functions of §2.4 — whose transitions (ν(1) = 1.470/1.494) sit between the geometric mean and the occupation law — were then given the same six seeds. **They close a third of the transition gap: ΔlnL = −5.8 ± 1.9 (F1) and −5.5 ± 2.3 (F2) behind the occupation law, against the geometric mean's −8.5 ± 2.1** — with every amplitude interior (α̂ = 1.395 ± 0.039 and 1.487 ± 0.017; no edge-riding anywhere, and one realization flips positive). Against pre-stated read bands (mean within −3 = accepted; beyond −6 = geometric-mean-grade rejection) the result straddles: improved, not accepted — the binaries still lean to the pure occupation law by ~2.4 SE, localized exactly where the §4.4 anatomy pointed (the transition wants an even smaller admixture than the spontaneous fraction leaves there). One ledger item is flagged open rather than computed: the a₀ translation of the α̂ ≈ 1.4–1.5 amplitudes requires these functions' own external-field κ tables; at geometric-mean-grade κ it would repeat the sharp-function temperature strain of the preceding paragraph, at occupation-grade κ it would not. [`calcs/stage5w_runbin.py`]

Both open items then closed, against the derived functions. The κ tables give κ = 1.26–1.35 (geometric-mean grade) for all four running functions, so the **a₀ translations land at 1.54 ± 0.10 (+5.2σ), 1.62 ± 0.09 (+6.3σ), and 1.63 ± 0.09 (+6.2σ)** — the two-leg energy member's row is edge-invalidated (α̂ = 2.0) — while pure occupation stands alone at +1.9σ: the temperature lock is a second, independent binary veto of every sharpened function. And the two-leg refinement, despite raising ν(1) to within 3% of the occupation value, is rejected too (**−6.8 ± 1.6 interior; −7.0 ± 2.1 with the amplitude edge-ridden on all six realizations**): across eight sharpened functions the binary penalty is −5 to −8.5 *nearly independently of the transition value* — what the binaries reject is the function's screening behavior under the dominant external field, not its transition. The decisive control — the pointwise drive-weighted rule of §2.4, whose external-field table is within 0.35% of the occupation law's at the wide pairs — is **excluded at −9.6 ± 1.5 (better in 0/6)** with BE-grade interior amplitudes (α̂ = 1.098 ± 0.021) and a *passing* temperature row (a₀ = 1.31 ± 0.13, +2.1σ): the likelihood punishes exactly the mid-separation sag the rule predicts, so the binaries demand soft screening at all their separations, including where their internal field dominates the ambient one. The split between the systems is system-level, and its two surviving readings (ambient-gated bath character; the modified-inertia trajectory functional) are stated with their tests in §9.2. [`calcs/stage5y_kladder.py`, `calcs/stage6b_twobin.py`, `calcs/stage6d_drivebath.py`]

The ambient-gated function of §2.4 then passed where everything else failed: **−0.88 ± 2.66 against the occupation law across the six realizations (two preferring it), every amplitude interior (α̂ = 1.060 ± 0.024), κ = 0.924, and a₀ = 1.28 ± 0.15 — at +1.6σ from cH₀/2π, the strongest binary temperature row in this paper**, marginally better than pure occupation's own +1.9σ. Both binary vetoes — the screening shape under the external field and the temperature translation — are passed by the same zero-parameter function that carries −59 on the hierarchical galaxies (§2.4, §4.4). Its binary-side pass was largely built in (the gate puts the function within 0.3% of the occupation law at this ambient — the post-hoc caveat of §2.4); what was not built in is the κ/a₀ outcome and the survival of its galaxy leg, and both landed. [`calcs/stage6g_ambbin.py`]

### 6.5 The realization systematic

A methodological result with field-wide consequence: the finite-population realization of orbital elements shifts distribution-level lnL by *more than the Newton-vs-MOND gap* unless marginalized. Analyses that fit distribution shapes against a single simulated population inherit an unquantified systematic of exactly the disputed magnitude; our budgets marginalize six realizations and report the scatter. We found no published quantification of this effect. [`calcs/stage3a_likelihood.py`, Stage 3H]

## 7. The eccentricity sector, the perpendicular ceiling, and the reconciliation

### 7.1 The near-parabolic population: a confirmation, not a discovery

The folded direction-angle distributions are U-shaped — excesses at both γ < 20° and γ > 80° against every single-power-law eccentricity family. The radial arm and its cause are *prior art*: Hwang, Ting & Zakamska (2022) measured, on this same catalog with the same v-r angles, an eccentricity distribution running from uniform at 100 AU to superthermal (α_e = 1.2–1.3) at 1–31.6 kAU, with "e > 0.9 enhanced." Our contribution is the joint gravity-law × eccentricity inference they proposed but did not run, extended to 50 kAU, with contaminant fences — and a cross-validation that we regard as one of the paper's strongest systematics results:

> the e > 0.9 mass fraction implied by their superthermal law is **20.4–21.7%** across their three wide bins; our independently fitted near-parabolic weight is **w_rad = 0.20**, selected in every one of 30+ fit variations.

Two pipelines with disjoint assumptions — theirs directions-only and Newton-assuming, ours a joint 2D fit with the gravity law free — agree on the population's size to 1.5 points. The main nuisance of our measurement is externally validated by a hostile-assumptions replication. (A circular sub-population was separately vetoed: w_circ → 0, and circularity narrows ṽ, the wrong direction.)

### 7.2 The perpendicular velocity ceiling

The ceiling is the field's founding bound, not ours. Vis-viva gives the geometry-independent theorem that a *bound* Newtonian pair can never exceed ṽ = √2 at any eccentricity or phase — stated at the outset of the wide-binary program by Pittordis & Sutherland (2018: "the well-known result that u_3D < √2 for any bound orbit") and Banik & Zhao (2018: ṽ "can't exceed √2 in Newtonian gravity. In MOND, we expect the upper limit to be somewhat higher") — and Pittordis & Sutherland (2023) name the band above the Newtonian decline "the key discriminant between models." What has kept the discriminant from being decisive is that, in the full sample, the band is dominated by unbound and multiple contaminants; Banik (2019) in fact proposes the above-√2 population as a contamination *diagnostic*. Our contribution is the column where that degeneracy breaks: the perpendicular slice of the (ṽ, γ) plane, where flyby geometry is disfavored, the direction channel has independently fixed the eccentricity mixture, and a boosted law raises the ceiling to √(2·B) — B ≈ 1.36 at wide separations for α = 1. Among wide pairs (s ≥ 6 kAU) with γ ≥ 75°:

> 11 pairs in [1.2, 1.414); **11 pairs in the Newtonian-forbidden band [1.414, 1.67) at nearly equal density; 1 pair in [1.67, 2.2); none beyond.**

With the measured per-pair noise (σ_ṽ = 0.044), a true Newtonian edge predicts 0.9 pairs in the forbidden band — P(≥11) = 3.8×10⁻⁹ — while a true edge at the boosted 1.65 predicts 11.6 in-band and 0.5 beyond (P = 0.62 and 0.91): the distribution terminates at the boosted escape edge, with no parameter tuned to this test. Rival identities fail on their own numbers: field-star flybys at closest approach would arrive at ṽ ~ v∞/v_c ~ 100, not 1.5, and possess no ceiling (the low-γ unbound continuum indeed runs smoothly to ṽ > 3 — and demonstrates that the catalog's selection, at ṽ ≈ 3, is not the cliff); the band's astrometry is cleaner than average (median RUWE_max 1.06 vs 1.28 for the same-ṽ low-γ continuum; S/N = 30; R_chance ~ 8×10⁻⁴); the measured 2.4% mass error cannot produce 15% shifts; companion inflation could move 1–2 pairs, not 11, and would smear past the cliff. The slow island of the same column is the apocenter face of the §7.1 population (implied e median 0.957, velocities at 4.9× the noise floor, γ-peaked as orbits require).

We state the caveats with the claim: eleven pairs; the ceiling location inherits the per-pair spread of B; the §7.4 perspective correction and an independent reimplementation of the angle machinery each move the census by ±1 at the band boundaries (one pair relocates to ṽ = 1.675 ± 0.044 — edge pile-up, consistent with the cliff), leaving the leakage null no worse than ~10⁻⁸; and the islands were flagged by residual maps before the edge test was formulated (the occupancy-and-termination statistic is new; the ceiling bound and the band's discriminating role are the field's, as credited above — Appendix A, item 11). Gaia DR4 multiplies the band tenfold and converts this into a sharp prediction: the band fills, and the cliff stays at 1.65 — or the boosted reading is wrong. As a model-completeness check, adding the closest-approach (γ ≈ 90°) arm to the flyby template — the component our fit generations lacked — improves the global fit by +4.7/+3.4 while moving α̂ by ≤ 0.06: the residual that led us here was a model-shape gap, not a boost bias. The census is released pair-by-pair (`data/ceiling_pairs.csv`, adopted from an external-review request): every wide top-γ pair in or adjacent to the band, with Gaia EDR3 source IDs, ṽ and γ under both raw and perspective-corrected velocities, systemic RV and perspective exposure, direction S/N, per-pair noise σ_ṽ, RUWE of both components, chance-alignment probability, and parallax consistency — sufficient to re-count the band under any edge (√2 vs 1.414), angle, or noise convention without our pipeline. [`calcs/stage4j_gamma82.py`, `calcs/stage4m_fly90.py`, `calcs/stage7i_ablations.py`]

**The companion-audit the §6.3 correction demanded was then executed (7K-b) — the count falls, and the pair of numbers stands stronger than the count ever was.** Evaluated as a forward null at the §6.3 landed Newton-best cell — which carries the companion wobble and hidden-mass machinery, the demanded noise, and the smear, and is therefore the companion-*marginalized* leakage null — the raw band count loses its power by over-prediction: the landed cell would put 26–38 pairs in the forbidden band against the 9 observed under the corrected convention (the pre-registered NULL-BROKEN outcome). But the same cell predicts 17–32 pairs in the overshoot [1.67, 2.2) where the data show **two** (P(≤2) = 7×10⁻⁶ … 3×10⁻¹¹), and no configuration tried — physical noise, smear off, the boost cells themselves — reproduces nine-with-a-cliff-of-two (minimum P ≈ 9×10⁻⁵; roughly half the model's flood is the direction-blind smear lifting the perpendicular ṽ ≈ 1.1–1.3 population, the rest is landed noise scattered off the near-parabolic pericenter pile, whose velocity is perpendicular by geometry). The census is therefore *self-defending as a pair*: the cliff's sharpness bounds the tail noise to approximately the formal errors — the only regime fitting the observed termination (P = 0.62/0.91 above) — and at formal noise the band is unleakable (the 3.8×10⁻⁹ null). One cannot simultaneously hold "noise large enough to fake the band" and "the observed cliff." The operative census statistic from this stage forward is the (band, cliff) pair; the count alone is retired as absorber-conditional; and the tail region joins the noise-ceiling profile and the Gaia calibration as the third independent witness that the §6.3 unmodeled width object is mid-shoulder-localized and never reaches the tail. (A per-pair unconditional wobble bound attempted alongside proved vacuous by design — a short-period companion's wobble is 8–30× the circular velocity and such pairs leave the catalog — and is logged as superseded by the population null; the per-pair photometric leg folds into the population-sector successor, the measured mechanism not being companions.) [`calcs/stage7kb_census.py`, `data/stage7kb_census.txt`]

### 7.3 Reconciliation by ablation — retracted in part (Appendix A, item 16)

Banik et al. (2024) fit (r_sky, ṽ) counts with the hidden-triple fraction free — it fits to 69% under Newton — on a 2–30 kAU sample with no deep-Newtonian anchor, and report Newton preferred at 16–19σ. Hernandez, Chae & Aguayo-Ortiz (2024) identify three defects: the fitted fraction exceeds every independent calibration; ṽ bins narrower than the measurement errors are compared without noise convolution; and no high-acceleration anchor exists to validate the method. Rather than argue, we ablate our own pipeline toward those choices (proxy-grade, honestly labeled) and measure the consequences against our baseline (+108.7/+98.8 over Newton):

| ablation | Δ lnL(Newton), simple/occ. | α̂ | reading |
|---|---|---|---|
| direction channel removed (ṽ-only, as all published analyses) | +105.3 / +98.4 | 1.18 / 1.55 | detection intact; the *measurement* degrades — α̂ unpins from 1, w_rad drifts. The γ channel protects the parameters. |
| companion fraction unfenced (0 → 0.8) | +42.8 / +43.8 | 0.71 / 0.83 | **~60% of the Newtonian rejection absorbed** — while requiring fractions the *flagged* photometry appeared to forbid (flagged rate 12%; their fit, 69%; see the 7J status note below) |
| both + 2–30 kAU window (anchor dropped) | +37.5 / +29.8 | 0.68 / 0.73 | **two-thirds of the significance manufactured away; boost estimate biased low** |

Three conclusions. The detection never flips: Δ lnL ≥ +30 survives full Banik-style freedom on our data. The dominant manufactured component is the unfenced companion fraction — a parameter that is *photometrically boundable* and, when bounded, cannot absorb the signal. And the residual distance to an actual Newton-preferred verdict plausibly lives in the one defect our always-convolving pipeline cannot honestly reproduce (the sub-error unconvolved binning documented by Hernandez et al.) together with the §6.5 realization systematic. The prescription for the field is measurement, not adjudication: bound the companions photometrically, keep a deep anchor bin, use the direction channel. [`calcs/stage4n_banikstyle.py`]

**Correction #16 — the retraction, stated plainly (one clause re-graded under #18; see the bracketed note).** This section's central claim was that the Newton-favored result was *manufactured*: that the freed companion fraction "requires fractions the photometry forbids." That claim was wrong regardless of scale: it treated a flagged rate as a bound with no completeness accounting, and when we freed the same parameter under a completeness-based prior and a corrected amplitude law, our own marginalized verdict landed on the Newton-favored side of the fit-level question. The companion freedom was directionally legitimate. [Re-grade under correction #18: the first version of this retraction added that "Banik et al. were substantially closer to the truth than our fenced fits," conditioned on Part A's measured host rate of 0.42–0.57 — a scale since retracted. Under the now-primary literature subsystem rates (~0.2 per pair), our old f ≤ 0.1 fence is ~1.5σ low — nearer the truth than their 0.69, which stays excluded under every reading. The valid core of #16 — the flagged-rate-as-bound logic was broken, and the freed fraction was not photometrically forbidden — stands.] The phrase "two-thirds of the significance is manufactured" is therefore retracted in its normative sense for all three ablation legs: each leg's *arithmetic* stands as measured on the as-published law, but the reading that each freedom "manufactures away" a true signal presupposed the fenced fit was right — the presupposition 7J removed. What survives of this section, stated exactly: (i) the ablation table as a mechanical map of where each modeling freedom acts; (ii) the exclusion of their *specific* fitted fraction (0.69: ln π = −36 against the measured distribution, and ~−2000 in our 2D likelihood even under their own amplitude law); (iii) the internal inconsistency of the as-published wobble amplitude law itself (§6.3) — a defect our correction repairs and their fit inherits; (iv) Hernandez et al.'s sub-error-binning critique, which is theirs, not ours; and (v) the in-principle superiority of the direction channel, which carries more information whichever way the verdict goes. The prescription this section ended with — bound the companions photometrically, keep a deep anchor, use the direction channel — survives *because* we followed it: the completeness instrument it demanded is the one that ruled against us.

### 7.4 The 2026 quality framework, and the perspective audit it triggered

As this manuscript was being finalized, Cookson et al. (2026) — a seven-author quality-checklist paper whose lead author is an independent researcher and whose co-authors span the field's Newton-favored analyses (Banik, El-Badry, Sutherland, Penoyre, Pittordis, Clarke) — published a best-practices framework for the wide-binary test together with a median-ṽ flatness analysis of 1,421 RV-clean pairs within 130 pc, finding no MOND-predicted step (their cleanest sample prefers Newton by ~1500× in likelihood; a 2.7σ Gaussian-equivalent rejection of the 20% step). We engage it on three fronts.

**(a) Their checklist, applied to this pipeline.** Most items we satisfy by *measurement and modeling* rather than exclusion: companions are photometrically fenced and kinematically capped (§5.3) where they cut the HR diagram; the catalog's velocity acceptance is measured from the data and applied to the models (§5.2) where they cut at ṽ < 2.5; per-pair noise is convolved into every template (their §4.2 concern) where they threshold on it; the common-distance convention (their item 4.1.1) is shared. Two genuine divergences: we impose no ΔRV cut (our sample does not require Gaia radial velocities), and — the one outright gap — **no spherical-projection correction (their item 4.1.6) had been applied anywhere in this program**. Their Newtonian-equivalent external field, g_N,e = 1.184 a₀, independently confirms our §6.2 conversion (1.15 ± 0.05 a₀) to 3%.

**(b) The perspective audit (Appendix A, item 12).** The omitted term is the spurious on-sky velocity Δv = −RV_sys·θ directed along the separation vector: purely radial in the (ṽ, γ) plane, growing as s^1.5 in ṽ terms — shaped, in other words, like a signal. Cookson et al. show its omission inflates their median ṽ by ~0.15 beyond r_M. We therefore audited our statistic (`calcs/stage4q_perspective.py`). The effect is *present* in our data with the predicted sign and size: regressing the observed widening rate against −RV·θ over our wide pairs gives slope 0.92 (CI 0.80–1.08; prediction 1) — their figure 7, reproduced on our own selection. It is *not* our signal, on three independent grounds: (i) the Newton-plus-perspective-only prediction for our anchor ratio is 1.016, against the observed 1.086; (ii) the perpendicular velocity component — immune by construction, since the spurious term is purely radial — shows a *stronger* boost (1.151, CI 1.115–1.197) than the full statistic and rises monotonically across all four separation bins, the opposite of the artifact signature; (iii) the exact per-pair correction using the catalog radial velocities moves the anchor from 1.086 to **1.078 (CI 1.052–1.103)**, adopted in §6.1 as the corrected value. The ceiling census of §7.2 moves by at most one pair at the band boundaries. The full re-fit on corrected velocities was then executed (six paired seeds, both laws, all nuisances active; `calcs/stage4r_corrected_refit.py`): α̂ moves by −0.012 ± 0.012 (simple) and −0.022 ± 0.015 (occupation) — consistent with zero and an order of magnitude inside the quoted errors; the Newtonian rejection cedes ~5 lnL (five per cent), standing at +103 ± 9 / +90 ± 7 across seeds; w_rad = 0.20 is selected in all twelve fits; and the simple-over-occupation lean is unchanged (−12.6 ± 2.4, versus −12.3 ± 2.2 uncorrected). The §6.3 numbers therefore stand as quoted, with the perspective correction executed rather than merely bounded.

**(c) Their null and this measurement.** Their flatness test assumes the eccentricity distribution and companion incidence do not vary with separation, citing Hwang et al. (2022) as the constraint on the former. Hwang et al. in fact *measured* the eccentricity distribution to steepen from uniform at 100 AU to superthermal at kAU separations — the trend our w_rad sector fits jointly with the gravity law (§7.1). A near-parabolic population at wide separations *lowers* the median ṽ (orbits linger near apocenter), so an eccentricity trend of the measured sign suppresses a step in their statistic rather than faking one; deciding between a suppressed step and no step requires forward-modeling both, which their framework explicitly declines ("we do not discuss what would constitute a suitable forward model for the WB population") and which §6 does. Conversely, their remark that counting ṽ > √2 systems "is not a practical MOND test" because contaminants dominate the band is precisely the objection the perpendicular column answers (§7.2): the direction channel is what purifies the band. Their deep coverage and ours barely overlap (their sample reaches r_sky/r_M ≈ 3 within 130 pc; our 214 widest pairs at 20–50 kAU extend beyond, at larger distances where the perspective term we now correct is proportionally smaller). The disagreement between their flat medians and our fitted boost is therefore not yet an arithmetic contradiction but a modeling-philosophy fork — quality cuts versus measured nuisances — and our stated side of it is the §7.3 prescription plus the corrected anchor of (b): bound the companions photometrically, keep a deep anchor bin, convolve the noise, apply the spherical correction, and use the direction channel.

**(d) The fork, quantified (7L — pre-registered, executed inside this pipeline).** We rebuilt their selection on our catalog (their cuts stepwise: distance, parallax precision, RUWE, both-component RVs with ΔRV < 10, 1–30 kAU, σ_ṽ, their ṽ < 2.5 ceiling, HR filter with overluminous exclusion; N = 1194 against their 1,421 — a 16% proxy discrepancy, declared) and ran both directions. *Their statistic on our data:* the median-ṽ step between r_sky/r_M ∈ [1, 3.1] and [0.05, 0.5] reads **0.985 (CI 0.909–1.060) — their flatness replicates on our own catalog under their cuts.* *Their statistic on our model:* the landed boost cells forward-predict a step of **1.092–1.101, not 1.20** — the measured eccentricity trend, the marginalized nuisances, their ceiling and their range cut the naive MOND step roughly in half — so their 2.7σ rejection of a 20% step is ≈ 1.3–1.4σ against what the landed model actually predicts, and the boost-versus-Newton separation on their statistic at their sample size is ≈ 0.9σ (a scale-free wiring control passes at 0.995–1.002, and the fitted-eccentricity Newtonian baseline is ≈ 1.00 on this statistic — their normalized-separation range excludes the bins where that systematic bites). *Our likelihood on their selection:* the full two-dimensional fit at the landed anchor is CONSISTENT with the operative α in all four law × realization reads (the operative cells sit 1.2–3.3 log-likelihood behind the peak, α = 2 excluded at −25…−39) while the marginal prefers Newton by only ~0–3 — the N-scaled expectation. The conclusion sharpens §7.4(c) into arithmetic: their cleaning discards 92% of the joint sample, including the entire deep anchor, and what remains carries ~1σ of boost information on either statistic — **"no MOND evidence on 1,421 RV-clean pairs" and "Newton excluded at the co-quoted band on 14,071 pairs" are mutually compatible statements**, and the entire disagreement resides in whether the removed 92% is data or contamination. [`calcs/stage7l_cookmask.py`, `calcs/stage7l_step.py`, `calcs/stage7l_read.py`, `data/stage7l_step.txt`, `data/stage7l_read.txt`]

## 8. Solar-system consistency: the paradox and its open door

### 8.1 The quadrupole: our boost, our solver, Saturn's veto

*(Status after §6.3: every result in this section is α-scaled by the binary amplitude, and that amplitude is fence-conditional — at the measured multiplicity the marginalized α is 0. The section's distinct contribution — a binary-calibrated quadrupole immune to the mass-to-light and bulge mitigations — is therefore suspended with it; the underlying tension reverts to its galaxy-calibrated form (Desmond, Hees & Famaey 2024), which our solver cross-validation independently supports at the 15% level but no longer independently strengthens. The composition contest of the final subsection and the MI/MG brackets of §8.2 are measurements on the same binary likelihood and carry the same flag. Retained in full as the conditional record.)*

The external-field solve is scale-free: the solution that produced the binary boost tables is, relabeled, the Sun in the Galactic field (r_M = √(GM⊙/a₀) = 7,032 AU), and its interior carries an anomalous quadrupole δφ = q·r²·P₂(cosθ) along the Galactic axis — the term constrained by planetary ephemerides (Blanchet & Novak 2011) and invoked to torque trans-Neptunian orbits as a Planet-Nine alternative (Jones-Smith & Mathur 2023, arXiv:2304.00576). Extracting the ℓ = 2 interior moment from our solver (six gates, including an exact analytic-integrator test and a cross-formulation validation: Blanchet & Novak's AQUAL value for the simple function at the total field 1.9×10⁻¹⁰, Q₂ up to 4.1×10⁻²⁶ s⁻², against our QUMOND solve at the matched Newtonian 1.2 a₀ — agreement at 15%, which independently confirms the §6.2 convention mapping):

> **Q₂ = (3.95 ± 0.39)×10⁻²⁶ s⁻² (simple) / (3.82 ± 0.46)×10⁻²⁶ s⁻² (occupation), α-scaled.**
> Cassini radio tracking (Hees et al. 2014): Q₂ = (3 ± 3)×10⁻²⁷ s⁻² — **exceeded ~4.3×.**

Two structural points. The two RAR-compatible families give the *same* quadrupole to 1%: Q₂ is sourced in the transition region (r ~ r_M), where they are near-identical — exponential (Wien) screening rescues the local anomaly at Saturn (the §3 test) but not the long-range ℓ = 2 moment of the transition shell. And the tension is not new in kind — Desmond, Hees & Famaey (2024) reported 8.7σ between the RAR and the solar quadrupole — but our version is calibrated by the wide binaries rather than by rotation-curve fits, and is therefore immune to both mitigations available to theirs (mass-to-light freedom; bulge removal): binaries have no bulges, and our mass error is measured at 2.4%. Within modified-gravity formulations (AQUAL/QUMOND), the binary boost and Saturn's ephemeris are in direct conflict. A corollary: the proposed MOND external-field explanation of the trans-Neptunian clustering runs on precisely this capped term and inherits the same veto. An independent exclusion in the same arena — AQUAL's failure against Oort-cloud comet binding energies and the detached disk (Vokrouhlický, Nesvorný & Tremaine 2024) — brackets the conclusion from population dynamics rather than ephemerides, and ends on the same escape clause our §8.2 reaches: theories "in which non-Newtonian effects are screened on small spatial scales, at small masses, or in external gravitational fields." [`calcs/stage4k_quadrupole.py`]

The sharpened-screening functions of §4.4 were tested against this wall, and the wall is *shape-invariant*: ν_p(0.65) lowers the raw quadrupole to 0.81× the ½-branch value — but the binaries then demand α̂ ≈ 1.45 under it, and the product returns 3.9×10⁻²⁶, 4.4× the cap; the geometric-mean bootstrap behaves identically (0.85× raw). The binaries pin the transition-region boost, the quadrupole is sourced by exactly that region, and **Q₂·α̂ is invariant under the tail-sharpness dial** — there is no interpolating-function escape from the Cassini tension anywhere in this family, which strengthens rather than weakens the modified-inertia door of §8.2. [`calcs/stage5i_quadrupole2.py`] The scan was then completed across the full mixing family of §2.4 (member and Newton-control gates exact; resolution doubling ≤ 0.1%): the raw quadrupole falls monotonically along the dial (q = −0.099 → −0.073 for β = 0 → 1), the binary amplitudes rise faster than it falls, and **every member lands at 4.0–5.8× the Cassini cap — the edge-ridden high-β members being lower bounds, since their amplitudes saturate the grid.** No mixing exponent escapes; Saturn's veto is β-blind. The derived running-β functions of §2.4 complete the picture: q = −0.090/−0.094 with their measured binary amplitudes gives Q₂ at **4.8× and 5.3× the cap** — inside the family band; the amplitude lock admits no exception, derived or fitted. [`calcs/stage5s_betaquad.py`, `calcs/stage5x_runquad.py`] The ambient-gated function closes the scan at its own solar gate (g(1.2 a₀) = 0.11, occupation-grade by construction): q = −0.099, within 0.4% of the occupation law's, and the measured binary amplitude α̂ = 1.060 ± 0.024 gives **Q₂ = 3.6×10⁻²⁶ s⁻² = 4.0× the cap** — the family's floor, recorded with no rescue claimed; the modified-inertia door of §8.2 remains the standing escape. [`calcs/stage6i_chaegate.py`]

One further escape was then constructed and *closed by the data*, which locks the tension at a deeper level. The quadrupole is a prediction not of low-acceleration dynamics per se but of the **vector composition** of internal and external fields — it is the P₂ anisotropy of the transition-shell phantom that Cassini forbids. A direction-blind ("scalar") EFE — the interpolating function responding to the ambient's magnitude only, as a thermodynamic bath parameter — would make that shell spherical, and by the shell theorem the inner solar system would feel nothing: the residual is the true Galactic tide, ~8×10⁻³¹ s⁻², four orders below the cap. No such theory class exists in print (scout-level), the galaxy-side EFE evidence is magnitude-level (Chae et al. 2021 state the field direction "has a minor effect," a 1/6 modulation their fits do not resolve), and the wide binaries turn out to be the only data probing the composition at the transition — a contest never before run. We ran it under pre-registered bars with the consequence map stated both ways: **the binaries demand the vector composition** — the natural scalar-sum tables ν(y+e) trail the sphericalized vector solve by 10.4 ± 2.0 per realization (0/6, all amplitudes interior: a valid shape rejection, the strongest single-model exclusion in our function contests), and the scalar variant of the ambient-gated function fails identically (−11.2 ± 1.9). Recorded without rescue: the scalar reading's temperature row would have been this paper's best (α̂ = 1.002 ± 0.049 — the parameter-free α = 1 dead-center — with a₀ = 1.20 ± 0.16, +1.0σ), which makes its shape rejection the more decisive. Three consequences. *First*, this is to our knowledge the first measurement of the EFE's composition character on any system (strictly: of the vector composition's angle-averaged radial profile, our likelihood being direction-blind by construction). *Second*, the Cassini tension is now **composition-locked as well as amplitude-locked**: the same binaries that calibrate the boost demand the vector structure in exactly the regime that sources the solar quadrupole — the conflict is data-forced, not formulation-chosen. *Third*, the job description for any Saturn-safe modified gravity sharpens to: reproduce the angle-averaged vector-EFE radial profile at e ≈ 1.2 a₀ while suppressing its P₂ component — a target an "isotropized-kernel" theory could meet by construction, but only as an ad hoc posit unless derived; the modified-inertia door of §8.2, which meets it naturally, remains the standing escape. [`calcs/stage6w_scalarefe.py`]

### 8.2 Modified inertia versus modified gravity, on the binaries themselves

Milgrom's modified-inertia (MI) formulations make the low-acceleration boost a functional of the trajectory rather than of the local field; circular orbits coincide with modified gravity (MG), eccentric orbits differ, and the time-nonlocal external-field effect need not produce the capped quadrupole (Milgrom 2011, 2023 — the principle stated without a closed form, the 2023 paper noting that modified inertia predicts "possibly a stronger external-field effect" on very wide binaries; Desmond et al. explicitly propose the quadrupole as the MI/MG diagnostic). Our dataset — with its fitted eccentricity mixture and 2D likelihood — is the natural instrument, and to our knowledge this discrimination had not been run on binaries. Nearest art, all on rotation curves: Petersen & Lelli (2020) — "a first attempt," 15 SPARC galaxies, modified gravity mildly favored within 1.5σ — and Chae (2022), whose inner-versus-outer rotation-curve test disfavors current modified-inertia proposals at 6.9σ; on binaries, McCulloch & Lucio (2019) test quantised inertia, a different framework, without the MI-vs-MG contrast.

Because a per-orbit global boost is exactly Kepler dynamics with G_eff = B·G, MI populations run through the identical machinery (the engine's Newtonian mode with rescaled mass; ṽ normalized by the true mass), making the likelihood difference pure theory. We bracket the unspecified MI prescription two ways (boost set at the semi-major-axis acceleration, or at the exact Kepler time-average ⟨1/r²⟩) and bracket its external-field treatment two ways (the MG-derived suppression tables; or none, per the naive frequency-decoupling reading). Across six population realizations against the stored MG twins:

| MI bracket | mean Δ lnL (MI − MG) ± SE, simple / occupation | verdict |
|---|---|---|
| time-averaged, EFE on | −3.5 ± 3.3 / −0.8 ± 2.5 | **statistical tie with MG** |
| a-scale, EFE on | −9.3 ± 2.3 / −3.1 ± 2.6 | mild MG lean (one prescription, one law) |
| time-averaged, no EFE | −25.2 ± 3.4 / −19.5 ± 2.6 | excluded |
| a-scale, no EFE | −27.9 ± 2.5 / −21.5 ± 2.2 | excluded |

(A single-realization version of this test misleadingly favored MG in all comparisons; the budget corrected it — Appendix A, item 10.) The binaries thus **decisively demand the external-field suppression** — the no-EFE brackets fail twelve times out of twelve, their fitted amplitude collapsing to α̂ ≈ 0.5–0.6, i.e. to the field-suppressed value — and **decisively reject Newton under every model class** (+71 to +108, 24/24 fits), but cannot distinguish local from trajectory-global boosts once the suppression amplitude is present. The data pin the *amplitude* while remaining agnostic on its *mechanism*.

**The paradox and its open door.** Modified gravity fits the binaries and breaks Saturn; naive isolated modified inertia survives Saturn and breaks on the binaries; but external-field-respecting modified inertia matches the binaries *and* need not produce the quadrupole. The surviving theory space, squeezed from both sides by this paper's own results, is: (i) a modified-gravity completion whose quadrupole is screened ~4× below the AQUAL/QUMOND value while preserving the binary boost — not currently constructed; (ii) a time-nonlocal modified inertia whose emergent external-field effect supplies the suppression amplitude the binaries demand — also not currently constructed (our brackets are its adiabatic limits); or (iii) a systematic not yet imagined, kept honestly alive by (i) and (ii)'s nonexistence. [`calcs/stage4l_mi_runner.py`]

The door was then quantified with the paper's own final function, in both directions. *Saturn's side is arithmetic*: in the trajectory formulation the anomaly attaches to the worldline's own occupation, and Saturn's worldline sits at y ≈ 5.4×10⁵ — occupation argument u ≈ 1.06×10³, anomaly ν − 1 ~ 10^(−461), against a Cassini-equivalent sensitivity of 6.7×10⁻¹¹: **the trajectory observable sits 451 orders of magnitude under the bound**, because the transition shell that sources the field-formulation quadrupole never exists on Saturn's trajectory; the residual observable is the ordinary Galactic tide, itself 4×10³ below the cap. Two corollaries: trajectories that *do* cross r_M ≈ 7×10³ AU (Oort-cloud bodies, extreme-aphelion comets) sample the transition and remain the solar system's residual probe; and the escape is *tail-selective* — a power-law screening tail would leave an r-dependent effective G at the 2×10⁻⁶ level at Saturn (an order-of-magnitude statement, ephemeris-dead by orders), so the trajectory door stands open only for Boltzmann-screened functions, precisely the screening class the data of §4 selected. *The binaries' side is measured and honestly short of a tie*: the per-orbit proxy of the gated function trails its field twin by 8.4 ± ~3 units over the pre-registered seed pair (−11.3 and −5.5, one amplitude edge-ridden at α̂ = 2, the α = 0 bit-identity gate exact) — between the pre-registered "tie" and "narrows" thresholds, in the same band as the sharpened-function penalty, and with the amplitude inflation (α̂ = 1.55–2.0 against the field fit's 1.06) pointing at proxy-discretization loss without proving it. The standard-law tie above does not automatically transfer to the gated function; distinguishing the proxy's crudeness from a genuine class cost needs the budget-grade seed set and a beyond-proxy trajectory implementation. [`calcs/stage7g_trajsaturn.py`]

Both were then supplied, with the mechanism named in advance. The proxy evaluates the boost *at* the time-averaged acceleration, B(⟨g⟩); the honest adiabatic trajectory functional is the time-average *of* the boost, ⟨B(g)⟩ — and since eccentric orbits linger at apoapsis, where the acceleration is low and the boost high, Jensen's inequality makes the averaged prescription strictly boostier for this population (a fifth of it near-parabolic; the gate block measures the recovery at +0.08 for e = 0.9 at the transition). The signed pre-registered prediction — boost recovered, amplitude deflated, gap closed — landed on all three counts: over the six-realization budget (the two-seed result triggered the pre-registered extension), **the trajectory-averaged gated function ties its field twin at −2.9 ± 3.5, sign-mixed across seeds, with every amplitude interior and α̂ deflated from the proxy's 1.55–2.0 to 1.272 ± 0.038** — the same tie grade as the standard-law bracket, with the α = 0 bit-identity exact throughout. The 7G gap was the proxy's discretization, not a property of the class. The combined statement is the strongest this paper can make about Saturn: **the trajectory formulation of the derived function fits the wide binaries exactly as well as the field formulation and passes Cassini by 451 orders of magnitude — the quadrupole tension is a property of the field formulation, not of the measured function** — with the honest residuals attached: a ~20% amplitude recalibration between formulations (1.27 against 1.06 — prescription-level, not fit-quality-level), the adiabatic average still an MI bracket rather than a full nonlocal theory, the field-formulation tension standing in the world table for the field reading, and DR4 eccentricity resolution as the formulation kill test. [`calcs/stage7h_miavg.py`]

## 9. Predictions and program

### 9.1 The scorecard

Every parameter-free prediction of the thermal/oscillator reading, against its measured value ([`calcs/stage4v_scorecard.py`]; the wide-binary a₀ rows translate the §6.3 amplitude through the external-field tables, dln(B−1)/dln a₀ = 0.78/0.92, and share the §6.3 systematics):

| quantity | measured | predicted | pull |
|---|---|---|---|
| a₀ — SPARC screening fit (§3) | (1.05 ± 0.10)×10⁻¹⁰ | cH₀/2π = 1.04×10⁻¹⁰ (Planck) | +0.1σ |
| a₀ — joint SPARC+lensing (§4.5) | (1.00 ± 0.09)×10⁻¹⁰ | 〃 | −0.5σ |
| a₀ — wide binaries, translated (§6.3) | (1.48 ± 0.18) / (1.37 ± 0.17)×10⁻¹⁰ | 〃 | +2.5σ / +1.9σ |
| screening index p (§3) | 0.578 +0.121/−0.115 (flat); ≈ 0.65, Δ = 56 over ½ (hierarchical, §4.4) | ½ | +0.7σ flat; tail sharper than ½ hierarchically |
| zero-point coefficient c₁ — galaxies (§4.3) | profile 0.26–0.45 (M/L-treatment span); bootstrap ≈ 0.4 ± 0.3 | ½ | 0 excluded; ¼ vs ½ open (the hier ¼-function lead is ~75% tail, §4.4) |
| zero-point coefficient c₁ — binaries (§6.4) | fenced-model indication 0.37–0.50; ERASED under the landed posterior (7J-d: λ profile flat, no constraint) | ½ | fence-conditional only |
| binary amplitude α (§6.3) | 1.18 ± 0.11 / 1.13 ± 0.13 | 1 | +1.6σ / +1.0σ |
| perpendicular ceiling edge (§7.2) | terminates at ≈1.65 | √(2×1.36) = 1.65 | P = 0.62 |
| rung 2: c₂ (§4.5) | unresolved | 1/12 | 0.1σ reach |
| mode count N (§4.6) | 20–60 if thermal; identity unresolved at SPARC depth | — | — |

Five dials measured; all sit within ≲1.6σ of the oscillator's values against the Planck-calibrated temperature. The binary amplitude is the consistent high-side outlier — its a₀ translation lands ~2σ above cH₀/2π for *either* H₀ calibration — and we carry it as the reading's sharpest internal tension rather than absorbing it: either the external-field calibration owns it (§6.2's ±0.05 a₀ covers part), or the binaries are telling us the deep amplitude is not fully H₀-locked, which is exactly what the a₀ ∝ H(z) test (below) will eventually adjudicate. Two late additions strengthen the temperature rows: the full-hierarchy galaxy fits with measured vertical priors land on a₀ = 1.04–1.05 ×10⁻¹⁰ for both leading laws (+0.1σ; §4.4), while under the *sharpened* functions the binary translation worsens to +4.9σ (§6.4) — the a₀ ladder itself votes for the ½-branch transition on the binary side. Under the ambient-gated function of §2.4, which restores occupation-grade screening at the binaries' environment, the binary translation improves to its best value in this paper: **a₀ = 1.28 ± 0.15 (+1.6σ)** (§6.4).

### 9.2 The falsifiers

The framework's falsifiers, with their current reach stated:

1. **Rung 2 of the Bernoulli ladder** (1/12 vs 1/8) decides the quantum-versus-classical bath question of §2.3. Present reach: 0.1σ (§4.5). Requirements: lensing stellar-mass cross-calibration at ~0.02 dex, or the Gaia DR4 distribution-level test.
2. **The perpendicular ceiling** (§7.2) becomes a sharp near-term prediction: DR4 multiplies the forbidden band's occupancy ~tenfold; the band fills and the cliff stays at √(2×1.36), or the boosted reading fails.
3. **The MI/MG discrimination** sharpens two ways: at DR4 statistics the eccentricity-resolved boost separates the brackets we could not; and on the theory side, either ghost of §8.2 — the screened-MG completion or the nonlocal-MI construction — is a concrete target whose success or failure is decisive. The Cassini quadrupole remains the standing arbiter (Desmond et al. 2024).
4. **a₀ ∝ H(z)**: the thermal reading ties the acceleration scale to the horizon temperature; high-z rotation curves (JWST/ALMA-era) test whether a₀ tracks H. Present high-z kinematics are dispersion-dominated and cannot yet decide.
5. **The reconciliation prescription** (§7.3) is itself a falsifiable program: any analysis of this catalog that bounds companions photometrically, retains a deep anchor bin, and convolves noise should find the anomaly at Δ lnL ≥ +30 regardless of its other choices. We invite its execution on the published pipelines.
6. **The bath's second moment** (§4.6): the fluctuation channel is new and cheap, and its current state is an identifiability boundary — the anchored-distance subsample improves it without resolving it (more anchored galaxies, or a survey-grade distance ladder, would). The marginalization-proof transition-region excess is now characterized as *point-level* — surviving every per-galaxy channel tried — so its remaining candidates are physics or observation localized at the RAR knee (beam smearing at the turnover; intrinsic transition structure; or, in the thermal reading, the transition shell where mode character changes). The deep monotone trend's environmental rival has now been executed and excluded at published field amplitudes (β ≲ 0.05 of the collinear max-clustering pattern, §4.6); the thermal candidate stands as the survivor, its amplitude sample-dependent, its decision against per-galaxy distance structure still awaiting anchors.
7. **The mixing exponent's regime pattern** (§2.4, §4.4, §6.4) is the construction target in falsifiable form: β = 0 wherever the transition and the deep zero point are measured (binaries: β < 0.03 at 1σ, 24/24; SPARC's own ultra-deep arm), β ≈ ½–¾ in the screening tail alone — unreachable by any single member, since the family locks c₁·p_tail = ¼ while the data ask for ~0.3. A bath construction must therefore deliver a *running* admixture — β rising from 0 toward ~½ as the occupation drops through the transition, which would fix the asymptotic tail at the geometric-mean p = ¾ — or an equivalent lock-breaking shape. The program of instances has now been executed to its end state (§2.4, §4.4, §6.4): the spontaneous-fraction weightings lead every hierarchical galaxy treatment monotonically while the binaries reject every *fixed* sharpened function through the external field; the pointwise drive-weighted rule is excluded; and the **ambient-gated bath** — β = [n_amb/(1+n_amb)]²·½/(2ν−1)², derived from stimulated reservoir assistance, zero parameters — is the only function among those tested to pass both systems (binaries −0.9 ± 2.7 with a₀ at +1.6σ; galaxies −59 under the controlled treatment; both bars pre-registered, the plain-treatment miss disclosed). The reconciliation is a design property of the function, not a measurement (§2.4); what remains falsifiable is the non-built-in content below. The falsifiable structure this leaves is concrete: **the tail exponent is an environmental observable, p = ½ + ¼·[n_amb/(1+n_amb)]².** The gate predicts that wide binaries in *weak* ambient fields (halo samples, outer-disk pairs; Gaia DR4 reach) sharpen toward p ≈ 0.69, that environment-resolved SPARC outskirts soften along g_amb(e) (the Chae et al. per-galaxy fields give this a first, power-limited in-sample test), and that the deep series is environment-invariant through c₂ with the entire ambient dependence at c₃ = −g/16. The modified-inertia alternative predicts the split follows *orbit class* rather than environment (eccentricity-resolved boosts at DR4 precision, §9.2 item 3). Either signature decides; the gate's post-hoc origin makes these out-of-sample tests, not confirmations, and we state that plainly. Two of the gate's structural tests have since been run in-sample and passed (§2.4): the leg count — readable off the deep series because the Bernoulli ladder breaks at rung c_{L+1} — is point-preferred at **L = 2** (the one-leg alternative disfavored at lean grade, 29/40; two-versus-three legs unresolved under the population bootstrap — correction #13), and the measured Chae environments, substituted for the fiducial, carried the plain-treatment fit across the previously missed bar. What remains falsifiable on this axis: the new fourth rung, c₄ = s²/192 − 1/720 — a predicted *sign flip* of the fourth deep coefficient at galaxy ambients (+0.0025 versus the occupation law's −0.0014) — sits below current resolving power and is on record for deeper data, and the environment-resolved p(g_amb) trend across the Chae dial (isolated galaxies sharper) is the in-sample ordering DR4-era samples can sharpen. Since that assessment, the gate's status has changed twice over (§2.4): its *necessity* is now proven by triangulation — the derived resolution bath (local-in-y running, no gate) is galaxy-best-in-class yet binary-shape-rejected, the pointwise weighting was already excluded, and only the two-factor rule survives both systems — and its *form* is now derived, the KMS detailed-balance cost of two borrowed ambient quanta, selected uniquely against every natural rival weight by already-measured facts. The falsifier ledger gains two rungs from that derivation: the ambient-frequency convention (source versus dressed) splits by Δp ≈ 0.03 at weak-ambient binaries — a DR4 discriminator — and the Boltzmann cost at T_dS = H(z)/2π obliges the tail exponent to run with redshift (p_gal 0.688 → 0.702 by z = 1 at fixed physical environment), tying the gate's fate to the a₀ ∝ H(z) kill test.

## 10. Conclusions

We set out to test a specific, elegant reading of the radial acceleration relation — gravity's low-acceleration boost as the thermal occupation of a horizon bath — and to measure the wide-binary anomaly with systematics treated as measurements rather than priors. The program returned:

- a fit-level confirmation of the anomaly at strength one — **α = 1.18 ± 0.11 / 1.13 ± 0.13** at the physical external field, Newton excluded in 2000/2000 bootstrap contests and under five distinct boost-model classes — that the program's own final instrument then *unseated*: under companion marginalization the fenced +99–110 Newtonian rejection is dead under any *smooth* companion prior (the finely sampled α_marg(anchor) curve is nearly flat at 0.2–0.3 with Newton within +2…+4 across 0.06–0.30; the detection returns only behind a hard exclusion of f ≥ 0.2; §6.3), with the verdict label prior-dependent at the margins — COMPANION-WIN at our (since-retracted, correction #18) measured host prior, AMBIGUOUS at the literature anchoring — and the revival mechanism located in the model's missing width channel rather than in any subsystem-rate measurement; the α row retained fence-conditional, co-quoted with the curve — and then, in the deciders themselves, partially *revived*: parameterizing the width channel flattens the anchor curve entirely (spans 0.01–0.03 across 0.06–0.34) at α_marg = 0.70–0.74 with ΔlnL(Newton) = +23, prior-independent, AMBIGUOUS-CARRIED at 1.2–1.8 below the pre-registered detection bar, while the direction channel is shown to veto a phantom ṽ-only boost of half that size in the width-less configuration (§6.3);
- a nuisance sector with externally validated identities, anchored by the **w_rad = 0.20 ↔ Hwang et al. superthermal (20–22%)** cross-validation and a measured 2.4% mass error;
- a per-pair census of the field's founding forbidden-band discriminant, the **perpendicular velocity ceiling** — eleven pairs where bound Newtonian dynamics permits none, terminating at the boosted escape edge, released pair-by-pair for independent re-counting (its leakage null now awaiting re-derivation at the measured multiplicity, §6.3 — with the cut-stable median, the anomaly's surviving evidence channel);
- the first coefficient-level tests of the thermal reading: the zero-point coefficient measured in the galaxies and conditionally indicated in the binaries (the "measured twice, in disconnected systems" framing is suspended with the fit-level binary sector, §6.3) — **galaxies: profile 0.26–0.45 across M/L treatments, bootstrap ≈ 0.4 ± 0.3, the 0-branch excluded in every treatment; binaries: 0.37–0.50 at two-realization indication grade, fence-conditional, the 0-branch rejected at ΔlnL ≈ 20 per realization** — the ¼-versus-½ digit treatment-split, *decomposed* (three-quarters screening-tail, p ≈ 0.6–0.65 across treatments; the binaries veto the ¼ transition and the vertical-channel control collapses its galaxy case), and enriched by a parameter-free geometric-mean construction (c₁ = ⅓, c₂ = 1/12) that leads every hierarchical galaxy treatment while the binaries hold the ½ transition and its milder a₀ translation — the whole ladder then compressed into one measured mixing exponent (ω = ω_source^(1−β)·ω_total^β, exact ladder c₁·p_tail = ¼): **the binaries pin β < 0.03 at 1σ in 24/24 contests, the galaxies profile to 0.45–0.64, and the decomposition shows the split runs through regimes, not datasets — the transition and the deep zero point hold β = 0 everywhere, the screening tail alone dissents — a regime-running exponent no member can accommodate, with Saturn's veto β-blind (4.0–5.8× Cassini across the dial)**; the pattern then *derived* rather than fitted — the spontaneous-emission fraction gives zero-parameter running functions (c₁ = ½ and p = ¾ exact in advance; both Bernoulli rungs at two legs) that lead every hierarchical galaxy treatment monotonically, the two-leg member at −64 vertical with a 37/40 bootstrap — against which the binaries reject every fixed sharpened function through the external field (−5 to −10 in likelihood, +4 to +6σ in temperature; the pointwise configuration rule excluded at solver level) — **and the split then closed by the ambient-gated bath, β = [n_amb/(1+n_amb)]²·½/(2ν−1)²: admixture = local quantumness × ambient classicality, zero parameters, post-dicting both measured tail exponents, accepted by the binaries (−0.9 ± 2.7; a₀ at +1.6σ, the paper's least-strained a₀ row) while holding −59 on the controlled-treatment galaxies — the only function among the thirteen tested to pass both systems — a reconciliation that is a design property of the function, not a measurement, with its evidence confined to the non-built-in content and its out-of-sample tests stated — a flag its subsequent history has largely retired: the gate's necessity was proven by the resolution-bath triangulation and its form derived as the KMS cost of borrowed ambient quanta, unique against every rival weight under already-measured facts, leaving one reading-grade seam (the borrowing microphysics)** — then formalized as β = ½·[q_loc·s_amb]^L with the local share fixed by the exact (2n+1) dispersive pull and the leg count *read* off the deep Bernoulli-break rung (**L = 2 point-preferred; the one-leg alternative disfavored at lean grade and two-versus-three unresolved under the population bootstrap — correction #13**), while the measured Chae environments, substituted for the fiducial gate, improved every galaxy treatment at zero parameters and retro-resolved the one missed bar (a gain later reattributed to gate heterogeneity by the permutation control, correction #14 — the ordering claim is out-of-sample) (plain −100.5/−105.9 across ≤ −100); rung 2 honestly out of reach, the identity's priority correctly assigned to Cadoni & Tuveri — and a first second-moment constraint: if the excess low-acceleration scatter is thermal, the bath averages N ~ 20–60 modes — surviving disk-M/L marginalization, not identifiable against per-galaxy distance/inclination freedom at SPARC depth (the boundary is demonstrated, not assumed), with floorless baths excluded and a marginalization-proof transition-region scatter excess left standing;
- a quantitative anatomy of the field's disagreement whose verdict-bearing claim we then retracted (correction #16, one clause re-graded under #18): the ablation map stands, but its "manufactured by a photometry-forbidden fraction" reading rested on flagged-rate-as-bound logic no completeness accounting supports; the companion freedom was directionally legitimate and the marginalized re-fit ruled against our own fit-level signal — while the completeness instrument built to decide the question was itself corrected by its within-pair validation (#18), leaving literature subsystem rates primary, our old fence ~1.5σ low rather than absurd, the fitted 69% excluded under every reading, and the shared wobble amplitude law missing photocenter cancellation — the field's dispute ends where both sides' critics said it would, in the companion sector, now with instruments instead of fences;
- and a self-inflicted, load-bearing tension: our own boost, through our own solver, predicts a solar quadrupole **4× above Cassini's bound** for every modified-gravity reading — with the external-field-respecting modified-inertia branch surviving as the Saturn-safe alternative that the binaries cannot currently distinguish from modified gravity. This bullet's distinct contribution — a *binary-calibrated* quadrupole, immune to the mass-to-light and bulge mitigations available against the rotation-curve version — is suspended with the binary amplitude (§6.3): the tension itself reverts to its galaxy-calibrated form (Desmond–Hees–Famaey 2024), which our solver cross-validation supports but no longer independently strengthens.

Our credence accounting after the final stage and its review rounds: the *wide-binary* anomaly, ~70% likely real physics at this program's peak, stands at **~45%** — the durable negative is that the fenced +99–110 Newtonian rejection is dead at every defensible companion-prior anchoring (that alone keeps it well below the peak); the partial recovery from the first post-verdict ~35% reflects that the exact-zero collapse was partly manufactured by our own completeness instrument's over-attributed scale (correction #18), leaving the verdict label AMBIGUOUS at the now-primary literature anchoring pending the pre-registered six-realization budget. The residual probability is carried by what the winning Newtonian model still cannot do (its own 60–116 log-likelihood misfit; the cut-stable median; the forbidden-band census), with the attribution instruments specified and queued. That number was then **frozen** under a review-adopted cadence rule until the two decisive instruments reported — and both now have, so the pre-committed map re-affirms **~45%** on its AMBIGUOUS branch rather than by inertia: the width-channel round returned an interior α_marg = 0.70–0.74 at ΔlnL(Newton) = +23.2–23.8 — 1.2–1.8 short of the pre-registered detection bar — on a curve flat to 0.01–0.03 across every companion anchor 0.06–0.34 (the fenced rejection stays dead; the honest zero stays confined to the width-less slice), and the direction-channel decomposition confirmed the two-dimensional method's operation (a phantom ṽ-only α ≈ 0.5 vetoed at α grade in the four-absorber configuration; absorber-pinning at nuisance grade in the full model). The standing statement is now the width-model curve, quoted with its dependencies: an interior boost near 0.7 at +23 against Newton, independent of the companion prior, conditional on the per-system width channel being the right error model and on the occupation arm's unresolved noise-grid edge; the measured subsystem rate (0.29–0.32 host per component, ~3× field, unshipped pending its numerical certificate) sharpens the named tension between what the sky hosts and what the kinematics want; and the queued instruments — the certificate run, the anchored re-read, the arm suite with coherence-degraded injections — decide whether the +23 hardens toward the bar or dissolves into the width sector. The galaxy-side program — the coefficient measurements, the screening index, the second-moment constraint, the function ledger — is untouched by that verdict and carries its own stated grades. What the anomaly *is*, if real, remains genuinely open: the two simplest realizations are each wounded by a different one of our results, and the surviving candidates do not yet exist as derived theories — though one now exists as a function-level rule: the ambient-gated bath, zero parameters, passing every in-sample number in both systems — its last missed bar resolved by measured environments, its process order point-preferred (L = 2, the round trip — lean grade under the population bootstrap), its local weight anchored in the exact dispersive (2n+1) pull — with its fate assigned to stated out-of-sample tests rather than to our preference. We regard this as the correct state for a measurement program to leave its field in — the easy explanations executed, the discriminating observables identified, one of our own headline results killed by our own instrument and reported at the same volume as the detections, and every step reproducible.

---

## Appendix A: the transparency record (eighteen corrections)

This program logged every correction and retraction in its lab notebook at the time it occurred. We reproduce the list because it is the paper's credibility spine: a reader can weigh our positive claims against our demonstrated willingness to kill our own results.

1. **Axial-angle double-fold.** An early orientation statistic returned R = 2/π exactly — a geometry bug (double-folded angle), caught by its suspicious round value.
2. **Non-conservative EFE recipe.** A first external-field implementation pumped orbital energy (ṽ → 7.4 over ten orbits); replaced by the conservative quadrature recipe, gated on energy conservation.
3. **Acceleration-scale and external-field conventions.** Multiple a₀/IC convention biases, culminating in the cross-formulation error of §6.2 (an AQUAL-total field fed to a QUMOND solver for five fit generations); resolved by our own RAR inversion and validated externally in §8.1.
4. **The v1 "α = 1 bullseye."** An early headline localization was a grid-edge artifact; retracted, and edge-flagging added to all subsequent fits.
5. **The mass-error hypothesis.** The ṽ-broadening was attributed to photometric mass errors until the error was measured (σ_m = 0.024, twelve times too small); refuted.
6. **The circular-mixture reading.** The high-γ arm of the U-shape was read as a circular sub-population; the joint 2D fit vetoed it (w_circ → 0; circularity narrows ṽ).
7. **The identity priority.** The Bose–Einstein occupation identity, its horizon derivation, and a₀ = H/2π were claimed as apparently novel until a primary-source read of Cadoni & Tuveri (2019) showed all three published there; every such claim was retracted and this paper reframed as the framework's test.
8. **Raw-χ² inflation.** The 198–200/200 branch verdict and an apparent within-branch "simple lean" were shown to be likelihood-model dependent; the scatter-marginalized treatment deflates the former to a robust lean and reverses the latter into noise (three galaxies).
9. **The U-shape priority.** The superthermal wide-binary eccentricity distribution and its v-r-angle signature were published by Hwang, Ting & Zakamska (2022), whose headline number our own notebook had quoted while claiming novelty; retracted, and converted into the cross-validation of §7.1.
10. **The single-seed MI sweep.** A first modified-inertia comparison favored modified gravity in all eight contests; the six-seed budget showed this was realization luck, and the EFE-respecting MI brackets are statistical ties.
11. **The ceiling priority.** The √2 Newtonian bound and the discriminating power of the band above it were established at the field's founding (Pittordis & Sutherland 2018; Banik & Zhao 2018) and named "the key discriminant" by Pittordis & Sutherland (2023); our notebook and first draft nevertheless described the ceiling test as new. Corrected at the pre-circulation verification pass: what is new is the perpendicular-column census and the edge-termination statistic, and §7.2 now leads with the crediting.
12. **The perspective omission.** No spherical-projection (systemic-RV perspective) correction was applied anywhere in this program, despite the term being radial-directed and growing as s^1.5 — shaped like the signal. Caught at the pre-circulation full read of Cookson et al. (2026), whose checklist names it; audited the same day (`stage4q_perspective.py`): the effect is present in our data at its predicted size (slope 0.92 vs 1), accounts for at most 1.6 points of the 8.6-point anchor excess (Newton+perspective predicts 1.016), and the immune perpendicular component boosts *more* (1.151) — the anchor is corrected 1.086 → 1.078 and the corrected-velocity re-fit (Stage 4R, six paired seeds) moved α̂ by −0.01 ± 0.01 (simple) / −0.02 ± 0.02 (occupation) — conclusions intact. The audit also reproduced the §7.2 census to ±1 with an independent angle implementation.
13. **The leg-count "measurement."** The v2.4 draft claimed the grammar's process order was *measured* (L = 2 beating both flanking integers on the hierarchical galaxies, §2.4). The same-day 40-rep galaxy-population bootstrap (`stage6l_legboot.py`) deflated it: the L=1 rejection holds in 29/40 resamples (a lean), L=2-versus-L=3 is unresolved (21/40), and L=2 is strictly best in only 10/40 — the deep arm that carries the discrimination is populated too thinly at 153 galaxies for a population-grade reading (the §7.1 realization-systematic lesson recurring at the function-structure level). Softened everywhere to a point-preference with a lean-grade one-leg rejection. Unaffected: the Bernoulli-break↔leg-count correspondence (exact mathematics), and the measured-ambient results, which carry their own independent 37/40 bootstrap.
14. **The measured-ambient attribution.** The improvement from substituting Chae et al.'s per-galaxy environments for the global gate (−59.05 → −61.68 controlled; the plain-treatment bar crossing) was read as environmental *ordering* information. The pre-registered permutation control (`stage6z_ordering.py`) showed otherwise: the true assignment loses to all eight gate-preserving shuffles (−61.62 against a null of −62.91 ± 0.80) — the gain is gate *heterogeneity*, generic under permutation, not the physical ordering (the s-flat-control lesson of §7 recurring at the galaxy level). The ANTI verdict carries its stated weak grade (empirical p ≈ 0.11, one-in-nine). Reattributed everywhere; the ambient-ordering prediction p(e_N) moves fully out-of-sample (environment-resolved DR4-era samples), where it remains the rule's signed falsifier. Unaffected: the gate's amplitude-level support — the two-system split, the binary acceptance, the tail postdictions (median-gate level), the p ≤ ¾ ceiling, and the single-collective-mode exclusion theorem.
15. **The cut-survival immunity inference.** Stage 7I concluded that the median boost's survival of the strict multiplicity cut refuted a companion carrier ("companions-as-median-carrier refuted"). The completeness measurement (§6.3) showed the argument was under-powered: the photometric flag catches only 41% of companions, and because the cut also removes non-companion pairs, the companion *fraction* barely changes across it (0.51 → ~0.47) — a fully companion-carried median would survive the cut within the confidence intervals. The inference is retracted; the median values stand as measurements; their attribution is assigned to the forward-modeled median at the measured-multiplicity winning cell, the declared decisive instrument. Unaffected: parts 2 and 3 of the same investigation (the f_pm and w_rad refutations), and the COMPANION-DIRECTION verdict itself, which the marginalized analysis then confirmed at full strength. Provenance note on the photocenter correction that made the marginalization decisive: it was registered after an interim misspecified-prior verdict had shown the companion direction and before the as-published conditional was explicitly read — sequence disclosed in §6.3, with the as-published conditional co-quoted everywhere as the guard.
16. **The "manufactured significance" claim.** Section 7.3 claimed the published 16σ-Newton result was manufactured by a companion fraction "the photometry forbids," citing our 12% flagged rate against their fitted 69%. The retraction's valid core: that logic treated a flagged rate as a bound with no completeness accounting, the companion freedom was directionally legitimate, and the marginalized re-fit returned the Newton-plus-companions preference at fit level. The "manufactured" framing is retracted in its normative sense for all three ablation legs (the arithmetic of the ablation table stands). One clause of the first-issued version — "Banik et al. were substantially closer to the truth than our fenced fits" — was conditioned on Part A's host-rate scale and is re-graded under item 18: at literature subsystem rates our old fence is ~1.5σ low, nearer the truth than their 0.69, which stays excluded under every reading. Still-standing components of the critique: the 0.69 exclusion; the shared wobble amplitude law's omission of photocenter cancellation; the sub-error-binning defect (Hernandez et al.'s finding). §7.3 is retitled and rewritten accordingly.
17. **The prior-axis exemption from our own criterion.** Amendment 6 registered a boundary-free AMBIGUOUS criterion (interior α_marg, or marginal gap > −5 lnL) for the seed axis — and the same day's summary claimed "COMPANION-WIN fires at every plausible prior anchoring" while the literature-anchored cell showed exactly the interior-α signature the criterion names. Reviewer-caught, same-day. Fixed by adopting the criterion on both axes and the reviewer's presentation: α_marg as a function of the companion-prior anchor is the primary result, verdict labels are annotations on the curve — COMPANION-WIN at the measured prior, AMBIGUOUS at the literature prior; the anchoring-invariant statement (the fenced Newtonian rejection is dead everywhere) is the durable one.
18. **Part A's absolute scale.** The completeness stage's host multiplicity (f_host = 0.42–0.57; flag completeness C = 0.41) was retracted by its own second validation, a reviewer-designed over-attribution test the injection gate could not perform: the within-pair correlation of overluminosity residuals is ρ ≈ +0.47 on the pre-registered core slice (+0.39 with component colors forced apart; attenuation-corrected; N ≈ 5,400 pairs) — wide-pair components are coeval and co-chemical, so roughly half the single-star spread is common-mode (metallicity/age/extinction) that the one-dimensional mixture attributed to companions. The bias direction (C low, f_host high → permissive prior → α → 0) is the one that manufactures the companion verdict, and the factor ~2–3 tension with published wide-binary subsystem rates resolves in the literature's favor. Retained: the completeness formalism, the photocenter physics, and the within-pair coherence measurement itself. The repair — a joint two-dimensional (δ₁, δ₂) mixture separating common-mode from per-component displacement geometrically — is queued; the literature-anchored prior is primary until it reports. Items 15 and 16 each carry one clause re-graded by this correction, marked in place; item 15's retraction of the cut-survival argument is now *undetermined* rather than settled (at high true C the argument regains power), pending the repaired completeness.

All flagged verification items were executed at the pre-circulation pass (2026-07; primary sources, verbatim retrieval, agent-fetched and spot-checked): the Chae (2023) external-field sentence is confirmed and quoted in §6.2; the arXiv:2304.00576 byline is Jones-Smith & Mathur; the Pittordis–Sutherland–Shepherd (2025) triple-population caveat is quoted per the published text; the Cadoni–Tuveri citation record (31 entries, 24 retrieved) contains no empirical test of their form; and the "Timeflow Gravity" construction (Trofimov 2026, CQG) was checked to abstract depth — a vacuum phase-interference mechanism distinct from our §2.3 identity — with its paywalled full text noted as the single unexecuted read.

## Appendix B: reproducibility

Every quantitative claim in this paper is produced by a named script in the public repository (github.com/TheCake/thermal-horizon-rar), which also contains the chronological lab notebook (NOTES) with all eighteen corrections logged in place, and a curated measurement ledger (`LEDGER.csv`, format and rules in `LEDGER.md`): every headline number with provenance (stage, script, output, data dependence) and supersession status — superseded and retracted values stay visibly marked so they cannot be silently resurrected. `calcs/stage6q_worldtable.py` audits the ledger mechanically (provenance on disk, supersession resolution, verbatim value spot-checks against stage outputs) and assembles the world table scoring every candidate law against every ledger test. Large datasets are re-fetched by documented URLs: SPARC (Lelli et al. 2016; Zenodo), the El-Badry et al. (2021) EDR3 binary catalog (Zenodo), the Brouwer et al. (2021) KiDS release (KiDS portal), and the Mistele et al. (2024) table (arXiv source, extracted verbatim). Key claim→script mappings: screening index — `sparc_rar_fit.py`, `stage4h_p_ml.py`; branch and rung tests — `stage4a`–`stage4f`; the ĉ₁ measurement — `stage4s_c1fit.py` (hierarchical: `stage4z_hierc1.py`); the second moment — `stage4t`–`stage4y`, `stage5a`; the environmental control — `stage5b_envtest.py`, `stage5e_envtest_conv.py` (Chae et al. 2021 Table 3 extracted to `data/chae2021_table3.csv`); the hierarchical bath contest and its decomposition — `stage5c`, `stage5d_hierbath_conv.py`, `stage5g_tailtest.py`; boost anchor and stress tests — `stage2c`, `stage2d`, `stage4i`; hierarchical fits and budgets — `stage3o`, `stage3p` (+ `stage3u/3v` summaries); binary coefficient dial — `stage4x_binlam.py`, `stage5f_bootbin.py`, `stage5h_punified.py`; the mixing family — `stage5p_betafam.py` (ladder algebra: `stage5q_c3ladder.py`; binary profile: `stage5k_binprofile.py`, `stage5o_seeds.py`, `stage5r_binbeta.py`; regime decomposition: `stage5t_betasplit.py`; vertical control: `stage5m_hierv.py`, `stage5n_dvboot.py`); geometric-mean construction — `stage5j_gmboot.py`; the derived running-β functions — `stage5u_runbeta.py`, `stage5z_twoleg.py` (galaxies: `stage5v_rungal.py`, `stage6a_twogal.py`, bootstrap `stage6c_f4boot.py`; binaries: `stage5w_runbin.py`, `stage6b_twobin.py`; quadrupole: `stage5x_runquad.py`; κ/a₀: `stage5y_kladder.py`; drive-weighted control: `stage6d_drivebath.py`); the ambient-gated bath — `stage6e_ambgate.py` (galaxies: `stage6f_ambgal.py`; binaries + κ/a₀: `stage6g_ambbin.py`; grammar, leg-count ladders, and pre-registered bars: `stage6h_grammar.py`; the L contest, measured-ambient legs, and the AMB quadrupole record: `stage6i_chaegate.py`; bootstrap: `stage6j_ambboot.py`); the resolution bath (horizon-side derivation + its two-system test) — `stage6r_resolution.py`, `stage6s_resngal.py`, `stage6t_resnbin.py`; the gate derivation and its untied-exponent falsifier — `stage6u_gatederiv.py`, `stage6v_untied.py`; the scalar-vs-vector EFE composition contest — `stage6w_scalarefe.py`; the borrowing-dynamics channel test — `stage6x_borrow.py`; a₀ ladder — `stage5l_ladder.py`; ceiling — `stage4j`, `stage4m`; quadrupole — `stage4k` (family scan: `stage5i_quadrupole2.py`, `stage5s_betaquad.py`); MI/MG — `stage4l_mi_runner.py`; reconciliation — `stage4n_banikstyle.py`; perspective audit — `stage4q_perspective.py`, `stage4q_ceilingcheck.py`; reviewer robustness rows and the strict-cut investigation — `stage7i_ablations.py`, `stage7i_material.py`, `stage7i_fpm.py`, `stage7i_sw.py`; companion completeness + the marginalized verdict + cube diagnostics — `stage7j_completeness.py`, `stage7j_marginal.py`, `stage7j_diag.py`. GPU population engine: `stage2b_population.py`; EFE solver: `qumond_efe_solver.py`.

## References (INSPIRE-verified at the 2026-07 pre-circulation pass)

- Banik, I. 2019, MNRAS 487, 5291 (arXiv:1902.01857)
- Banik, I., & Zhao, H. 2018, MNRAS 480, 2660 (arXiv:1805.12273)
- Banik, I., et al. 2024, MNRAS 527, 4573 (arXiv:2311.03436)
- Bekenstein, J. D., & Milgrom, M. 1984, ApJ 286, 7
- Blanchet, L., & Novak, J. 2011, MNRAS 412, 2530 (arXiv:1010.1349)
- Brouwer, M. M., et al. 2021, A&A 650, A113 (arXiv:2106.11677)
- Cadoni, M., & Tuveri, M. 2019, PRD 100, 024029 (arXiv:1904.11835)
- Chae, K.-H. 2022 (arXiv:2207.11069); 2023, ApJ 952, 128 (arXiv:2305.04613); 2024, ApJ 972, 186 (arXiv:2402.05720)
- Chae, K.-H., Lelli, F., Desmond, H., McGaugh, S. S., Li, P., & Schombert, J. M. 2020, ApJ 904, 51 (arXiv:2009.11525)
- Chae, K.-H., Desmond, H., Lelli, F., McGaugh, S. S., & Schombert, J. M. 2021, ApJ 921, 104 (arXiv:2109.04745) [Table 3 = per-galaxy environmental fields used in §4.6]
- Chae, K.-H., & Milgrom, M. 2022, ApJ 928, 24 (arXiv:2201.02109)
- Deser, S., & Levin, J. 1997, CQG 14, L163 (gr-qc/9706018)
- Desmond, H. 2023, MNRAS 525, 6130
- Desmond, H., Hees, A., & Famaey, B. 2024, MNRAS (arXiv:2401.04796)
- Cookson, S. A., Banik, I., El-Badry, K., Sutherland, W., Penoyre, Z., Pittordis, C., & Clarke, C. J. 2026, MNRAS (arXiv:2602.24035)
- El-Badry, K., Rix, H.-W., & Heintz, T. M. 2021, MNRAS 506, 2269 (arXiv:2101.05282)
- Famaey, B., & Binney, J. 2005, MNRAS 363, 603
- Famaey, B., & Durakovic, A. 2025 (arXiv:2501.17006)
- Hees, A., Folkner, W. M., Jacobson, R. A., & Park, R. S. 2014, PRD 89, 102002 (arXiv:1402.6950)
- Hernandez, X., Chae, K.-H., & Aguayo-Ortiz, A. 2024, MNRAS 533, 729 (arXiv:2312.03162)
- Hwang, H.-C., Ting, Y.-S., & Zakamska, N. L. 2022, MNRAS 512, 3383 (arXiv:2111.01789)
- Jones-Smith, K., & Mathur, H. 2023, AJ (arXiv:2304.00576)
- Lelli, F., McGaugh, S. S., & Schombert, J. M. 2016, AJ 152, 157 (SPARC)
- Lelli, F., McGaugh, S. S., Schombert, J. M., & Pawlowski, M. S. 2017, ApJ 836, 152 (arXiv:1610.08981)
- McCulloch, M., & Lucio, J. 2019 (arXiv:1908.01434)
- McGaugh, S. S., Lelli, F., & Schombert, J. M. 2016, PRL 117, 201101
- Milgrom, M. 1983, ApJ 270, 365
- Milgrom, M. 1999, Phys. Lett. A 253, 273 (astro-ph/9805346)
- Milgrom, M. 2010, MNRAS 403, 886 (arXiv:0911.5464) [QUMOND]
- Milgrom, M. 2011, Acta Phys. Pol. B 42, 2175 (arXiv:1111.1611); 2023 (arXiv:2310.14334)
- Mistele, T., McGaugh, S., Lelli, F., Schombert, J., & Li, P. 2024, JCAP 04, 020 (arXiv:2310.15248)
- Paranjape, A., & Sheth, R. K. 2022, MNRAS 517, 130 (arXiv:2112.00026)
- Pazy, E., & Argaman, N. 2012, PRD 85, 104021 (arXiv:1106.4108)
- Pazy, E. 2013 (arXiv:1302.4411)
- Pecaut, M. J., & Mamajek, E. E. 2013, ApJS 208, 9 (arXiv:1307.2657)
- Petersen, J., & Lelli, F. 2020, A&A 636, A56 (arXiv:2001.03348)
- Pittordis, C., & Sutherland, W. 2018, MNRAS 480, 1778 (arXiv:1711.10867); 2019, MNRAS 488, 4740 (arXiv:1905.09619); 2023, OJAp (arXiv:2205.02846)
- Pittordis, C., Sutherland, W., & Shepherd, P. 2025, OJAp (arXiv:2504.07569)
- Raghavan, D., et al. 2010, ApJS 190, 1 (arXiv:1007.0414)
- Tokovinin, A. 1998, AstL 24, 178
- Trofimov, M. 2026, CQG 43, 135020 (Timeflow Gravity)
- Vokrouhlický, D., Nesvorný, D., & Tremaine, S. 2024 (arXiv:2403.09555)

