# THE SPLIT — the two-paper architecture (2026-07-27, post-7L)

Source: PAPER.md v3.7 (the monolith, tag-ready as one paper). The split
converts it into two submission-ready papers per the standing queue
design (7I/round-9/round-10 rules), now updated for the accounting
round and 7L. This plan is the executable map; the prose assembly
follows ratification of the OPEN DECISIONS at the bottom.

## Standing drafting rules (fixed, from the log; round-11 additions marked)
- STRUCTURAL INDEPENDENCE: no §2.4 (mechanism) gestures in Paper 1;
  the gravity-law layer appears there only as "an amplitude α relative
  to an externally calibrated modified-dynamics family". §2.4 stays in
  the log (neither paper).
- "Pre-registered" = bar-locking, defined at first use (both papers).
- α is quoted CONDITIONAL on its measured curves, never bare.
  (Round 11:) the ABSTRACT carries the conditional form with the
  DEFENSIBLE span: α ≈ 0.6–0.8 across noise-cap choices inside the
  physical envelope, with the 0-reaching configurations quoted as
  REJECTED-PREMISE conditionals (the noise corner excluded by the
  census cliff + Lindegren; the forced-multiplicity cell by the
  certificate's q-table at −162 and its own 135–153 misfit) — NOT
  "0.0 to 0.8, each defensible" (Opus's phrasing, corrected).
- (Round 11:) the NOISE-CEILING CURVE is the primary reporting form
  for α's noise dependence — both envelopes marked (Gaia-physical;
  the sky's demanded 4.3×), the width object's identification stated
  OPEN, and the bracket logic stated: with the object unmodeled, the
  low branch absorbs boost into noise and the high branch risks
  object-as-boost masquerade; identification collapses the curve to
  a point (the successor instrument's stakes).
- TENSION-AS-RESULT (round-10, adopted): the reconciliation of the
  field's conflicting verdicts inside one pipeline IS Paper 1's
  headline contribution, not a by-product.
- (Round 11:) 7K-b is Paper 1's FLAGSHIP empirical object (the
  self-defending pair — an internal-consistency argument, robust to
  the absorber accounting). Two rules travel with it everywhere: the
  Poisson P is never quoted without the counts (9, 2) adjacent; and
  "no configuration reproduces it" is a SEARCH statement — the
  searched space is enumerated at every claim site (landed grid +
  posterior-adjacent variants + smear-off + physical-noise +
  boost cells).
- Corrections appendix: each paper carries the subset of the eighteen
  that touches its claims, renumbered, with the full-log pointer.
- Superlatives ledger-scoped; credences stated with numbers where
  stated at all.

## PAPER 1 — the binaries (methods + accounting paper)

Working titles (pick one, or supply your own):
  (a) "The Wide-Binary Velocity Anomaly Under Measured Nuisances:
       a Width-Budget Accounting That Reconciles the Field"
  (b) "Four Absorbers, One Width Budget: the Gaia Wide-Binary
       Gravity Test With Its Nuisances Marginalized"

Thesis: the three published verdicts (Chae boost; Banik/Cookson
Newton) are configurations of one shared width budget; measured
inside a single pipeline with the companion sector *measured* (not
fenced), the width channel parameterized, and the direction channel
active, the binary anomaly is: a fit-level α = 0.68–0.80 conditional
on its noise-ceiling and anchor curves (AMBIGUOUS-CARRIED at the
certificated anchor), a model-light median excess half-covered by
absorbers, a self-defending perpendicular (band, cliff) pair, and a
function-robust Newton rejection with NO discrimination among
modified laws. Every rival result is reproduced inside the pipeline
(Banik's 69% by unfencing; Cookson's flatness by their cuts) with
the arithmetic of the disagreement stated.

Section map (source = PAPER.md v3.7 unless noted):
  1. Introduction — the three-way conflict; the four-absorber
     diagnosis as the organizing claim (from §5 intro + §6.3
     methodological conclusion; tension-as-result framing).
  2. Data + the 2D (ṽ, γ) likelihood — §5.1–5.3 + the v7 envelope;
     the perspective correction (§7.4(b)).
  3. The companion sector MEASURED — §6.3's completeness arc:
     kernel discovery (vector common mode), the certificate
     (f̂ = 0.159 six-way; twin-heavy q-shape +162; conversion band
     [0.10, 0.39]); El-Badry & Rix §2.4.1 bracket (NOTES lit-note).
  3b. (Round 11, NAMED SECTION) "The multiplicity interface" — the
     structural result that a SCALAR companion fraction is the wrong
     interface between photometric and kinematic measurements: the
     photometric host ≈ 0.30 and the kinematic preference ≈ 0.1 are
     the SAME population read through differently-q-weighted moments;
     the q-resolved conversion (twin-heavy → kinematic-equivalent
     0.10–0.15) dissolves the apparent ×3 tension at measurement
     grade; the residual open item is the joint population fit.
     Stated as resolved-then-residual, NOT as an open tension (the
     round-11 staleness correction); cross-referenced from Paper 2's
     nuisance section. Also the standing correction: the forced-
     multiplicity cell is not "indifferent at +0.0" — the likelihood
     pays 135–153 to avoid it (this exact misread has now been
     corrected twice in review).
  4. The width channel + the phantom-veto — 7J-z width
     parameterization; 7J-g dual-config (the ṽ-only phantom α ≈ 0.5
     vetoed by γ); the methods centerpiece.
  5. The landed verdict and its curves — the anchored read
     (α_marg = 0.74/0.70 @ +23.8/+23.2, AMBIGUOUS-CARRIED); the
     anchor-flat curve; the E-arm band; the arm suite (4/4, clean
     null); 7J-z6: the noise-ceiling profile (PHYS 0.80/+35 →
     operative → corner 0.00), the exchangeability arm, the shape
     exclusion set + the population-sector successor.
  6. The accounting round — 7K-a (median half-absorbed, GRAY);
     7K-b (the census-pair self-defense; count retired); 7J-d
     (no function discrimination; Newton dead ≥ +7.9 × 32;
     the fenced-vs-landed lesson).
  7. Reconciliation — §7.3 Banik ablation (retained, relabeled);
     §7.4 incl. (d) the Cookson quantification (7L); the Chae
     comparison (§6 intro numbers). (Round 11:) the 7L close is
     stated as a WELL-POSED EMPIRICAL QUESTION, not a philosophy
     fork: "is the removed 92% data or contamination?" — with the
     settling measurement named (run the completeness + width
     machinery ON the removed subsample; TODO-logged, not opened).
  8. The open object + DR4 — the width-shape fingerprint
     (mid-shoulder, radial column, inner bins), the population-
     sector successor; the DR4 kill tests (band ×10, e-resolution).
  App A: corrections subset. App B: script map + LEDGER pointer.
  Census CSV released with the paper.

## PAPER 2 — the coefficients (galaxies-led)

Working titles:
  (a) "Measuring the Zero-Point Coefficient of the Radial
       Acceleration Relation"
  (b) "The RAR as a Planck Oscillator: Coefficients, Screening,
       and the Solar-System Lock"

Thesis (round-11 spine, replacing "two systems, one dial" — retired
outright, no caveated thread): **SPARC measures the coefficient
ladder — ĉ₁ interior with c₁ = 0 excluded in every treatment — and
the binaries, once their nuisances are marginalized honestly, do not
resolve it: an instrument-resolution finding, stated as the finding
rather than as a loss.** Contents: the ν = 1 + n_BE identity (C&T's;
we test); the screening index p; the second-moment channel to its
identifiability boundary; the function ladder with its controls
(Chae ambient, vertical channel); the a₀ temperature checks; the
amplitude-locked quadrupole tension (Saturn's veto with the MI
door). (Round 11, stated prominently as a METHODS finding:) the
sixteen-law ±8 lnL degeneracy under honest nuisances means the
entire binary function-contest apparatus — ours AND the field's —
measures nothing about the law family at current data; rotation
curves are where laws are told apart. The binaries appear ONLY as:
the α ≈ 1 external-field calibration (fence-conditional history +
the landed band, cross-referenced to Paper 1) and the HONEST 7J-d
erasure of the fenced binary-ĉ₁ indication.

Section map: §2.1–2.3 (identity, ladder, bath matrix — NO §2.4),
§3 (p), §4 (c₁ flat/hier, 4T–5A second moment, bath contests + the
survivors at SPARC grade), §6.2/6.4-as-erased (the binary
calibration + the honest erasure), §8 (quadrupole + MI door +
trajectory margin), §9 scorecard (galaxy rows + the erased binary
row), freeze statement, corrections subset.

## Mechanics
- PAPER.md v3.7 stays in the repo as the program log's assembled
  form (frozen at the split; header note added).
- New files PAPER1-BINARIES.md / PAPER2-COEFFICIENTS.md assembled
  from the map; LEDGER rows get a paper1/paper2 column note only if
  requested (default: both papers cite LEDGER.csv as-is).
- World table: Paper 1 quotes the binary column (unsuspended,
  no-discrimination form); Paper 2 quotes the galaxy columns.
- Publication path unchanged: colleague review → Zenodo DOI →
  arXiv (endorsement).

## OPEN DECISIONS (need the author)
1. Titles: pick (a)/(b)/other for each paper.
2. Does Paper 1 state the program credence (~50%) explicitly in the
   discussion, or keep credences in the log only?
3. Author line: "Filip Hájek — independent researcher" on both,
   acknowledgments as in the monolith?
4. (Round 11, Opus's orphan flags — adopted as options:) two
   standalone short-note candidates spun off Paper 1's nuisance
   sector, each a measurement with its own audience: (a) the
   wide-pair photometric coherence vector (ρ 0.86 → 0.36 with color
   separation — stellar astrophysics; the binary-photometry crowd);
   (b) the 0.2–50 kAU subsystem rate + twin-heavy q-shape (formation
   channels). Publish as notes alongside Paper 1, or keep as Paper 1
   sections with visible subsection titles? (Costs: two more
   author-name artifacts; benefits: the audiences that need them
   will actually find them.)
5. The reply to Opus (round-11 adjudication: adopted 1–6 with
   corrections A/B/C): drafted only on your go-ahead, per the
   standing rule.
